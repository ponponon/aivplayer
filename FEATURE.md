# AIVPlayer 功能列表

- 字幕翻译和内容总结新增动态网络路由：优先使用当前可用的环境代理或 QuickQ 本地 HTTP / SOCKS5 端口，否则使用 Electron 系统网络；每次请求按短时缓存重新探测，代理在应用运行期间开关或端口变化后无需重启即可切换，并在代理连接失败时回退系统网络。
- 内置托管翻译服务新增京东云大陆入口：服务器端以双 Docker 实例、健康检查、自动重启和 OpenResty 故障摘除提供进程级高可用；AIVPlayer 每 30 秒探测 Cloudflare 全球入口和大陆入口，优先使用可达的全球入口，并在大陆网络或全球入口失败时自动切换，不把智谱 API Key 放入客户端。

- AI 服务设置改为清晰的服务管理页：用一张紧凑列表统一展示服务、配置、状态和操作，当前服务单独突出显示；新增或编辑自定义服务时打开聚焦弹窗，保存才写入配置，取消、关闭或按 Esc 不会留下空白档案。

- 任务中心支持删除单条终态记录：已完成 / 失败 / 已取消的任务行提供垃圾桶按钮，点击后同时从内存列表和 `task-center.json` 持久化中移除，重启后不会复活；运行中任务仍只提供取消入口，不影响原有取消、重试、清除已结束和批量重建语义。

- AI 工作流状态条采用真正的非阻塞悬浮提示：运行、失败、取消和自动完成提示显示在顶部，不占用播放器布局空间；提示背景穿透鼠标，只有取消、重试和打开导读等操作按钮接收点击。

- 模型下载提示升级为可解释的进度反馈：顶部和 ASR / AI 设置入口统一显示百分比、已下载 / 总大小、实时下载速度与预估剩余时间；当响应没有总大小或速度样本不足时显示计算中，并使用语义化进度条和减少动效支持。

- 官网下载在浏览器无法可靠提供 CPU 架构信息时，推荐按钮会明确提示选择平台和架构；点击后自动展开、滚动并聚焦到手动选择区，不猜测架构，也不静默下载可能不兼容的安装包。

- 自动更新检查与提醒分离：启动后延迟 1 分钟首次检查，运行期间每 6 小时检查一次；同一版本关闭提醒后 24 小时内不再自动弹出，手动检查仍可立即查看，跳过版本继续永久跳过。

- macOS 关闭主窗口后仍保持单实例后台运行；从 Finder、命令行或其他应用再次打开视频时会自动重建并唤起播放器窗口，不需要手动点击 Dock。

- 自动更新体验收口：更新偏好明确为后台检查，发现版本时使用右下角通知栏；下载、进度和重启安装分别由对应状态展示，避免同一更新同时出现在两个提示层。

- 自动更新提示改为右下角非模态通知：不再使用全屏遮罩或抢占焦点，播放器可以继续操作；版本下载、重启安装、稍后提醒和跳过版本仍保留原有流程。

- 官网各平台下载默认格式使用显式优先级：macOS 优先 DMG、Linux 优先 AppImage、Windows 使用唯一的 EXE 安装器；发布清单同时包含多个格式时，推荐下载和默认手动选择不再依赖 JSON 键顺序，其他格式仍可手动选择。
- 模型下载完整性统一：ASR、Vision 和人物抠像共用远程 manifest、文件大小 / SHA-256 校验、唯一临时文件和原子替换；损坏或版本不匹配的缓存会失效并重新下载，ASR 默认源失败时自动按 R2、ModelScope、Hugging Face 回退。ASR 模型清单同时固定 large-v3-turbo-q5_0 和 small-q5_1 的文件大小与 SHA-256，避免“文件存在但实际模型错误”。
- Vision Pack 下载兼容旧 R2 版本清单：线上历史清单缺少 revision 时继续按版本 / 平台路径下载并校验归档；新清单继续使用 revision 内容寻址和 active pointer，不要求用户手动清空旧缓存。

- Vision Pack 改为内容寻址存储：R2 中同平台的视觉运行时按内容 revision 只存一份（revision 由打包后的 node_modules 逐文件哈希生成），app 版本只写一份小映射清单（`vision-pack/<版本>/<平台>/manifest.json` 指向 revision）；依赖未升级时不同版本共享同一份压缩包，消除跨版本重复上传与存储，发布流水线检测 revision 已存在时跳过内容上传。应用下载时先取版本映射再按 revision 拉取内容，本地已安装同 revision 则直接复用，不再重复下载。

- 新增可复用的 R2 大文件分片上传脚本：通过 S3 Multipart Upload 绕过 Wrangler 300 MiB 单文件限制，支持失败重试、状态文件断点续传、对象元数据和 SHA-256 校验，适用于模型与其他大体积资源发布。

- ASR 首次启动自动准备：正式安装包启动后会在后台优先从 AIVPlayer R2 官方镜像下载推荐 Whisper 模型，不阻塞播放器界面；失败时按 ModelScope、Hugging Face 顺序回退，下载进度和失败状态会显示在顶部，失败后下次启动会继续重试；whisper.cpp、FFmpeg、ffprobe 和 HEIF 原生运行时仍由正式安装包内置，Vision Pack 与视觉模型继续按需下载。

- Linux Snap 打包恢复：改用 electron-builder 原生 snapcraft target（`snapcraft` 配置，core24 / strict / stable / LXD 隔离构建，gnome extension 自动处理 GTK/主题依赖），自动生成 snapcraft 配置并处理 chrome-sandbox、desktop 入口；废弃的手写 `snap/snapcraft.yaml`（version 停留在 0.3.1、缺 FFmpeg/whisper/heif 运行时）已删除；发布流水线 x64 job 构建 `.snap` 并随 GitHub Release 与 R2 下载清单发布，CI 格式契约与证据校验同步支持 `.snap`（squashfs magic 校验），ARM64 暂不构建 snap。

- 字幕翻译新增开箱即用的内置托管服务：默认通过保留的 `aivplayer-translation.ponponon-universe.workers.dev` 调用服务端固定的 `glm-4-flash-250414`，客户端不携带 BigModel API Key；高级用户可以切换到自定义 OpenAI-compatible 服务，并继续使用本机加密保存的 API Key 配置。

- 新增 Cloudflare 智谱翻译 Worker：客户端不携带智谱 API Key，Worker 使用 Cloudflare Secret 转发固定的 `glm-4-flash-250414` 请求，并通过设备 / IP 短时限流、Durable Objects 每日配额、请求体积和输出 Token 上限保护公共免费接口。

- Linux `.deb` / AppImage 改用 512×512 的 hicolor 标准图标源；Ubuntu GNOME 可以正确找到 `aivplayer` 图标，不再因只安装 1024×1024 图标而显示默认齿轮；Linux x64 / ARM64 发布 job 会检查最终 `.deb` 中确实包含该标准尺寸图标。

- 关于面板新增 ISO 8601 格式的发布日期（`2026-08-20`）：macOS 原生 About Panel、跨平台关于弹窗和设置页统一显示发布日期，并随界面语言切换标签。

- 0.6.0 macOS 发布链路已接入 Developer ID Application 签名、Hardened Runtime 和 GitHub Actions 公证凭据；正式发布只提供经过签名和公证的便携式 DMG / ZIP，不再构建或上传 `.pkg`，也不再携带 Windows `aivcli.cmd`，避免 macOS 深层代码签名失败。
- macOS 自动更新发布包会在运行时复制和最终打包检查阶段确保 native runtime 文件具备所有者写权限：可执行文件保持 `0755`，动态库等 sidecar 使用 `0644`，避免 ShipIt 清理 quarantine 属性时因只读文件放弃替换应用。

- 官网下载清单的 R2 自定义域名已配置只读 CORS，允许 Cloudflare Pages 和本地预览读取 `download-manifest.json`；避免浏览器跨域拦截后误触发 GitHub 回退链接。

- 官网下载推荐的架构判断改用 UA-CH 高熵 `architecture` / `bitness` 异步提示，不再从普通 UA 字符串猜测 x86 / ARM；浏览器无法可靠确认时会引导手动选择，避免把不确定的设备静默发到错误安装包。

- 发布同步使用 `CLOUDFLARE_API_TOKEN` 通过 R2 REST API 上传当前版安装包；超过 300 MiB 的资产会在上传前明确阻止，当前版本全部成功并生成清单后，才清理所有更老的 R2 安装包对象。

- 官网下载区提供当前版本自动下载和平台 / 架构手动选择；更早历史版本继续通过已验证的 GitHub Release 直链访问。

- 官网下载推荐会避开 macOS 浏览器常见的 `MacIntel` 误报：在没有可靠架构信息时按当前可用安装包推荐，并保留真实直接下载链接；Apple 平台图标改用 Simple Icons 的正式 SVG 路径。

- 官网下载手动选择器升级为品牌化可访问组件：使用平台 SVG 图标、自定义下拉菜单、键盘导航、焦点状态和移动端布局，避免暴露浏览器原生列表。

- 官网下载区改为单一自动推荐入口：根据用户的操作系统与架构从发布清单选择最佳安装包，同时保留手动平台 / 架构选择；R2 只保留最新版本的安装包，稳定清单放在 `aivplayer/releases/download-manifest.json`，更早版本继续从 GitHub Releases 下载。
- R2 桌面下载同步只保留最新正式版本：当前版本安装包和清单写入成功后删除所有更早版本对象；历史版本仍以 GitHub Releases 为权威来源，避免下载镜像持续占用存储空间。

- Cloudflare Pages 官网完成四语国际化：支持简体中文、English、日本語和한국어，按已保存选择、浏览器首选语言自动匹配并允许手动切换；官网界面预览使用真实 Electron 截图，上传到 Cloudflare R2 的 `aivplayer/site/screenshots/` 并通过 `releases.quniv.cn` 引用，不进入 Git。

- 视觉索引改为完全手动触发：打开视频、启动应用、保存影视库目录或扫描全部目录都不会自动消耗 SigLIP2 / FFmpeg 算力，用户需明确点击“批量建立影视库”或收件箱的“加入索引队列”后才开始处理。

- 播放控制区的“一键完整处理”入口精简为单独 Logo 按钮：保留点击、快捷键、无障碍标签和悬停提示，减少底部控制区的视觉占用。

- 视觉运行组件未安装时不会影响普通播放和媒体导入：视觉面板提供 Vision Pack 与 SigLIP2 模型的按需下载引导，并过滤历史内部组件缺失错误。

- 0.5.5 安装包瘦身：SigLIP2 模型改由 Cloudflare R2 统一存储并在视觉功能首次使用时下载；Transformers.js、LanceDB、Apache Arrow 及 ONNX/Sharp 原生依赖拆为按平台构建的 Vision Pack，主安装包只保留播放器、FFmpeg、HEIF 和 whisper.cpp；发布流水线生成平台体积报告并把 Vision Pack 上传到 R2，Vision Pack 与模型均按 SHA-256 / 固定 revision 校验。

- 发布流水线构建优化：macOS 只安装 libheif 实际使用的 `libde265`、`kvazaar` 和 `jpeg-turbo`，改用 Homebrew 预编译包，避免无关的 FFmpeg 全依赖源码编译和 x265 链接失败；libheif 使用 Ninja 和明确的 CPU 并行度，macOS 额外缓存按 runner 架构和固定源码校验值隔离的 FFmpeg 编译产物，五个平台共享 Electron 下载缓存，五个平台按架构缓存可直接打包的 FFmpeg、libheif 和 whisper.cpp 原生运行时，Windows x64 / ARM64 额外使用 vcpkg binary cache、源码下载缓存和固定 FFmpeg 归档缓存，减少重复发布等待时间并降低上游源短暂不可用的影响。

- Windows NSIS 安装包兼容性修复：`electron-builder.yml` 中同时设置 `useZip: true` 和 `differentialPackage: false`，确保 NSIS 内置负载实际使用通用 Zip 格式，解决 Windows ARM64 安装包中的 7z ARM64 BCJ 滤镜导致旧版 `nsis7z.dll` 静默解压失败的问题，确保全平台 Windows 设备均可正常安装。

- Windows NSIS 安装包改为标准安装向导：双击后显示安装流程，允许选择安装目录，固定创建桌面和开始菜单快捷方式，并在安装完成后提供启动 AIVPlayer 的选项；后台自动更新仍继续使用静默安装流程。

- Microsoft Store MSIX 发布链路已接入：Windows x64 / ARM64 发布作业在配置 Partner Center 身份变量后，会额外生成独立的 `.appx` Actions artifact；GitHub Release 继续使用 NSIS `.exe`，商店版运行时检测 `process.windowsStore` 并关闭 `electron-updater`，由 Microsoft Store 管理更新。
- 新增公开 Code signing policy：记录 SignPath Foundation 免费签名范围、项目角色、构建审核、隐私边界和发布流程，并接入 README 与产品下载页。

- Cloudflare Pages 新增公开隐私政策页：说明本地媒体与项目数据、用户主动配置的 AI 服务、模型下载、局域网 Web 播放和更新检查等数据流，并提供中英文内容供应用商店审核使用。

- 说话人 Provider 已建立独立的模型契约：模型目录按版本隔离，状态检查同时核对分段模型、说话人向量模型和随附许可证文件，并明确识别各平台原生包能力；当前只完成能力诊断，尚未自动下载或打包模型。
- 说话人 Provider 运行时已接入 sherpa-onnx 原生 Node API 的本地调用边界：只接受 16 kHz WAV，按本地模型路径执行分段并输出统一的秒级 speaker segment；模型缺失、平台不支持、原生模块加载失败和采样率不匹配都会明确报错。
- 说话人 Provider 已接通主进程 IPC：从媒体文件使用内置 FFmpeg 抽取 16 kHz 单声道 WAV，执行本地分段后通过 preload 返回统一结果；临时音频会在任务结束后清理，并对相同请求复用进行中的任务。
- 视频设置中新增本地说话人 Provider 状态卡，展示平台能力、模型文件状态、模型目录和可刷新诊断；四种界面语言均有对应文案。
- 新增 `smoke:speaker-diarization` 真实验证入口：通过环境变量指定本地模型目录和 16 kHz WAV，输出耗时、分段数和 speaker id；脚本不会联网下载测试资产。
- 视觉面板新增本地说话人分段入口：针对当前视频执行分析，显示可点击的 speaker 时间段并跳转播放头；结果同时写入本地视觉 evidence，不改写视频或字幕。
- 新增说话人证据转换层：分段带源指纹、模型版本和中英检索文本，使用 `speaker` evidence 类型独立于字幕、场景和实体证据。
- 新增按媒体指纹隔离的说话人标签目录模型：可为当前视频的 speaker 编号保存名称和别名，搜索结果只投影同一源指纹的标签，不把 speaker 编号当作跨视频身份。
- 新增说话人证据清理核心 API：可只移除当前视频的 `speaker` 视觉证据，保留字幕、画面、场景、实体和本地标签目录。
- 说话人面板新增当前视频证据清理按钮：清理后保留当前分段和标签编辑状态，用户可重新运行分析恢复索引。
- 视觉索引核心新增说话人证据源聚合和串行批量清理：可按视频统计 speaker 证据数量，批量动作只删除 speaker 行并返回清理计数。
- 视觉面板新增说话人证据来源管理卡：展示已落库视频、证据数量和生成时间，支持刷新、多选和批量清理，清理范围仍只限 speaker 证据行。
- 说话人 Provider 支持在应用设置中保存用户选择的本地模型目录；旧配置继续使用用户数据目录下的版本化默认路径，非法或相对路径不会被接受，也不会触发自动下载。
- 新增说话人标签目录的主进程存储 IPC 与搜索投影链路：标签写入本地目录后，可通过渲染层 API 读取/更新，并参与视觉文本、图片搜索结果展示。
- 说话人标签检索新增来源范围隔离：标签扩展查询会同时携带 source fingerprint 和 speaker 编号，结果合并前按二者精确过滤，避免不同视频复用编号时产生误命中。
- 新增说话人标签编辑 UI：分析结果可按当前视频为每个说话人设置名称和别名，时间段按钮与搜索结果会显示本地标签。
- 增加真实 Electron 说话人面板 Smoke：使用临时用户目录链接本地模型，验证 Provider 就绪、真实媒体分析、分段展示与播放头跳转。

- README 已完成国际化：`README.md` 作为英文主文档，并提供 `README.zh-CN.md`、`README.ja-JP.md` 和 `README.ko-KR.md` 三份本地化版本；四份文档顶部互相提供语言切换入口。

- macOS 原生运行时构建目标固定为 12.0：FFmpeg 及其 Homebrew 依赖从源码构建，whisper.cpp 和 libheif 的 CMake 构建显式设置 `CMAKE_OSX_DEPLOYMENT_TARGET`，避免 macOS 26 构建机把过高的最低系统版本带进发布包。

- Flatpak 离线 npm 构建会显式跳过 ONNX Runtime 的 CUDA 扩展下载，同时兼容新旧 `onnxruntime-node` 安装变量；CPU 运行时继续使用 npm 包随附的文件，避免构建阶段访问 NuGet。

- 发布 CI 新增 macOS FFmpeg / FFprobe 运行时门禁：对 `resources/ffmpeg` 中的 Mach-O 可执行文件和 dylib 执行 `file`、`otool -l` 与 `-version` 检查，所有切片的 `minos` 必须不高于 macOS 12.0，避免把构建机 macOS 26.0 等过新的最低系统版本带进安装包。

- 播放器新增 VLC 风格播放结束策略：可在控制条切换播放结束后停止 / 播放下一条、循环当前、循环播放列表和随机播放；策略写入应用设置，播放列表边界和单文件场景均有明确行为。
- 播放记忆升级为媒体指纹档案：基于路径、文件大小和修改时间生成稳定指纹，按媒体独立保存进度、音量、静音和倍速；文件被替换后不会误用旧媒体的播放状态，同时保留原路径进度作为旧设置兼容回退。
- 播放时间轴新增容器章节和用户时间书签：ffprobe 章节会显示为可点击标记，控制条可在当前时间创建书签，点击定位、右键移除，书签按媒体指纹持久化。

- 剪辑时间线新增 Pireel 风格构图预设：全景基线、重点特写、角落留白和左右分屏留白会按固定编辑基线一键应用到当前片段或多选主轨片段；预设遵守画布方向规则，横屏禁用角落、竖屏禁用分屏，同时保留底层构图控件继续做精细调参。
- 剪辑片段构图新增 Pireel 风格画布方向约束：竖屏优先并限制角落构图，横屏 / 方形优先并限制左右分屏；全景和特写两种通用构图始终可用，旧工程已有的非推荐值保留不做静默迁移，时间轴控件会显示当前方向和推荐状态。
- Pireel 人物抠像进入第三阶段：主轨片段可以持久化抠像开关、边缘柔化和描边参数，并按 source 指纹、源时间区间、模型提供方和采样率生成可复现缓存键；本地 Transformers.js MODNet 模型、原子下载、本地 alpha provider 和 mask track IPC 已接通，剪辑操作栏在模型就绪后可对选中片段启用抠像并调整参数，预览会按源时间播放缓存 mask、显示轨道生成进度、对 mask 做 alpha-only feather 并以同一画布尺寸映射描边参数，FFmpeg 成片导出会复用同一 mask track 做 alpha merge，并使用 `boxblur` / `dilation` 合成边缘柔化和描边，主轨滑入、淡出、上浮和缩放也会在透明前景上复用同一动效画布；未下载模型时不会隐式联网。
- 剪辑时间线吸收 Pireel 的磁性对齐交互：字幕、文字卡片和画中画拖动时，会统一吸附到播放头、视频片段切点、其他覆盖元素边界和整秒刻度；键盘微调也复用同一套吸附规则，并以“素材自身可用时长”作为上限，避免元素被拖出成片范围。
- 剪辑时间线新增 Pireel 风格的 Cmd/Ctrl 多选与框选：视频片段、字幕、文字卡片和画中画块可以跨轨道组合选择，按 Delete / Backspace 或批量删除按钮一次性删除；批量主轨删除会复用字幕与叠加元素时间压缩、撤销 / 重做和工程保存链路，选择状态本身不写入工程文件。
- 剪辑时间线新增多选叠加元素整体微移：字幕、文字卡片和画中画块在跨轨道多选后可统一向左 / 向右移动 0.1 秒，保持元素相对间距；移动会解除字幕 source 锚定、受成片边界约束，并作为一次撤销 / 重做和工程保存操作提交，主轨视频片段仍保持连续顺序。
- 剪辑时间线叠加轨道新增 Pireel 风格键盘精细移动：字幕、文字卡片和画中画获得焦点后，方向键按 0.1 秒移动，Shift + 方向键按 1 秒移动，继续复用时间线吸附、边界约束和撤销 / 重做链路。
- 剪辑时间线叠加轨道新增 Pireel 风格轨道排序：拖拽字幕、文字卡片或画中画轨道标签可调整前后层级，顺序写入项目文件并接入预览合成、FFmpeg 三层导出合成与撤销 / 重做；旧项目继续使用视频块 → 图形 → 字幕的默认层级。
- 剪辑时间线叠加块新增 Pireel 风格双边裁剪：字幕、文字卡片和画中画块都可直接拖拽左右边界调整成片起止时间，支持吸附、最短时长约束、字幕词时间裁剪，并作为单次撤销 / 重做和工程保存操作提交。
- 剪辑模式新增 Pireel 风格的脚本驱动剪辑：原文字幕会形成持久化脚本句子列表，点击句子可定位到成片时间；删除脚本句子会按 source 时间范围移除对应素材并压缩多素材时间线、字幕轨道，已删除句子保留原文和翻译状态，可按原 source 范围恢复，旧版 schema 1 工程文件继续兼容。
- 脚本剪辑内核新增纯函数操作和单独变更链路：源时间删除 / 恢复不会依赖 React 状态，恢复时保持其他 source 素材相对位置；脚本删除、恢复均进入撤销 / 重做和本地工程保存，便于后续接入 AI / CLI 编辑命令。
- 脚本剪辑新增 Pireel 风格原文行内编辑：双击句子或点击铅笔即可修改字幕文本，保存后同步脚本行与原文字幕预览 / 导出，并作为一次撤销 / 重做和工程保存操作提交；翻译文本、source 时间范围和删除状态保持不变。
- 脚本数据层新增 Pireel 风格逐词时间保留：有真实 ASR 词级时间时，工程脚本行会与字幕一起保存相对词时间，并提供源时间映射、保守填充词识别和删词后的文本重组纯函数；旧工程没有 `words` 字段时继续按句子脚本兼容，整句手动改文案会主动清除失效的词级时间。
- 脚本面板进入 Pireel 风格逐词删词 P1：有真实词级时间的句子会显示可点击词 token，点击后只删除该词对应的源视频区间并压缩后续时间线，同时同步脚本文本、字幕词时间、撤销 / 重做和工程保存；没有词级时间的旧字幕仍保持整句编辑体验。
- 脚本面板补充 Pireel 风格词级操作闭环：点击词 token 打开删除 / 替换浮层，替换只改文案并保留原 timing；Shift 连选或拖选多个词后可一次性删除，提供保守的中文 / 英文填充词批量清理，所有批量源区间删除共用一次撤销 / 重做和工程保存。
- 时间线范围选择和片段边界拖动新增轻量吸附：靠近整秒、当前播放头或已有片段切点时自动对齐；键盘微调也复用同一套吸附函数，避免鼠标和键盘产生两套时间语义。
- 时间线片段新增 Pireel 风格的片段级构图：选中片段后可切换全景 / 特写、调整 100%–250% 比例并选择左 / 中 / 右水平焦点；构图写入 `.aivproj`、播放头进入对应片段时同步预览，导出时由 FFmpeg 对每个片段执行同样的锚点裁切，旧工程默认保持全景居中。
- 片段级构图新增共享 framing resolver：按主轨片段边界生成去重关键帧，在全景 / 特写、角落缩小或左右分屏及焦点变化时用短时插值平滑过渡；Renderer 预览与 FFmpeg 导出共享同一组缩放 / 二维位移语义，新增构图尺寸独立保存为 0–100，人物抠像遮罩同步跟随，旧工程和未变化片段继续走原有静态裁切路径。
- 时间线视频轨会把构图关键帧边界显示为可点击的标记：悬停可查看目标构图和比例，点击直接定位并选中变化后的片段，立即进入该片段的构图编辑上下文；标记由共享 resolver 生成，不增加独立的隐藏时间线，也会随片段重排、裁切和旧工程默认值自动更新。
- 时间线片段新增 Pireel 风格的片段级颜色调整：亮度、对比度、饱和度按片段保存，播放头进入片段时即时应用 CSS 预览，导出由 FFmpeg `eq` 滤镜复现，恢复为中性值会自动清理工程字段。
- 片段级构图和颜色滑杆新增 Pireel 风格实时预览：拖动时只更新 Renderer 内存中的临时 clip patch，松手、键盘确认或失焦时才写入一次工程历史并保存文件，避免一次滑杆操作生成大量 undo 记录；临时状态不会进入 `.aivproj`，播放预览与最终提交仍复用同一套构图 / 滤镜计算。
- 时间线剪辑点新增轻量 Pireel 风格淡入淡出转场：转场挂在后一个片段的入点，支持 100–1000ms 调节，播放预览会从黑场淡入，导出时前后片段分别套用 FFmpeg 视频 / 音频 fade，并保持原时间线总时长。
- 时间线剪辑点扩展 Pireel 风格多效果转场：支持黑场、溶解、左右擦除、左右推入和缩放；旧 fade 保持原有黑场兼容路径，新效果使用 FFmpeg `xfade + tpad` 在原切点合成且保持总时长，预览同步使用同一转场类型语义。
- 时间线剪辑点补充 Pireel 风格圆形展开 / 交叉缩放转场：`circleopen` 直接映射 FFmpeg `xfade` 的同名效果，`crosszoom` 映射到兼容性更好的 `zoomin`，播放预览和导出保持同一入点时长语义；旧工程没有新字段时保持不变。
- 转场选择面板新增 Pireel 风格可视化预览卡：每种转场显示前后画面缩略示意，悬停时以低成本 CSS 动画预览方向、缩放、擦除和圆形展开，减少用户逐个试错的成本；不改变原有工程数据和导出语义。
- 主视频片段新增 Pireel 风格进场 / 出场动效：支持无、淡入淡出、左右滑入 / 滑出、上浮和缩放，动效时长写入主轨片段并由编辑预览与 FFmpeg 分段导出共享；旧工程缺失字段时保持静态。
- 时间线新增独立图形轨和文字卡片块：文字卡片与字幕分离保存，支持标题卡 / 标签、居中与四角位置、持续时长；图形块支持选中、修改文字 / 样式 / 位置 / 时长和拖动时间位置，编辑预览会叠加到视频画布，导出时由 Electron Chromium 栅格化中文卡片并交给 FFmpeg `overlay` 按时间合成，后续可直接承载 Pireel 的 corner/split 伙伴图形。
- 文字卡片位置扩展为 Pireel 风格 3×3 语义锚点：支持上、下、左、右及四角和中心，旧工程原有五种位置保持兼容，预览、画布拖拽和导出共用同一组坐标；边中点卡片会分别以横向或纵向中心线对齐。
- 图形卡片新增 Pireel 风格画布自由变换：选中的文字卡片可在预览画布中移动、调节左右宽度和旋转，支持靠近 3×3 画布锚点自动吸附、中心十字辅助线和 Shift + 旋转 15° 约束；坐标、宽度与角度写入工程并由预览和 Chromium 栅格化导出共用；没有新字段的旧工程继续使用原来的五位置预设。
- 文字卡片新增 Pireel 风格进场 / 出场动画：支持无、淡入、左右滑入、上浮和缩放，动画时长支持 100–1000ms；预览与 FFmpeg 导出共用同一时间语义，旧工程缺失动画字段时保持静态。
- 时间线新增独立画中画 / 分屏视频轨：视频块引用工程素材的源区间与时间线区间，支持左上 / 右上 / 左下 / 右下角落布局，以及主视频左侧 / 右侧分屏布局；支持调整素材起始时间、持续时长、块宽度、圆角和边框，支持拖动、删除和撤销重做，旧工程缺省字段自动回退到兼容默认值，编辑预览与 FFmpeg 导出共用同一数据模型。
- 剪辑时间线新增 Pireel 风格本地素材库：聚合当前工程已加入的视频 source，支持搜索、主轨插入、画中画插入和拖动素材卡到主轨 / 画中画轨的具体时间点；插入继续复用工程历史、字幕重排、filmstrip 和多素材 FFmpeg 导出，不引入网络素材依赖。
- 素材库卡片新增 Pireel 风格可视化素材墙：复用 source 级 filmstrip 显示缩略胶片，支持全部 / 已在时间线 / 未使用筛选，支持多选素材后一次性追加到主轨；批量追加一次性写入工程历史并沿用字幕重排与导出模型。
- 素材卡支持 Pireel 风格大图 / 视频预览；将素材拖到已有主轨片段会在保持片段时长、构图、调色、音量和转场字段的前提下替换源区间，片段内的源字幕锚点会改挂到新素材的相对时间，时长不足的素材会拒绝替换并保留原工程。
- 画中画 / 分屏视频块新增 Pireel 风格进场 / 出场动画：支持淡入、左右滑入、上浮和缩放，动画时长支持 100–1000ms；预览按播放头显示动画阶段，导出使用同一时间语义，出场阶段会冻结素材末帧，旧工程默认无动画且不改变原有成片时长。
- 时间线视频轨新增 Pireel 风格源时间锚定 filmstrip：每个素材独立异步抽帧并显示，剪切 / 分割只改变可见窗口，缩略图不会因为片段重排而重新采样；多素材工程按各自 source 映射，避免拿第一条素材的画面填充其他素材片段。
- 播放器进度条支持 Trickplay 连续预览：媒体加载后按时长预取多个时间点的缩略图，拖动或聚焦进度条时显示最近帧和时间码；缩略图按媒体路径、大小、修改时间、尺寸和质量写入用户目录缓存，重复打开媒体会直接复用已生成帧。
- 剪辑时间线新增片头 / 片尾 / 黑场结构分析：主进程复用 FFmpeg `blackdetect` 扫描实际素材，按媒体指纹、分析参数和时长缓存结果；编辑器显示带置信度的分析区间，点击可定位到对应成片时间，重复分析直接复用本地缓存。
- 播放器会在后台复用片头 / 片尾 / 黑场分析；播放头进入可跳过的黑场区间时显示跳过按钮，跳转到区间末尾，不改写原媒体文件。
- 结构分析结果支持按媒体指纹持久化“忽略本段”修正；编辑器结果列表可忽略或恢复区间，播放器跳过逻辑会遵守已忽略的黑场修正。
- 时间线新增 Pireel 风格自动场景切分：主进程通过 FFmpeg scene score 检测当前素材的场景切点，按源时间切开选中片段，保留原有时长、字幕、调色、构图、音量和撤销 / 重做语义，避免在 Renderer 里解码整段视频。
- 时间线新增智能去静音：主进程通过 FFmpeg `silencedetect` 扫描工程中实际使用的素材，合并相邻静音区间后按源时间批量删除，多个删除合并为一次撤销记录，并同步压缩字幕、图形、画中画和导出时长。
- 字幕预览和视频导出新增共享视觉预设：提供清爽、暖黄、薄荷、深蓝、金色衬线五套风格，关键词可按逗号 / 换行配置并在预览中高亮；烧录字幕使用对应 ASS 样式与关键词强调，外置字幕继续保持标准 SRT。
- 字幕新增 Pireel 风格逐词高亮：ASR 通过 whisper.cpp `-ojf` 保留 token 时间戳，普通播放和剪辑预览会按当前播放头高亮正在说的词；旧版 ASR 缓存、外部 SRT/VTT 没有词时间时按句内文本比例回退，编辑工程保存相对词时间，烧录字幕会生成 ASS karaoke 标签，外置字幕继续输出兼容 SRT。
- 逐词字幕会按字体、窗口 / 输出画布宽度估算中英文混排字宽，采用标点感知的均衡分组切换显示页；播放器预览和 ASS 烧录共用同一套分组、拼接和回退策略，长句不会挤成一行，也不会在导出后重新换行。
- 新增本地剪辑工程时间线核心：工程只保存媒体路径 / 指纹和保留的源视频区间，主视频轨按片段顺序生成成片时间；共享实现支持源时间与成片时间互相映射、分割、左右裁剪、区间删除、按源时间批量删除，以及删除后字幕 / 叠加元素的时间压缩，为后续 Pireel 风格时间线 UI 和 FFmpeg 多段导出提供统一基础。
- 视频播放器已接入单视频编辑模式 P0：剪刀入口切换到底部时间线，支持点击或键盘移动编辑播放头、片段分割、左右裁切、片段删除、撤销 / 重做、时间线缩放和字幕轨道占位；播放过程中会自动跳过被删除的源视频区间。时间线导出会把保留片段逐段编码并 concat 成片，外置字幕会同步重映射到成片时间，烧录字幕沿用现有 FFmpeg 字幕滤镜能力。
- 剪辑工程现在会按媒体指纹保存在本地 renderer 存储中，重新打开同一视频时恢复片段和字幕；视频时长或路径变化会自动放弃旧工程。已有原文 / 译文字幕文件会异步转换为时间线字幕块，字幕会随片段删除、裁切和撤销 / 重做一起保存。
- 剪辑工程支持通过原生文件对话框打开、保存 `.aivproj` 工程文件；工程文件只保存源视频引用、时间线片段和字幕，打开时会校验 JSON、检查源视频是否存在并自动切换到引用的视频，新建 / 重置会提供确认提示。
- 时间线片段支持点击选中、拖拽重排和键盘左右箭头重排；重排会进入撤销 / 重做历史，并优先使用字幕的源时间锚点重新计算字幕位置，避免片段顺序变化后字幕仍停留在旧位置。
- 剪辑模式支持从本地文件选择器添加多个视频素材，素材会按播放头位置插入主视频轨；工程维护 sourceId 到本地媒体文件的内存映射，播放头跨素材时自动切换预览源，导出继续复用多源 mediaPath 映射。
- 字幕轨道支持按 source 自动加载同名 `.srt` / `.vtt` 侧车字幕；字幕块可点击定位、键盘微调或拖拽移动，手动移动会解除源时间锚点，避免后续片段重排把用户调整覆盖掉。
- 编辑预览会直接按工程的成片时间匹配原文 / 译文字幕块；切换到第二个素材或拖动字幕后，画面叠加字幕与底部字幕轨道保持一致，退出编辑模式再恢复普通播放器字幕来源。
- 多素材时间线导出会由主进程并行探测各 source，以第一条素材为输出基准统一画布、帧率、像素比例和音频采样规格；无音频素材自动补静音轨，竖屏 / 横屏混剪会用黑边保持画面比例。
- 时间线导出优先使用工程内已经按成片时间调整过的原文字幕块生成 SRT；因此第二个及后续 source 的字幕、手动拖动后的字幕也会进入外置字幕或烧录字幕成片，不会再次按单一主视频源时间轴错位。
- 时间线支持选中片段后单独调节音量和静音；音频设置保存在片段级工程数据中，预览跨素材切换时同步应用，导出会对每段套用对应增益，同一 source 的不同片段也可以有不同音量。
- 时间线支持在视频轨空白区域拖拽选择成片时间范围，选区可点击垃圾桶或按 Delete / Backspace 删除；删除会跨片段切开并拼接剩余素材，同时复用字幕压缩、历史记录、工程保存和播放头校正链路。
- 视频片段边缘提供起点 / 终点拖柄，拖动只向片段内部收缩并在结束时提交单个历史操作；键盘聚焦拖柄时可用左右方向键按 0.1 秒微调。
- 时间线导出按钮旁新增导出摘要，实时显示实际成片时长、片段数量、第一素材输出画布分辨率 / 比例，以及片段级音量或静音处理状态，导出前即可发现关键参数。
- 时间线导出确认面板新增导出前资源审计：只检查实际被主轨、画中画和文字卡片引用的 source，覆盖文件可用性、源区间、最短时长和叠加元素时间范围；发现问题会显示具体素材并禁用导出，未使用的素材缺失不会误阻塞工程。
- 文字卡片控制新增 Pireel 风格可复用元素入口：内置主标题、角标、引语三类本地预设，可在当前播放头一键插入并继续编辑，不依赖网络生成服务；编辑操作栏在侧栏存在时自动换行，弹层统一从控件左侧展开，避免导出按钮或参数面板溢出到素材栏。
- 文字卡片支持 Pireel 风格的本地元素资产：编辑后的卡片可以保存为跨工程复用的元素，资产独立存储在 renderer 本地存储中，支持搜索、一键插入、重复内容去重和删除，不把视频文件或网络生成结果塞进工程文件。
- 剪辑操作栏新增 Pireel 风格主题模板：提供清爽、暖黄、薄荷、影院、金色五套字幕 + 文字卡片组合，可一键应用到字幕设置和现有图形轨，并支持保存、搜索、删除自定义主题；图形主题修改作为一次撤销历史写入工程。
- 剪辑主题升级为可持久化 Frame 视觉系统：主题额外保存清爽、贴纸、描边、影院、衬线五种 Frame 变体，统一驱动字幕 / 文字卡片的色板、字体、圆角、阴影和布局；编辑预览与 Electron 导出栅格化共享同一套 Frame 语义，旧工程缺失 `frameId` 时自动回退清爽 Frame。
- 剪辑字幕新增 Pireel 风格花字动效：工程保存无效果、当前词高亮、胶囊卡拉 OK、逐词弹出、重点冲入和编辑强调六种效果；有词级时间时按真实时间驱动，无词级时间时按句内时长均摊，编辑预览使用 DOM 动效，烧录字幕使用同一时间语义生成 ASS karaoke / 缩放标签，旧工程缺失 `captionEffect` 时回退无效果。
- 字幕烧录导出新增 FFmpeg `subtitles` / `ass` 滤镜能力探测：时间线和片段导出在选择烧录模式时提前提示 libass 缺失并禁用确认，主进程和导出核心仍保留服务端兜底；外置字幕和纯视频导出不受影响。
- 剪辑时间线新增可持久化多画幅画布：支持源画幅、16:9、9:16、1:1，预览阶段同步切换画布比例与 contain / cover 策略，竖屏和方形画布显示安全区参考线，导出分段、ASS 字幕 PlayRes 与最终尺寸共用同一份画布计算。
- 剪辑字幕新增 Pireel 风格画布布局：工程保存字幕横纵位置、最大宽度和字号；编辑模式将字幕叠加到画布内，拖动式参数控件即时更新预览，ASS 烧录使用同一坐标、字号和分行宽度，普通播放器字幕仍保持原有底部显示。
- 剪辑预览新增 Pireel 风格字幕画布直接编辑：字幕出现时会显示可拖动的字幕框，拖动框体移动位置、拖动左右手柄调整宽度并吸附画布中心；拖动过程使用本地 ghost，释放时只提交一次布局变更，继续复用工程保存与撤销 / 重做。
- 剪辑字幕新增 Pireel 风格双语字幕独立布局：原文和译文可分别调整位置、最大宽度和字号，旧工程继续使用原文布局默认值，预览与 ASS 烧录共用同一套布局数据。
- 时间线导出现在会先打开确认面板，允许选择无字幕、外置字幕或烧录字幕模式；确认后才启动 FFmpeg，并沿用现有导出模式偏好和字幕可用性校验。
- 时间线导出确认面板会预览默认文件名和保存目录，支持直接修改 `.mp4` 文件名，或通过系统保存对话框切换完整输出位置；确认后的目标路径会直接传给 FFmpeg，不会重复弹出保存框。
- GitHub Actions 已接入 Cloudflare Pages 自动发布：公开站点集中在 `docs/site/`，只有 `main` 分支的站点文件变化会先执行静态站点校验，再使用 GitHub Actions Secret 中的 Cloudflare API Token 通过 Wrangler 发布到现有 Pages 项目；发布手册、许可证和内部计划等 `docs/` 文档不会触发或进入 Pages 部署，Account ID 使用 GitHub Actions Variable 保存，不进入仓库文件。
- GitHub Actions 已接入自动发布工作流：推送 `v*` 标签时自动构建 macOS、Windows、Linux 安装包，创建 GitHub Release 并上传全部产物；也可以通过 workflow dispatch 输入 Release tag 手动重跑，tag 会真正传递给 Release action。桌面平台构建显式关闭 electron-builder 的隐式发布，避免构建阶段意外触发重复发布。
- 三个平台的发布工作流在 electron-builder 完成后会检查安装包资源目录，确认 Web `index.html`、Web 静态资源、平台对应的 FFmpeg 和 FFprobe 都已进入最终产物，避免出现“安装包生成成功但局域网 Web 或转码不可用”的漏包。
- 发布资源检查脚本支持显式传入 `--platform darwin|win32|linux`，可以在任意一台开发机上复核其他平台的安装包目录结构。
- electron-builder 会排除 LanceDB 包内重复嵌套的 HuggingFace、ONNX Runtime、sharp 和平台无关图片依赖，复用应用根目录已经验证过的运行时，减少安装包体积并避免携带两套不兼容的推理依赖；LanceDB 当前平台原生模块仍会保留。
- 新增无界面 `aivcli` 入口，复用 AIVPlayer 的 ASR、字幕翻译、媒体信息和视觉影视库服务；支持 `doctor`、`asr`、`subtitle convert/translate`、`media info`、`library scan/index/status/search`，默认复用已有字幕缓存，并提供 `--json` 输出，便于终端和自动化脚本使用。
- `aivcli batch` 支持对目录或多个视频顺序执行 ASR、字幕翻译和影视库索引；任务可用 `--asr`、`--translate`、`--index` 组合，默认单个视频失败后继续，支持 `--fail-fast`、`--recursive`、`--force`、`--output-dir` 和 `--json`；只做翻译时会读取视频旁边同名的 `.vtt` 文件。
- `aivcli batch` 支持断点续跑：状态默认保存到 AIVPlayer 用户数据目录，也可通过 `--state-file` 指定；`--resume` 会校验任务参数并跳过已完成且产物仍存在的阶段，视频大小或修改时间变化会自动失效对应状态，`--reset-state` 可安全创建新的任务状态。
- `aivcli batch` 对 ASR 和字幕翻译增加可恢复错误重试：默认每个阶段最多重试 2 次，可用 `--retry 0..5` 调整；网络、429、5xx 和超时使用指数退避，认证、参数、字幕缺失等不可恢复错误不会重试；每次等待前会把阶段、尝试次数、错误和下一次重试时间写入断点状态，进程中断后可配合 `--resume` 继续。
- 新增 AI 短剧文本工作室 P0：短剧项目、章节、章节事件、故事骨架、改编策略、分集剧本统一保存到用户数据目录的 SQLite；支持从 TXT / Markdown 文本识别中英文卷章并重复导入，事件提取、骨架、改编策略和剧本阶段会记录任务状态，已完成节点可自动跳过实现断点续跑；桌面端新增 AI 短剧工作室面板，可创建项目、导入小说并执行文本阶段，终端提供 `aivcli drama list/create/import/show/events generate/plan generate/script generate/run`，OpenAI-compatible 服务配置优先读取应用内安全配置，未配置时兼容 `AIVPLAYER_DRAMA_API_BASE_URL`、`AIVPLAYER_DRAMA_API_KEY`、`AIVPLAYER_DRAMA_MODEL` 环境变量。
- AI 短剧 Provider 配置已接入短剧面板：支持保存 OpenAI-compatible 地址、模型、API Key、本地 Mock 模式和连接测试；API Key 沿用 Electron `safeStorage` 编码，GUI 与 `aivcli drama` 共用本地配置，未配置本地 Provider 时继续兼容环境变量；CLI 增加 `aivcli drama provider show/test`，只显示脱敏状态，不支持把 Key 放进命令行参数。
- AI 短剧 P1 已增加资产与分镜文本链路：根据章节事件抽取角色、场景、道具资产，基于分集剧本生成结构化分镜大纲（镜头、时长、地点、角色、动作、对白、画面提示词、镜头提示词）；数据保存到 SQLite，支持强制重算和断点跳过，GUI/CLI 共用，尚未绑定具体图片或视频生成厂商。
- AI 短剧资产库已支持持续维护：AI 再次提取按资产类型和名称合并，不删除人工资产并保留就绪状态；桌面端支持角色、场景、道具的新增、编辑、删除、搜索、分类过滤和草稿 / 就绪管理，视觉提示词可供后续生成任务复用。
- AI 短剧已增加独立媒体生成任务队列和可恢复 Worker：图像、视频、音频各自拥有排队、领取、进度、完成、失败、取消、重试和停止恢复状态；任务与项目及资产目标关联，Provider 可上报实时进度，应用重启会把中断的运行中任务恢复为排队状态；桌面端可运行 / 停止队列，未配置 Provider 时会明确记录失败原因。
- AI 短剧节点图模板已保存为可校验 DAG：模板记录节点输入、输出、Provider、估算成本和连线拓扑，桌面端展示每个节点的执行合同，为后续按拓扑执行生成任务保留稳定数据结构。
- AI 短剧媒体 Provider 已接入统一 HTTP JSON 适配器：图像、视频、音频可以分别配置接口、模型、Provider ID、API Key 和单次成本；支持同步结果 URL、异步轮询、Base64 和本地结果路径，结果下载到用户目录并复用任务重试、取消、进度和时间线回流链路，媒体 API Key 继续使用安全存储。
- AI 短剧媒体生成队列新增宿主 Electron Smoke：使用本机临时 HTTP fixture 验证图像、视频、音频三类任务的请求、结果下载、任务中心终态、成本回写和应用重启恢复，不连接外部 Provider。
- AI 短剧图像任务新增 `openai-images` 兼容适配器：将统一任务参数映射到 Images API，支持根地址或完整生成路径、`b64_json` / URL 结果和常用尺寸 / 质量字段；其他 Provider 仍走通用 HTTP JSON 协议，不发生隐式切换。
- AI 短剧音频任务新增 `openai-tts` 兼容适配器：将提示词映射到 `/audio/speech` 的 `input`，接收同步音频二进制并原子保存；支持 voice、格式、速度、指令字段，明确拒绝 SSE 响应，避免把流事件误当成音频文件。
- AI 短剧视频任务新增 `ark-video` 兼容适配器：将统一视频任务映射到火山方舟 Seedance 的创建任务 / 轮询 / 视频下载协议，支持模型、比例、时长、分辨率、音频开关和首尾帧 URL 参数；未识别的参数不会被隐式转发。
- AI 短剧生成结果支持回流现有剪辑时间线：完成任务带有本地结果路径且当前已打开剪辑工程时，可复用现有素材源、主轨追加、撤销 / 重做和工程保存链路；未打开剪辑工程时不会修改时间线。
- 桌面安装包集成 `aivcli` 启动器：Windows NSIS 将启动器加入用户 PATH，Linux `.deb` 在 `/usr/bin/aivcli` 安装命令；macOS DMG / ZIP 保持便携，不修改系统 PATH。启动器只转发到 AIVPlayer 的 `--cli` 模式，不重复打包业务运行时。
- M4 Agent 剪辑第一阶段新增 `aivcli edit inspect/captions` 只读查询：复用严格 `.aivproj` 解析器，输出稳定的素材、成片时间线、字幕统计和脚本行检索结果；包含已删除脚本行与译文命中信息，不写工程、媒体或字幕文件，为后续 Proposal 提供可复核输入。
- M4 Agent 剪辑第二阶段新增 `aivcli edit propose delete-script`：基于工程 revision 生成确定性的 Proposal / Diff，展示删除源区间、原编辑时间线区间、保留区间、脚本行状态、字幕变化和预计时长；应用前会拒绝 stale 工程，CLI 仍只读生成方案，不写回 `.aivproj`。
- M4 Agent 剪辑第三阶段把脚本行删除接入桌面确认链路：删除动作先展示 Proposal 的删除 / 保留源区间、脚本行、字幕影响和时长变化，确认后才通过工程 revision 校验写入现有撤销 / 重做历史和本地工程缓存，确认期间工程被修改会拒绝应用。
- 脚本面板支持按住 Shift 多选未删除脚本行，一次生成包含多行影响的 Proposal；批量删除仍需人工确认，取消后不改变工程，确认后共用一次撤销 / 重做和本地工程保存。
- M4 Agent 剪辑第四阶段新增本机 stdio MCP：启动时固定一个 `.aivproj` 工程，只提供 inspect、captions 和 propose delete-script 三个只读工具；不监听网络端口，不提供 apply、任意文件浏览、写文件、媒体删除或 shell 能力，Proposal 仍回到桌面确认链路执行。
- 新增 `npm run smoke:editing-mcp`，在不启动 GUI 的情况下真实拉起 stdio MCP 子进程，覆盖 initialize、tools/list、inspect、Proposal 和 EOF，确认工具集合没有 apply 且工程路径保持固定。
- M4 Agent 剪辑第五阶段新增受控桌面确认桥接：`aivcli mcp serve <project.aivproj> --desktop` 通过按用户隔离的 Unix socket / Windows named pipe 把 Proposal 送到已打开的匹配工程，复用现有确认弹窗、revision 校验、撤销 / 重做和工程保存；默认 MCP 仍保持只读，不提供直接 apply、网络监听、任意文件、媒体删除、shell 或 Provider 凭据能力。
- 新增 `npm run smoke:editing-agent-bridge`，在临时 Electron 用户目录中启动桌面端和 CLI MCP 客户端，覆盖 Proposal 取消、确认、工程缓存落盘和 Renderer 控制台错误检查；该 Smoke 需要宿主 GUI 与本机 IPC 权限，不进入默认无 GUI 测试集合。
- LanceDB 的 Apache Arrow peer dependency 已显式声明为桌面运行时依赖，确保 electron-builder 生成的 macOS、Windows、Linux 安装包都把 `apache-arrow` 一起放入应用资源，Finder“打开方式”启动时不会因视觉影视库模块加载失败而崩溃。

- 播放器 CSS 已按页面职责改为语义化文件名，保留稳定的加载顺序，并通过自动检查禁止新增 `part-数字.css`，降低多分支并行开发时的文件名冲突。
- Linux / Windows 使用与应用主题一致的自绘窗口控件，最小化、最大化 / 还原和关闭按钮不再依赖突兀的原生 title bar overlay；macOS 继续使用原生 traffic lights。
- ASR 面板在 whisper.cpp 或 ffmpeg 未就绪时直接提供自动检测和手动选择入口，模型文件与运行时组件的安装顺序更清晰。
- 视觉影视库支持把视频按 3 秒间隔抽帧，使用本地 SigLIP2 `siglip2-base-patch16-224-ONNX` UINT8 模型生成 768 维归一化向量，并保存到用户数据目录下的 LanceDB；支持文字描述搜索和以图搜图，点击结果可直接跳转到视频命中时间点。
- 视觉影视库支持增量索引：按视频文件大小 / 修改时间 / 抽帧间隔 / 模型版本判断是否需要重算，未变化的视频跳过抽帧和模型推理；旁挂 VTT / SRT 变化时只更新字幕映射，不重复生成视觉向量。
- 视觉影视库的文字搜索支持混合检索，把 SigLIP2 视觉召回、字幕文本和视频文件名的匹配合并排序，并在结果中展示命中的字幕片段。
- 视觉影视库新增本地语义证据层：在保留原有帧、字幕和全文索引表的基础上写入 `video_evidence`，每条命中带有证据类型、源视频、精确起止时间、字幕文本、缩略图帧和源指纹；字幕搜索优先使用原始 cue 时间，不再把选段边界错误地落到最近抽帧。
- 视觉影视库支持可选的场景证据索引：用户勾选“分析场景切换并加入搜索”后，FFmpeg scene score 切点会转换为有界的场景片段，按源文件指纹写入 `video_evidence`，并尽量锚定到最近视觉帧；普通索引和收件箱流水线默认关闭，避免无意增加全量解码耗时。
- 视觉影视库支持可选的 SigLIP2 零样本实体标签：基于本地图片 / 文本向量相似度为视觉帧生成“人物、车辆、动物、食物、室内、室外、城市、自然、夜景、文字、背包、相机、电脑、手机、书本”等 `entity` 证据；使用固定标签目录、按 uint8 模型输出校准的最低相似度和 Top-K 限制，不做联网识别或人物身份聚类，框级检测由独立的本地 `object` Provider 负责，旧索引和普通收件箱流程不受影响。
- 视觉影视库新增本地实体标签目录：用户可以为固定模型标签改名、添加别名、隐藏或合并到另一个标签；目录以 `userData/library/vision-entity-catalog.json` 原子保存，搜索时按别名召回并将合并 / 隐藏规则应用到结果，不改写模型生成的 `video_evidence`。
- 真实 Electron Smoke 已验证自定义实体创建、用户目录重启恢复、真实 SigLIP2 可选实体索引生成 6 条 `entity` 证据，以及使用自定义标签搜索命中实体证据；Renderer 无错误。
- 视觉影视库支持保存本地语义搜索：可为当前文字查询命名并保存到 `userData/library/vision-saved-searches.json`，重启后恢复，点击即可按原检索模式重跑，也可删除；搜索名称和查询内容会做长度限制、去重和原子写入，不联网。
- 视觉影视库搜索支持按证据类型多选筛选：可组合画面、字幕、OCR、场景、实体、物体和说话人；文本搜索会扩大候选召回后再截取结果，以免类型筛选被默认分页提前截断，图片搜索、保存搜索和 Clip Inbox 也复用同一筛选合同。
- 视觉搜索结果支持按相关度、源文件时间或文件名稳定排序；当前排序方式和证据类型筛选会以版本化偏好保存在浏览器本地，不保存样本图片、媒体绝对路径或结果选段。
- 视觉搜索配置支持导出 / 导入 JSON：只迁移搜索名称、查询、模式和证据类型筛选；导入会去重并保留本机媒体路径、模型和 Clip Inbox 不变。
- 视觉搜索结果支持“全选当前结果”和“清空当前选择”，批量选择只作用于当前结果集合，随后仍可统一保存为 Clip Inbox 或创建剪辑工程。
- 真实 Electron Smoke 已验证视觉搜索结果全选当前结果、清空当前选择、单选以及重新搜索清除旧选择；全选不会扩大为全库选择。
- 视觉搜索结果支持按当前查询“加载更多”，从 24 条结果逐步扩大到最多 100 条；首次分页会建立短期本机内存游标，后续读取同一结果快照，追加结果会保留已有选段选择，不改变证据筛选、排序或全选当前结果的边界。
- 真实 Electron Smoke 已验证视觉搜索首屏 24 条、加载更多追加到 30 条、已有选段选择保留以及当前结果窗口结束后隐藏加载更多入口。
- 真实 Electron Smoke 已验证短期游标建立后索引新增数据不会进入原结果快照，继续加载仍返回原 30 条窗口，并拒绝跨文字 / 图片搜索类型复用游标。
- 视觉搜索结果支持将当前结果窗口或已勾选结果导出为 JSON / CSV；导出包含源路径、精确时间、证据类型、匹配文本、置信度和物体框，不复制媒体文件。
- 真实 Electron Smoke 已验证当前结果窗口 JSON 导出 2 条、勾选优先 CSV 导出 1 条，并实际通过桌面保存路径写入后读取文件校验格式。
- 视觉搜索结果支持按当前文字、图片或相似镜头查询重新扫描全库并导出 JSON / CSV，最多 100 万条；全库导出使用确定性分数 / ID 排序，不复制媒体文件，也不改变当前结果选择。
- 真实 Electron Smoke 已验证当前结果窗口只有 24 条且勾选 1 条时，全库 JSON 导出仍由任务中心完成 120 条结果，不受当前窗口和选择集合限制。
- 全库视觉搜索导出改为任务中心后台任务：查询和写入阶段会报告进度，支持从任务中心取消；JSON / CSV 先分块写入用户选择路径旁的临时文件，完整成功后再原子替换，取消或失败不会留下半成品，完成 / 失败 / 取消终态会保存到任务中心历史。
- 真实 Electron Smoke 已验证从任务中心取消全库导出后任务进入 cancelled，目标输出和 assembly 临时文件均不会落盘。
- 全库视觉搜索导出会把创建任务时的五张 LanceDB 表版本，以及实体目录、说话人目录快照写入任务契约；恢复 / 重试继续使用原版本和原目录规则，避免索引或本地别名在任务中断后变化导致导出结果漂移。
- 视觉搜索普通导出与全库导出共用相邻输出锁；应用内活动任务会提前拒绝同一路径，跨 AIVPlayer 进程或遗留锁会进入明确失败提示，锁只在写入任务结束后按 token 释放，并能回收已退出进程留下的锁。
- 当导出依赖的 LanceDB 历史版本已被清理时，任务中心会保留失败任务并明确显示具体表名 / 版本和“重新搜索后再导出”的处理建议，不会静默退回另一份索引结果。
- 全库导出失败或取消后，任务中心支持“用当前索引重新创建任务”：保留原任务审计记录，复用原查询和输出路径，但重新获取 LanceDB revision、目录快照和独立分块目录；真实 Electron Smoke 已覆盖按钮操作、新 taskId 和 manifest 落盘。
- 视觉证据来源管理支持按视频来源查看 OCR、场景、实体、物体和说话人派生证据的数量，并按来源 / 类型批量清理；清理不会删除媒体、字幕、基础视觉索引或人工实体 / 说话人标签。
- 视觉搜索结果可以通过共享纯函数归一化为源时间选段，自动合并同源重叠 / 相邻区间，并生成带 source 锚点和字幕的 `.aivproj` 工程；没有新增证据表或旧字段时，旧视觉搜索仍保留回退路径。
- 视觉搜索结果支持勾选多个时间证据，并在补齐真实媒体元数据后直接创建剪辑工程；工程会进入现有编辑模式，保留多素材 source、原始时间范围、字幕文本和撤销 / 保存链路。
- 视觉影视库新增本地 Clip Inbox：可将多个时间证据命名保存到 `userData/library/clip-inbox.sqlite`，重启后恢复，支持再次生成 `.aivproj` 或删除集合；只保存源文件引用和时间证据，不复制媒体文件。
- Clip Inbox 集合支持标签、源时间 / 时长 / 文件名排序、相邻选段合并和时间范围反选，并可通过桌面保存对话框导出 JSON、CSV、CMX3600 EDL；导出内容保留源文件路径、源时间、指纹和证据文本，便于继续进入剪辑工程或外部剪辑工具。
- Clip Inbox 已保存集合支持一键复制；副本继承标签、排序和选段证据，但使用新的集合 ID，后续修改副本不会覆盖原集合。
- Clip Inbox 支持勾选多个已保存集合后批量复制；批量操作一次性提交，最多处理 20 个集合，复制完成后保留原集合并将新副本置顶。
- 播放时间轴在原有无长度 Bookmark Marker 之外支持可命名、有颜色的 Segment；起点 / 终点和名称按媒体指纹保存在本地，片段可点击跳转、在菜单中删除，右键时间轴片段也可快速移除。
- Clip Inbox 会检查集合引用的源文件；源文件被移动或删除时集合卡片显示不可用数量，可批量选择替换媒体，系统会按文件名匹配、补齐新指纹和 metadata，并在新媒体过短时阻止错误修复。
- 视觉影视库支持后台增量索引队列：只有用户明确提交索引任务后才会排队，队列会合并重复路径、串行写入 LanceDB；手动索引会复用同一条队列并取消冲突任务。
- 视觉影视库为每帧建立轻量文本文档表，并使用 LanceDB n-gram FTS 索引同时召回字幕和文件名；新增数据通过 `optimize()` 纳入索引，旧数据缺少文档表时会自动重建，FTS 失败仍保留全表扫描回退。
- 视觉影视库支持选择文件夹递归扫描视频，扫描完成后可批量建立影视库；扫描和索引分别展示文件夹数、视频数、当前文件、帧进度、跳过数量、字幕更新数量，并提供取消操作。
- 视觉影视库会把已选择的目录持久化到应用设置，启动后自动恢复但不会自动扫描或索引；面板中可以手动重新扫描、切换目录或移除目录，缺失目录不会阻塞其他目录的操作。
- 视觉影视库支持一键扫描全部已保存目录，按目录顺序显示当前目录、已完成目录数、累计视频数和不可用目录数；扫描只负责发现视频，用户点击“批量建立影视库”后才会提交索引任务。
- 视觉影视库的 SigLIP2 归一化向量使用 LanceDB `dot` metric；当 `video_frames` 达到 10,000 帧时自动建立 `IVF_FLAT` ANN 索引，增量写入后按未索引数据阈值维护，并自动迁移旧的错误 metric / 索引配置，小数据集继续使用精确检索以避免无意义的索引训练开销。
- 视觉影视库的批量索引进度拆分为变化检查、模型加载、视频帧处理、可选场景证据、可选实体证据、可选物体证据、向量 ANN 索引维护和全文索引维护阶段；视频帧处理完成后会继续报告场景 / 实体 / 物体分析和 LanceDB 索引维护状态，取消或失败时也会携带明确阶段。
- 视觉影视库会记录变化检查、模型加载、帧处理、场景证据、实体证据、物体证据、向量索引、全文索引和总耗时；索引完成、取消或失败时，面板会展示阶段耗时，便于定位模型推理、抽帧、场景 / 标签 / 物体分析或 LanceDB 维护瓶颈。

## 核心播放
- Desktop 支持一键启动仅绑定局域网的 Web 播放服务：当前播放列表会以随机访问令牌保护，通过 Chrome / Firefox / Safari 等浏览器打开媒体库，服务停止后令牌和媒体映射立即失效，不暴露任意本地文件路径。
- LAN Web 播放支持 HTTP Range / HEAD 响应，5GB 级别视频按浏览器请求的字节区间流式读取，不把整部电影加载进内存；前端播放器支持时间轴拖动、原生全屏和旁挂 SRT / VTT 字幕。
- Web 媒体库会显示 MP4 / WebM / OGV 等浏览器大概率直播放格式、MOV / MKV / MPEG-TS 等需要实测的格式，以及 AVI / FLV / WMV / 3GP / VOB / MXF / RMVB、F4V、OGM、NUT、DV、GXF、CAVS、Dirac、R3D、WTV、FLI/FLC、RoQ、Smacker、Motion JPEG、Bink、Y4M 和 H.264/H.265 raw stream 等通常需要转码的格式；详情页按需读取 ffprobe 元数据并对 HEVC、DTS、TrueHD 等高风险编码提示转码。
- 浏览器无法直接解码的媒体可以按需调用本机 FFmpeg 转成 H.264 + AAC 的兼容 MP4；转码以临时文件原子落盘、按源文件指纹缓存，Web 端显示转码进度并继续通过 Range 播放，原始 5GB 文件不会被覆盖或复制进内存。
- 多个局域网设备同时请求不兼容视频时，Web 转码按本机并发上限排队执行，网页端区分“等待队列”和“正在转码”，避免多部大视频同时转码打满 CPU、磁盘和内存。
- Web 播放器会按媒体 ID 隔离转码回退和轮询状态，切换文件时不会让旧文件的详情、缓存版本或转码锁串到当前播放项；ffprobe 识别出高风险编码后，当前详情面板会立即显示对应的转码提示。
- Web 页面遇到至少 1GB 且需要转码的媒体时，会提前提示首次转码可能耗时，并额外占用约源文件大小的本机磁盘空间；原文件不会被改写。
- Web 共享可以从桌面端选择一个或多个目录，递归发现视频并忽略其他文件；网页端“刷新媒体库”会增量重新扫描，新增文件可见、已删除文件自动移除，已有媒体 ID 保持稳定。
- Web 分享弹窗会展示所有可用局域网地址，而不是只显示首个网卡地址；多网卡、Wi‑Fi 与有线网络并存时，用户可以分别复制或打开适合手机/平板的地址。
- Web 分享弹窗会在本机为每个局域网地址生成二维码；二维码只包含当前访问地址，不经过第三方服务，手机相机可以直接扫描进入共享页面。
- 新增 `smoke:web-share-dialog` 桌面端回归入口：实际启动打包版分享弹窗，检查每个地址的二维码 data URL，并验证 390px 手机视口没有横向溢出。
- 新增 `smoke:web-browser` 桌面端浏览器级回归入口：实际加载局域网 Web 页面、读取视频元数据、跳转到视频尾部，并确认浏览器发出了目标媒体的 HTTP Range 请求。
- 新增 `smoke:web:all` 聚合入口：按顺序执行格式矩阵、并发转码、大文件 Range、浏览器 seek 和分享弹窗回归，避免 Web 功能验证入口再次分散。
- LAN Web 页面提供轻量 PWA manifest 和移动端主屏幕元数据；添加到手机/平板主屏幕后仍通过局域网连接桌面端，不缓存或上传视频文件。
- Web 分享弹窗的“打开”操作改为调用 Electron 主进程的 `shell.openExternal`，统一交给 macOS、Windows 和 Linux 的系统默认浏览器处理；按钮会显示打开中、成功或失败反馈，并保留复制地址作为兜底。
- 大文件转码前会检查缓存所在磁盘的可用空间；缓存会清理过期产物和残留临时文件，并在源文件大小或修改时间变化后自动失效旧结果。
- Web 转码对同一源文件的并发请求采用 single-flight 去重；多个局域网客户端请求同一视频只启动一次 FFmpeg，不同视频继续受本机并发上限保护，并由 `smoke:web-concurrency` 验证 3 客户端、2 个源文件和 4 次 Range 播放请求。
- 新增 `smoke:web-real-file` 真实文件回归入口：可用打包版 Desktop 验证长视频的 ffprobe 时长、媒体库发现和文件尾部 Range 响应，默认覆盖约 84 分钟的本地测试视频，不把整部大文件读入测试进程。
- LAN Web 媒体库现在会按稳定媒体 ID 保存本地收藏、播放历史和断点续播；共享目录会构建可展开的“目录组 → 子目录 → 文件”树，并显示相对路径，列表支持缩略图、搜索、排序、收藏 / 继续观看 / 未观看筛选，缩略图按需由 FFmpeg 生成并缓存，不在启动时批量转码；树节点的展开状态和当前目录会持久化。
- LAN Web 媒体库补充 OpenList 风格的列表 / 网格视图、当前目录面包屑和媒体详情操作；详情页可下载原文件并生成当前共享会话有效的单文件访问链接，停止 Web 共享后链接随令牌失效，媒体流仍复用 HTTP Range。
- LAN Web 媒体库支持进入多选模式，对当前搜索 / 筛选 / 目录结果全选或逐项选择；可以批量收藏、取消收藏，并通过服务端流式 ZIP 下载选中媒体，ZIP 保留共享目录层级且不把视频整体读入内存。
- Web 批量下载限制单次文件数和总大小，继续复用访问令牌与媒体 ID 校验；当前目录刷新会保留目录上下文，排序、筛选、列表 / 网格视图和目录选择同时持久化到本地并同步到 URL，刷新页面后可以恢复浏览状态。
- 进入目录后可直接“下载当前目录”，打包范围只按目录树节点计算，不受当前搜索词、收藏或观看筛选影响；继续复用批量 ZIP 的媒体 ID 校验和目录层级，避免用户必须先手动全选。
- LAN Web 目录扫描现在会纳入 JPG、PNG、GIF、WebP、AVIF 和 BMP 图片；图片与视频共用列表 / 网格、目录树、搜索、收藏、批量下载和详情操作，选中图片后在播放器区域直接预览，不触发视频转码、字幕或音轨逻辑。
- LAN Web 新增当前共享会话的任务中心；视频转码从当前媒体切换后仍会保留排队、进行中、完成和失败状态，任务可点击回到对应媒体，失败任务可直接重试；页面刷新后不伪造旧任务记录。
- LAN Web 播放器提供可见的 Web 播放队列，队列跟随当前目录 / 搜索 / 筛选结果，支持点击队列项、上一部 / 下一部和播放结束自动进入下一部；同时支持从 ffprobe 发现的多字幕轨选择字幕；多个音轨可选择，服务端按需生成只包含所选音轨的 MP4，原文件保持不变。
- Desktop 与 LAN Web 支持状态联动：Web 每秒读取 Desktop 当前媒体、播放进度、播放 / 暂停状态、倍速和播放列表，默认跟随当前媒体并同步 Web 播放器的进度与暂停 / 播放状态，顶栏可关闭跟随；桌面端分享弹窗提供默认关闭的“允许远程控制 Desktop 播放”开关，开启后 Web 可发送选择媒体、播放、暂停、上一部、下一部和进度同步命令。
- 视频设置提供 GPU 加速开关：关闭时会在 Chromium 初始化前禁用 GPU 合成，设置确认后先保存配置再真正重启 Electron；启动阶段同步读取该偏好，不使用未经验证的 `--no-zygote` 作为 GPU 修复参数。
- 本地视频播放器，支持拖拽导入和文件选择导入。
- 播放按钮状态以当前媒体元素的真实 `paused` / `ended` 状态为准；媒体切换、自动播放、循环播放、编辑预览和时间更新都会统一同步，并拒绝旧媒体实例及延迟 React updater 的回写，避免视频正在播放时按钮错误显示为暂停或反之。
- 播放列表支持切换上一条 / 下一条，支持键盘快捷键。
- 播放列表标题栏提供播放历史入口，最近播放的视频会在本地按时间倒序保留，点击历史项可重新加入队列并继续已有播放进度。
- 播放历史最多保留 50 条，重复播放会自动置顶；历史面板支持单条移除、清空全部记录和空状态引导，不上传本地文件路径。
- 播放历史打开前会检查文件是否仍然存在，已移动或删除的文件会保留为不可用记录，并可直接移除，避免点击后才进入播放失败状态。
- 播放历史保存已验证的媒体总时长；存在播放断点时显示真实进度线，无法确认时长时不显示伪进度。
- 播放历史支持按“未看完”筛选，只有存在真实播放断点且尚未播放结束的记录会进入筛选结果。
- 播放历史检测到失效文件时提供带数量提示的批量清理按钮，只移除历史记录，不触碰本地文件。
- 播放历史条目支持右键快捷菜单，可直接播放、移除记录或在系统文件夹中定位；失效文件的播放和定位操作会自动禁用。
- 播放历史提供独立的 Electron Smoke 回归脚本，覆盖徽标、失效文件状态、单条删除、清空和本地持久化，便于发布前快速验收真实界面。
- 控制栏新增停止按钮，点击后会把当前视频停回 00:00，而不是只停在最后一帧附近。
- 停止按钮图标改成单层实心方块，不再用 CircleStop / SquareStop 这类双层 stop 图标，避免缩小后看成“回字”。
- 停止按钮从 transport 组里拆出来，作为独立的轻量按钮放在主播放键和音量条之间，层级更清楚。
- 停止按钮改成图标 + 文案的轻量 pill，不再只靠图形让用户猜语义。
- 停止按钮的 hover / 无障碍提示会明确写成“停止并回到开头”，让按钮行为和文案一致。
- 停止按钮用轻微 danger tint 做语义暗示，让它更像“重置动作”而不是普通工具键。
- 停止按钮会把 `S` 快捷键写进 tooltip 和 `aria-keyshortcuts`，让鼠标用户也能发现快捷键。
- 窄屏下停止按钮会收缩成图标态，桌面保留文案，避免控制条被长按钮挤压。
- 窄屏下 stop 会和 transport 同行，不再独占一整行，避免把倍速组挤出视口。
- 窄屏下倍速和全屏不再右对齐，改成居中堆叠，让整条控制栏更像播放器而不是工具面板。
- 音量区收紧为中等宽度，不再无限拉长，避免它把底栏视觉重心拽偏。
- 倍速选择收成速度胶囊，不再是裸下拉框，和其他控制按钮的语气更一致。
- 控制按钮加入轻微按压反馈，点击时有一点真实的“按下去”的手感。
- 播放控制这一行改成居中成组，不再左右两块分栏对冲，底栏整体感更强。
- 停止动作支持 `S` 键快捷键，键盘操作和按钮操作保持一致。
- 没有载入媒体文件时，播放控制栏会自动隐藏，只保留真正有意义的打开入口。
- 空状态下，Space / 方向键 / M / S / F 不再误触发播放动作，避免快捷键和界面状态打架。
- 空状态的主画面不再死板居中，而是把视觉重心略微压低，让它和右侧栏的表头节奏更接近，底部留白更统一。
- 主舞台和右侧栏的顶部内边距一起加了一点，让标题栏下面不再那么贴，整页更有呼吸感。
- 播放控制里的 transport 按钮组改成 pill 容器，并补齐 focus-visible 高亮，键盘和鼠标都更顺手。
- 倍速和全屏也收进独立的辅助操作 pill，底栏两侧的操作层级更一致。
- 窄屏下底栏改成纵向三段分组，避免 pill 组折行后显得松散。
- 支持音量、倍速、全屏和时间轴拖动控制。
- 全屏按钮改为视频元素级全屏，只放大当前视频画面，不会把标题栏、播放列表和其他软件界面一起铺满；按钮会同步进入 / 退出状态，按 `F` 或 `Esc` 也能切换。
- 视频区域支持单击播放 / 暂停、双击进入 / 退出视频全屏，按 `Esc` 退出全屏；单击和双击会被区分处理，不会因双击连续切换播放状态。
- 时间轴这一行也收成了状态条样式，当前时间和总时长不再像两个孤立标签。
- 底部控制栏按“播放 / 音频 / 辅助操作”重新分组，音量不再单独飘在中间。
- 视频播放完毕后，再次打开同一个文件会自动从头开始，不再停在最后一秒。
- 支持符合常用桌面播放器习惯的 `Space` 播放 / 暂停快捷键；监听器会避开输入框、可编辑区域和按钮原生空格操作，并在媒体切换后读取最新播放状态。
- 快捷键设置分组提供少量基础操作速查：Space、左右方向键、M、S、F、⌘/Ctrl + O、L 和 Esc，不引入复杂的自定义键位系统。
- 视频画面会读取媒体实际宽高比例，在播放器舞台内等比缩放并居中显示；竖屏视频不会再被横向播放器框裁剪。

## 图片工作区
- 顶部工具栏新增独立的图片工作区入口，不影响原有视频播放和字幕流程。
- 支持通过文件选择器或拖拽一次导入多张 JPG、PNG、WebP、GIF、BMP 图片，并在左侧列表中切换查看。
- 中央预览区支持透明棋盘背景，展示原图尺寸、输出尺寸和输出体积，图片内容保持等比预览。
- 图片预览对横图、竖图和长图统一保持原始纵横比，在棋盘画布内居中显示，不拉伸、不裁切。
- 图片和视频预览共用媒体尺寸约束，新增预览入口必须复用统一的自然尺寸 / 最大边界规则。
- 支持恢复原图、缩小到 50%、网页尺寸三种快速调整，以及宽高输入和锁定纵横比。
- 支持旋转 90°、水平翻转、垂直翻转、JPG / PNG / WebP 格式切换和质量调节。
- 支持设置目标 KB / MB 体积，使用 Canvas 本地自动二分质量，必要时继续缩小分辨率以逼近目标体积。
- 导出前会通过 Electron 保存位置选择器落盘，原图数据只在本机处理，不上传外部服务。
- 支持一次选择输出目录批量导出全部图片，统一复用当前尺寸、格式、质量和目标体积设置，并显示当前导出进度。
- 支持在输出格式与原图扩展名兼容时批量覆盖原图；执行前会二次确认，格式不兼容时自动禁用覆盖入口，避免误写文件。
- 图片批量导出补充了 Electron Smoke 回归，验证输出目录写入、重名自动递增和覆盖原图链路。
- 图片 smoke 会真实点击批量导出和覆盖原图按钮，覆盖文件夹选择、进度提示、确认弹窗和多张文件落盘。

## 应用设置
- 应用支持深色、浅色和跟随系统三种主题模式，默认保持深色；主题选择会保存到本机，跟随系统时会响应操作系统外观变化，且设置弹窗、侧栏、播放器控制和图片工作区会同步切换配色。浅色主题采用 Chrome 风格的白色 / 浅蓝色表面和单一蓝色强调色，避免旧金色与蓝灰色混搭。标题栏工具组还提供一键切换按钮，设置页下拉框可恢复跟随系统。
- 右上角提供独立的齿轮设置入口，不再和媒体信息共用图标语义。
- 设置弹窗顶部提供分组导航，方便在通用、界面、视频、字幕、截图录屏和快捷键之间快速跳转。
- 设置弹窗表单项采用说明在上、控件在下的排版，避免窄卡片里中文标签被挤成竖排。
- 从 ASR 工作区打开设置时，会自动滚到 ASR 分组，减少一次额外切换。
- 设置会记住上次停留的分组，下次打开会优先回到用户上次浏览的位置。
- 设置分组导航会随滚动自动高亮，保持当前浏览位置和顶部标签一致。
- 设置和下载弹窗都会锁住键盘焦点，关闭后会回到原来的触发按钮。
- 设置页支持界面语言切换，覆盖简体中文、英文、日语、韩语四种语言。
- 设置面板支持选择启动时默认打开的侧栏，避免每次启动都手动切换。
- 设置面板支持选择默认媒体文件夹，并可一键清空。
- 设置面板支持自动加载同目录下的媒体文件，适合连续浏览同一目录的视频。
- 设置面板支持记住音量和静音状态，重新打开视频时直接沿用最近一次的听音习惯。
- 设置面板支持记住倍速，适合长期固定使用同一播放速度的场景。
- 设置面板支持记住播放进度，同一个文件再次打开时可直接续播。
- 设置面板支持单击画面暂停 / 恢复，以及窗口最小化时自动暂停。
- 设置面板支持播放控制条自动隐藏，并可配置无操作后延迟多少秒收起控制条。
- 设置面板支持切换时间显示方式，可在总时间和剩余时间之间切换。
- 设置面板支持调整左右方向键的快进 / 快退步长，以及长按右方向键时的临时倍速。
- 设置面板支持检测和选择 whisper.cpp 引擎路径，把真正的配置项放回设置中心。
- 设置面板支持选择默认字幕语言，生成字幕时可直接复用。
- 设置面板支持自动加载本地字幕缓存，重新打开同一个视频时可以直接回填字幕轨道。
- 设置面板支持选择 ASR 模型默认下载源，减少每次下载都要手动选源的重复操作。
- 设置面板补齐了截图和录屏的配置项，包括保存文件夹、复制到剪贴板、图片格式、文件命名、GIF 帧率和 GIF 分辨率。
- 设置面板的截图 / 录屏目录选择改成通用文件夹选择器，后续其他目录型设置也能复用。
- 设置面板支持一键恢复默认设置，方便试配置后快速回滚。
- 设置面板的数字输入框统一采用右对齐数值和等宽数字，compact 变体只调整宽度，避免同类控件再次出现靠右 / 居中混用。
- 设置页现在有 `smoke:settings-dialog` 视觉回归脚本，会直接打开 settings dialog、切到 interface section 并检查 `.settings-number` 的 computed style，避免以后只靠源码测试而漏掉真实渲染偏差。
- `smoke:settings-dialog` 还会按当前 locale 选择“打开设置”按钮文案，避免 smoke 在中文 / 英文 / 日文 / 韩文配置下因为入口文案不同而失效。
- `smoke:settings-dialog:en` 可以强制把设置页切到英文 locale，再验一次设置对齐和入口文案，方便抓住多语言下的布局偏差。
- `smoke:settings-dialog:ja` 和 `smoke:settings-dialog:ko` 可以直接覆盖日文 / 韩文的设置页回归，和默认中文、英文一起把 4 个支持 locale 都串起来。
- `smoke:settings-dialog:all` 可以一键顺序跑完中文 / 英文 / 日文 / 韩文，方便做完整的设置页多语言回归。
- settings-dialog smoke 每次都会用独立的临时 HOME，避免不同 locale 运行之间互相污染。
- settings-dialog smoke 的截图也会写到各自的临时 HOME 里，默认 / 英文跑出来的证据不会互相覆盖。
- `smoke:clip-export-dialog` 现在覆盖时间线导出确认面板、文件名编辑、指定输出路径的 1 秒真实 FFmpeg 视频导出，以及外置字幕视频与同名 SRT 落盘；`smoke:media-details-dialog` 覆盖媒体详情弹窗，`smoke:dialogs:all` 可以一条命令把它们顺序跑完。
- `smoke:all` 会把 settings、clip export、media details 和 open-video 四类回归一口气串起来，做完整手工验收时只需要记这一条。
- 设置面板把输入框、选择框、路径展示和次级按钮的高度、横向间距、圆角收口到设置域内的共享控件变量，后续新增表单项可以直接沿用同一底座。
- 设置面板的字段结构统一通过 `SettingsField` 渲染，说明区始终在控件前面，避免新增设置项时漏掉说明容器或写乱 DOM 顺序。
- 设置面板的下拉框和数字输入统一通过 `SettingsSelect` / `SettingsNumberInput` 渲染，避免新增表单控件时漏掉 class、数字解析或 compact 变体规则。
- 设置面板的卡片式开关项统一通过 `SettingsToggle` 渲染，避免标题、说明、checkbox 顺序和开关写回逻辑在新增设置时再次分叉。
- 设置面板的目录选择行统一通过 `SettingsFolderPicker` 渲染，路径展示、文件夹按钮、空值占位和可选清空按钮保持一致。
- 设置面板的紧凑开关 + 数值行统一通过 `SettingsToggleValueRow` 渲染，避免类似“自动隐藏控制条延迟”这类组合控件再次手写布局。
- 设置弹窗的两栏卡片按各自内容高度对齐，右侧字幕翻译配置变长时不会再把左侧视频 / 播放设置卡片拉伸出大块空白。
- 设置弹窗桌面端采用稳定的视口自适应高度，切换不同分组时标题栏和底部操作区不再跟着内容伸缩；当前分组内容在中间区域独立滚动，字幕等长分组不会把整个窗口撑高。
- 设置面板的分组写回统一通过 `createAppSettingsSectionPatcher` 绑定，实际合并由 shared 的 `updateAppSettingsSection` 承担，ui / media / playback / asr / capture 共用同一种 partial / updater 模板，避免以后再长出多套同构 helper。
- 设置面板本身直接接收 `AppSettingsSectionPatcher`，不再额外暴露整份 settings updater，确保 dialog 只保留自己真正需要的 section 级写回能力。
- 设置面板的分组 patch 类型统一通过 `AppSettingsSectionUpdate` 这一种共享别名描述，避免 App 和 settings dialog 各自重复写一遍 partial / updater 联合类型。
- 设置面板的 props 边界直接暴露 `patchSettingsSection`，避免 dialog 再绕一层 `onChange` 才拿到 section patcher，调用链更短也更不容易回退到旧写法。
- 设置面板的 section patcher 统一通过 `AppSettingsSectionPatcher` 命名，便于调用方明确自己拿到的是共享的 section 更新工具而不是临时闭包。
- 播放器对音量、静音和倍速的记忆统一通过 `syncPlaybackMemory` 收口，避免三个入口分别写一套 `lastVolume / lastMuted / lastPlaybackRate`。
- 截图 / 录屏导出的偏好统一通过 `syncClipExportPreferences` 收口，避免导出对话框直接散写 `clipExportLengthSeconds / clipExportMode`。
- 主进程的 app-settings 清洗逻辑按 ui / media / capture / playback / asr 拆成 section 级 sanitizer，和前端的 section patch 结构保持对称，后面补字段更不容易漏校验。
- 主进程会把旧版 `startup / playback / asr` section id 显式归一到 `general / interface / subtitles`，避免读取老配置时把设置页落到错误分组。
- 设置会自动保存到本地配置文件，下次启动继续生效。
- 设置弹窗改成左侧分组导航、右侧只展示当前分组；桌面端分组内容使用两列网格，移动端自动回落为单列，避免多个长卡片同时展开造成滚动和层级混乱。

## 多语言
- 播放器主界面、设置页、下载弹窗和运行提示已接入统一的多语言词条。
- 支持根据设置切换简体中文、英文、日语、韩语，避免不同模块出现混合语言。
- 媒体详情弹窗中的 ffprobe 字段名已支持多语言翻译。

## 媒体信息
- 信息 tab 现在会主动探测当前视频的技术元数据，摘要区展示文件大小、时长、封装格式、总体码率、分辨率、帧率、视频编码、音频编码、声道和采样率。
- 媒体信息摘要卡片里的被截断字段支持鼠标悬停查看完整内容，封装格式、编码、播放状态和字幕缓存路径都不会因为侧栏窄而丢信息。
- 主界面只保留摘要，点击「查看完整详情」会打开二级弹窗，展示 ffprobe 提取到的完整 format / stream 字段，方便排查编码、标签和流属性。
- 媒体元数据优先通过内置 ffprobe 探测，必要时回退到 ffmpeg 摘要解析，不依赖用户单独安装 mediainfo。

## 本地 ASR 字幕
- 新增 AI 准备向导：用户首次生成字幕、翻译或执行快速字幕流程时，应用会按运行时、推荐模型、翻译服务三个依赖逐项检查，直接展示缺失项和下一步动作。
- AI 准备向导会复用现有模型下载源选择、翻译服务自检和系统安全存储；模型下载或 API 配置完成后，可以继续用户刚才发起的操作，不需要重新寻找按钮。
- ASR 单独生成入口在缺少 whisper.cpp、ffmpeg 或推荐模型时也会进入准备向导；已有原文字幕时直接翻译不会被无关的 ASR 模型安装阻塞。
- 新增 Cloudflare Pages 快速开始区，解释本地字幕引擎、ASR 模型和在线翻译服务的区别，并说明下载源、API 费用与字幕数据发送边界。
- ASR 字幕卡片会直接显示翻译服务的“未配置 / 已配置 / 可用 / 不可用”状态；未配置时可直接进入设置，未检测或失败时可在翻译入口旁测试 / 重试。
- 基于 whisper.cpp 的离线字幕生成流程。
- ASR 生成改为单次 whisper.cpp 进程的流式字幕挂载：识别输出会实时写入隔离的 partial VTT / SRT，并按版本通知播放器增量重读，用户无需等待整段视频识别完成；任务结束后再切换到正式缓存文件，取消或失败时清理 partial 文件。
- AI 完整处理流程在 ASR 进行期间会按新增的完整字幕批次增量翻译：流式阶段每 6 条 cue 顺序翻译一次，只把未翻译 cue 发给翻译服务；识别完成后补翻最后不足一批并提升到正式翻译缓存，避免重复翻译已完成内容。最终离线翻译仍按每批最多 30 条执行。
- 当用户从视频中段开始生成字幕时，ASR 会先识别当前播放位置附近约 60 秒的优先窗口（Whisper offset + duration），并在全片识别前完成该窗口的原文与译文展示；优先窗口文件只作为临时结果，不进入全片正式缓存。
- ASR 在取消或异常中断且已有超过 30 秒识别结果时，会把 partial 字幕和最后时间点保存为 checkpoint；下次同一视频和模型生成时从最后 cue 前 3 秒续跑并合并，成功后删除 checkpoint，不影响正式缓存命名。
- ASR 面板会自动检测当前视频的可续跑 checkpoint，并显示带 3 秒重叠的续跑起点；开始任务后按钮和进度会明确显示续跑起点，避免用户误以为正在从头识别。
- 支持模型下载和字幕生成，ASR 面板保留运行时状态和工作流操作。
- 播放器底部控制栏提供“快速目标语言字幕”入口，不需要先打开 ASR 面板；一次点击会按当前目标语言自动完成原文字幕生成、翻译和译文挂载，不遮挡视频画面。
- 已经存在原文字幕时，快速入口会直接跳过 ASR 生成，只执行翻译；如果原文已经是当前目标语言，或目标译文已经命中缓存，则跳过重复计算并直接切换显示。
- 快速目标语言字幕入口在 ASR 引擎未就绪时会把用户带到 ASR 工作区完成配置，避免点击后没有反馈。
- 支持 `⌘/Ctrl + Shift + C` 快捷键，在有媒体文件时直接执行快速目标语言字幕流程；中文、英文、日文、韩文均复用当前目标语言设置。
- whisper.cpp 引擎的自动检测和手动选择已经搬到设置里，避免和运行面板重复。
- 支持字幕缓存，重复生成同一视频时优先命中本地结果。
- 生成字幕时同时输出 VTT 和 SRT 两份文件，VTT 直接挂载到播放器，SRT 便于导出和外部使用。
- 重新打开同一个视频时会自动探测字幕缓存，命中后直接回填字幕轨道和状态，不用重新生成。
- 生成字幕后可一键打开字幕缓存文件夹，或直接定位 SRT 文件，方便检查 VTT / SRT 文件和调试挂载结果。
- 生成字幕后可以直接用系统默认应用打开 SRT 文件，不再把“打开文件”误做成“在文件夹里定位”。
- ASR 字幕生成和云端翻译分别统计耗时；完成后展示耗时、字幕条数 / 翻译批次数和本地缓存命中状态，快速流程还会展示端到端总耗时。
- 字幕卡片会显示简短状态标签，直接告诉用户当前是“等待生成 / 缓存已就绪 / 已挂载”，减少猜测成本。
- 如果手头只有 VTT，也可以从当前字幕一键重新导出一份 SRT，继续给外部工具使用。
- 字幕操作菜单还可以直接复制 VTT / SRT 路径，方便丢给终端脚本、批处理或外部工具。
- 字幕操作菜单支持点击外部或按 `Esc` 自动关闭，避免菜单停留在半开状态。
- 信息 tab 会直接展示当前媒体文件、播放状态和字幕缓存状态，不再只显示一个空壳提示。
- 信息 tab 里播放状态和字幕缓存用 compact 双列卡片展示，把短信息收紧，减少空白和错位感。
- 导出和转换字幕时会保留 cue 内的换行结构，避免多行字幕被压成单行。
- 导出 SRT 时会自动清理 VTT 的 `voice / i / b / u / c` 等常见内联标签，减少外部工具兼容问题。
- 导出 SRT 时会去掉 `ruby / rt / rp` 的注音标记，保留正文文字。
- 导出 SRT 时会解码常见 HTML 实体，例如 `&amp; / &lt; / &gt; / &nbsp;`。
- 导出 SRT 时会解码更多常见排版实体，例如省略号、长破折号和引号。
- 导出 SRT 时会容忍非法数值实体，避免脏字幕文件直接导致转换失败。
- 生成 VTT 时会自动转义普通文本里的 `& / < / >`，确保输出始终是合法 VTT。
- 字幕栏支持便捷调整字号，播放时可以直接放大或缩小当前字幕文本。
- 字幕栏的 quick controls 里提供字号预设按钮，常用字号可以一键切换，不用反复点加减号。
- 字幕栏的 quick controls 里也把行高和显示模式做成了按钮组，原文 / 译文 / 双语切换和紧凑 / 标准 / 宽松切换都可以直接点，不用再钻下拉框。
- 字幕显示偏好会保存到本地设置，重新打开应用后继续沿用最近一次的字幕字号和行高。
- 设置面板的字幕分组新增显示设置，为后续原文、译文、双语字幕切换预留入口。
- 支持把已生成的 VTT 字幕翻译为目标语言字幕，译文会写入独立的 VTT / SRT 缓存文件。
- 翻译字幕使用 OpenAI-compatible Chat Completions 接口，通过 `AIVPLAYER_TRANSLATION_BASE_URL`、`AIVPLAYER_TRANSLATION_API_KEY` 和 `AIVPLAYER_TRANSLATION_MODEL` 环境变量启用，术语表也可通过 `AIVPLAYER_TRANSLATION_GLOSSARY` 提供，避免把密钥写进普通设置文件。
- 翻译服务也可以直接在字幕设置里配置，接口地址、模型和 API key 统一从字幕分组读写，API key 会通过系统安全存储加密落盘。
- 翻译服务设置里新增了自检按钮，可以用当前接口、模型和 API key 直接发送样例字幕，快速确认返回结果是否可解析，并在成功后展示一条样例译文预览。
- 翻译服务自检结果会额外展示源语言、目标语言、模型和接口摘要，方便一眼看出这次请求到底是拿什么配置去测的。
- ASR 面板会同步展示翻译服务的“未检测 / 可用 / 不可用”状态；当接口、模型或语言上下文变化时会清理旧状态，避免把过期的自检结果误当成当前配置。
- 翻译服务和正式翻译流程会把失败原因细分成网络错误、HTTP 状态错误、JSON 解析错误、响应结构错误和空结果，便于定位到底是接口不可达、鉴权异常还是模型输出不符合约定。
- 字幕设置支持可编辑术语表，每行使用 `原词=固定译词`；术语会同时用于服务自检、正式翻译 prompt 和缓存上下文，修改术语后会自动避开旧译文缓存。
- 字幕翻译会按最多 30 条 cue 分批请求，网络错误、HTTP 429 和 5xx 会自动重试，并通过 ASR job 进度通道展示当前批次。
- ASR 面板会在翻译进行中显示已耗时，完成后展示总耗时、字幕条数、翻译批次数，并明确标记是否命中本地缓存。
- 快速生成中文字幕会额外记录从开始生成原文字幕到译文落盘的一键流程总耗时，和单独的翻译耗时分开显示。
- 跨批次翻译会自动携带前后各 2 条邻近字幕；后续批次还会带上上一批已翻译文本，帮助模型保持术语、指代和语气一致，同时要求接口只返回当前批次 cue。
- 翻译进行中可以直接取消，取消信号会传到请求层；未完成的翻译不会写入新的 VTT / SRT 缓存文件。
- 新增 `smoke:translation`，通过隔离的本地 mock OpenAI-compatible 服务验证翻译 IPC、批次进度、译文落盘和取消链路，`smoke:all` 会一并执行。
- 新增 `smoke:translation:player`，用真实 Electron 播放窗口验证字幕缓存回填、点击翻译、译文 overlay 挂载、自动切到译文模式，以及重启后的译文缓存恢复。
- `smoke:translation:player` 默认使用本地 Mock；临时提供 `AIVPLAYER_SMOKE_TRANSLATION_BASE_URL`、`AIVPLAYER_SMOKE_TRANSLATION_MODEL` 和 `AIVPLAYER_SMOKE_TRANSLATION_API_KEY` 时，会在隔离用户目录中验证真实 OpenAI-compatible 接口，进程结束后连同设置文件一起清理，不把 Key 写入产品代码；额外设置 `AIVPLAYER_SMOKE_TRANSLATION_GLOSSARY` 可验证固定译法。
- 播放器字幕栏支持在原文、译文、双语之间切换；翻译完成后如果仍处于原文模式，会自动切到译文模式便于立即验收效果。
- ASR 结果会记录 whisper.cpp 实际识别到的字幕语言，翻译时会优先沿用这份真实语言信息，重开同一个视频后也能继续用同一来源语言翻译。
- 翻译后的字幕缓存会在重新打开同一个视频时自动回填，译文不需要每次都重新点翻译按钮。
- ASR 面板会显示当前字幕的语言对，并在译文生成后提示“译文已就绪”，方便确认这次翻译到底是从哪种源语言翻到目标语言，以及结果是否已经产出。
- ASR 面板还会显示当前翻译模型，便于确认译文缓存、设置项和实际生成结果是否来自同一条模型配置链路。
- 原文字幕和译文字幕统一写入 `asr-cache/subtitles`，文件名分别带 `-raw` 和 `-translated-目标语言-服务商-模型-缓存标识`，打开字幕文件夹即可看到同一视频的全部字幕结果。
- 旧版无 `-raw` 原文缓存和 `translated-subtitles` 译文缓存会在命中时安全复制到新目录，旧文件保留作回退，避免升级后丢失已有字幕。
- 字幕工具菜单会明确区分原文 / 译文 SRT 和 VTT 路径，避免用户打开原文文件却误以为翻译没有生成。
- ASR 面板新增中文、English、日本語、한국어目标语言快捷切换；切换时会复用对应缓存，没有缓存才重新请求翻译，翻译进行中会暂时锁定语言按钮。
- 点击 ASR 面板中的目标语言（包括当前已选语言）会立即复用或生成对应译文，并在成功后自动切换到译文显示；不再只保存目标语言设置却继续显示原文。
- ASR 运行状态的版本信息与界面语言解耦，切换语言后不会继续残留上一种语言的状态文案。
- 目标语言卡片在窄侧栏中保持标签横向显示，译文模式未生成前会明确提示需要先完成翻译。
- whisper.cpp 在 macOS Metal buffer 分配失败时会自动改用 CPU 重试，正常机器仍优先使用 GPU；回退过程会通过 ASR 进度状态展示。
- 开发态 Electron 会从项目 `resources/` 解析 whisper.cpp 和 ffmpeg，打包态则使用 `process.resourcesPath`，避免开发环境误报运行时缺失。
- 新增 `smoke:asr:player`，使用真实 whisper.cpp、真实模型和真实视频验证音频抽取、ASR 生成、VTT / SRT / JSON 落盘、语言探测和播放器字幕挂载；它不纳入默认 `smoke:all`，避免完整回归被 547MB 模型运行时间拖慢。
- 新增批量字幕任务中心：从 ASR 工作区选择视频文件夹，支持扫描子文件夹、预览并勾选要处理的视频，一键按当前目标语言生成原文字幕并翻译。
- 批量字幕任务使用主进程受控队列，ASR 始终限制为单路运行，翻译可配置 1–3 路并发；逐文件展示排队、ASR、翻译、完成、失败和取消状态，单个文件失败会继续处理后续文件。
- 批量任务支持暂停、继续、停止和失败项重试；暂停在当前视频完成后生效，避免强行终止 whisper.cpp 造成半截缓存。
- 批量任务状态会写入用户目录的 `batch-subtitle-job.json`，只保存本地视频路径和任务状态，不保存翻译 API Key；应用重启后正在运行的任务会恢复为“已暂停”，等待用户继续。
- 批量任务默认提供“跳过已有字幕缓存”，已存在原文和目标语言译文时直接标记完成，不重复占用 ASR 或翻译服务。
- 批量任务会在用户目录的 `logs/batch-subtitles` 下写入 JSONL 诊断日志，记录任务、文件、阶段、耗时、失败状态和脱敏后的翻译 response body；日志写入失败不会阻塞字幕任务。
- 批量任务中心的失败项支持展开查看 HTTP 状态、错误码和 response body，并提供“打开日志文件夹”入口，方便把完整证据交给开发者排查。
- 批量任务中心提供并行度选择，默认 1 路以控制内存占用；提高到 2–3 路时只并行翻译网络请求，不会同时启动多个 whisper.cpp 进程。
- 批量任务卡片会实时显示已耗时、平均单文件耗时、预计剩余时间、当前运行中的文件数和总完成数；预计值会基于已完成文件动态更新，尚未完成首个文件时显示“计算中”。
- 批量字幕支持按文件自动重试 0–3 次，默认 2 次；仅网络错误、HTTP 429 和 5xx 会进入 1 秒 / 3 秒退避，鉴权错误、配置错误和无效响应不会盲目重试。
- 自动重试会在任务日志中记录重试序号、退避时间、错误类型和脱敏响应摘要；暂停会保留当前文件，取消会立即终止等待中的重试。
- 批量失败项会按“可恢复”和“需检查配置”分组统计；当前任务与历史任务都支持只重试网络、429、5xx 等可恢复失败，避免重复触发鉴权或响应格式错误。
- 单视频 ASR 生成、字幕翻译和翻译服务连通性测试会写入用户目录的 `logs/asr/asr-YYYY-MM-DD.jsonl`，记录阶段、耗时、结果和脱敏后的错误详情。
- 单视频翻译失败时，ASR 面板会直接展示错误码、HTTP 状态和 response body，并提供“打开日志文件夹”入口，单视频与批量任务使用同一套排错证据链。
- ASR 面板和批量字幕面板都支持在应用内展开查看最近 80 条诊断日志，按时间倒序展示并限制滚动区域高度；日志读取失败、空日志和刷新状态都有明确提示。
- 诊断日志支持按全部、失败、ASR、批量筛选，并可复制当前筛选结果或单条 JSON，方便直接发送给开发者分析。
- 批量字幕任务会在用户目录独立保存最近 10 次终态任务历史，不和当前恢复状态混用；历史记录包含目标语言、文件夹、总耗时、完成数和失败项。
- 批量字幕面板提供紧凑的历史任务列表，可展开查看失败文件与错误摘要，并从历史记录直接重试失败项；重试仍沿用当前 ASR 单路、翻译 1–3 路的受控并发策略。

## AI 内容总结
- 在 ASR 面板、顶部工具栏和右侧面板导航中提供独立的「内容总结」入口；总结展示在可滚动的专用面板中，不遮挡视频播放。
- 复用「字幕」设置里的目标语言作为用户的默认内容语言，并在设置中明确说明：字幕翻译和 AI 内容总结统一使用这个语言；界面语言与内容语言可以分别设置。
- 总结优先使用当前目标语言译文字幕；没有译文时直接使用原始 ASR 字幕，并要求模型以默认内容语言输出，支持日文、韩文、中文、英文字幕混合输入。
- 针对长电影采用“分段剧情笔记 → 全片结构化总结”的两阶段流程，避免把两小时字幕一次性塞进模型上下文；进度会显示当前整理段落，并支持取消。
- 总结结果包含标题、一句话概览、剧情梗概、关键事件与看点、重要人物、主题和结局提示，并明确标记“含剧透”。
- 总结结果支持输出剧情章节时间点；章节列表显示时间、标题和简述，点击章节会直接跳转播放器到对应位置，并沿用播放进度记忆。
- 快速了解模式的章节同样遵守无剧透规则，只保留设定和前段看点；详细解说模式可展示完整剧情章节。
- 总结结果支持一键复制纯文本，并可通过系统保存对话框导出 Markdown、TXT 和结构化 JSON；导出内容沿用当前总结模式和目标语言。
- 总结支持“快速了解”和“详细解说”两种模式，默认使用无剧透的快速了解；快速模式只输出设定、人物初始关系和看点，不展示结局，详细模式才展示完整剧情和结局。
- 总结模式会同时进入提示词、结果匹配和缓存键；同一部视频切换模式时会复用对应缓存，不会把含剧透结果误当成无剧透导读。
- 总结缓存按字幕内容、原始语言、默认内容语言、总结模式和 AI 模型生成唯一标识，写入用户目录的 `asr-cache/summaries`；重新打开同一视频时会自动恢复已有缓存，不重复消耗接口额度。
- 总结结果会记录当前基于原始字幕还是目标语言译文；译文缓存异步恢复时不会自动清空已经展示的原文总结，只有用户主动点击“基于译文重新生成”才会切换总结来源，避免同一内容被隐式重复请求。
- 用户目录的 `asr-cache/index` 会为当前视频登记 ASR、翻译和总结缓存之间的文件关系、模型、语言、模式和媒体文件指纹；索引写入失败不会阻塞字幕或总结，旧缓存会在再次命中时按需补登记，术语表只保存哈希，不保存原文。
- 主进程会对相同视频、字幕来源、目标语言、总结模式和强制生成标识的并发总结请求做 single-flight 合并；自动工作流和手动操作同时触发时共享同一次请求与取消控制器，避免重复消耗 AI 接口额度。
- AI 内容总结与字幕翻译共用 OpenAI-compatible Chat Completions 服务配置，设置页会明确标注该服务同时用于翻译和总结；API Key 仍由系统安全存储加密保存。
- 总结 IPC、取消链路和失败响应会写入脱敏 ASR 诊断日志；网络、HTTP、JSON 和响应结构错误会在总结面板中给出可读提示。
- 新增 `tests/unit/subtitle-summary.test.ts`，覆盖目标语言提示、长字幕总结结果和本地缓存复用。
- 新增字幕缓存索引和 single-flight 单元测试，覆盖三类缓存登记、术语表脱敏、并发请求复用和完成后清理。
- 新增 AI 工作流入口：可一键执行「AI 导读」（ASR + 总结）或「完整处理」（ASR + 字幕翻译 + 总结）；只生成总结时会尽量跳过不必要的字幕翻译请求。
- 播放器底部保留一个快捷处理按钮；一次点击复用「完整处理」工作流，依次完成 ASR、目标语言字幕翻译和 AI 内容总结，并复用已有缓存、进度、取消与失败重试。
- AI 工作流使用统一的非阻塞任务条展示阶段、进度、取消、失败重试和完成通知；视频播放不会因为后台处理被切换或遮挡。
- AI 工作流取消会把信号传到本地 whisper.cpp、云端翻译和总结请求，避免界面显示已取消但后台进程仍持续运行。
- 设置中的「打开视频时的 AI 处理」支持仅恢复缓存、询问后生成、自动生成 AI 导读和自动完整处理四种策略，默认只恢复已有缓存，避免未经确认调用云端 AI。
- 自动工作流会复用 ASR、翻译和总结的已有缓存，并沿用默认内容语言；自动模式首次运行通过非模态确认条明确输出语言和后台处理行为。
- 设置的「字幕」分组新增本地 AI 缓存管理：显示字幕、总结、索引的占用空间和文件数量，并可安全清理媒体已失效的缓存索引及超过 1 小时的临时文件；实际字幕、翻译和总结内容不会被该操作删除。
- 缓存管理 IPC 和扫描逻辑会递归统计 `asr-cache`，忽略符号链接，遇到损坏索引或媒体文件已删除/变更时将其标记为失效；新增单元测试验证统计、清理边界和可用缓存保留。

## 片段导出
- 窗口顶部全局工具栏提供独立的「剪辑」入口，不放在播放控制栏，也不依赖 ASR 面板或「字幕工具」菜单；点击后可基于当前播放位置快速导出短片段。
- 导出时可在 15 秒、30 秒、60 秒三档长度里选择，适合不同截取场景。
- 片段导出弹窗升级为基础剪辑工作区：支持拖动时间轴入点 / 出点、直接输入秒数、按当前位置设置边界，以及恢复整段视频。
- 基础剪辑工作区内置选区预览，预览播放会在出点自动暂停，不会修改原视频文件。
- 选区预览会保留源视频的原始纵横比；竖屏视频在预览画布中居中显示，不再被横向拉伸或裁切。
- 自定义选区导出仍复用现有 FFmpeg 管线，最短支持 0.1 秒，并继续沿用原视频、外挂字幕和字幕烧录三种导出方式。
- 选区内字幕会按剪辑边界裁剪并重新从 00:00 开始，外挂 SRT 和烧录字幕的时间轴保持一致。
- 导出模式支持纯视频、外挂字幕和字幕烧录三种组合，其中字幕相关模式会在无字幕时自动禁用。
- 导出弹窗会记住上次选择的长度和模式，下次打开时直接预选，减少重复操作。
- 每次导出都会先弹出保存位置选择器，再按当前选项执行输出。

## Live Photo / 动态照片工作区
- 图片工作区支持识别并查看小米 Live Photo、Google Motion Photo，以及同名图片 + MOV 的 Apple Live Photo 组合；动态照片会在缩略图和预览区显示视频标识并可直接播放。
- 小米 Live Photo 优先适配 JPEG + MP4 追加容器和当前样本的 `version=3` 自定义时间线，解析时保留封面 JPEG 字节边界和视频旋转信息，不依赖小米 SDK。
- 动态照片视频支持设置起始时间、时长、中心裁剪、静音和中心马赛克打码；导出由主进程复用项目内 FFmpeg，避免把视频二进制处理放在 Renderer。
- 小米封面时间线在视频截取后会按新的起始时间和时长等宽改写，避免破坏 JPEG APP 段；Google Motion Photo 会同步更新 `Container:Item Length`，Apple Live Photo 会复制图片并生成对应的 `.mov` sidecar。
- 动态照片仍可单独导出当前封面；覆盖原图入口对 Live Photo 自动禁用，防止误把动态照片覆盖成静态图片。
- 当图片工作区封面发生尺寸、旋转、翻转或格式变换时，导出会同步重编码封面，并保留原 JPEG 的 APP 元数据；小米 JPEG 的厂商信息不会因为封面编辑丢失。
- 可选取剪辑后视频中的指定时间帧作为新的 Live Photo 封面；小米导出会同步更新 `videoPresentationTimestampUs` 对应的时间线，Google Motion Photo 会同步更新 `MotionPhotoPresentationTimestampUs`，确保封面时间与新视频一致。
- Apple sidecar Live Photo 的 HEIC 封面支持同步重编码：先从原 HEIC 提取 JPEG metadata donor，合并新封面，再通过 macOS `sips` 或 libheif `heif-enc` 编回 HEIC，保留 Apple `ContentIdentifier`、MakerNote、ICC 等配对元数据；MOV 导出会显式回写同一 `com.apple.quicktime.content.identifier`。
- HEIC 编码器通过运行时探测获得，不依赖开发机 Homebrew 动态库；macOS 优先使用系统 `sips`，其他环境可通过 `AIVPLAYER_HEIF_ENC` / `AIVPLAYER_HEIF_CONVERT` 指定 libheif 工具，缺少工具时明确返回错误而不生成不完整 Live Photo。
- HEIC 运行时支持随 electron-builder 进入 `resources/heif`；新增 `release:prepare-heif-runtime` / `release:check-heif-runtime`，为 Windows/Linux 发布准备并校验自包含的 `heif-enc`、`heif-convert`，macOS 继续优先使用系统 `sips`。
- 发布工作流固定使用 libheif `1.23.1` 源码构建无插件 HEIC 运行时：macOS 使用 Kvazaar，Windows/Linux 使用对应 Runner 可用的 x265 依赖，并在 electron-builder 前校验 `heif-enc` / `heif-convert`，不把开发机二进制提交进仓库。
- Apple HEIC 仅有 `ContentIdentifier`、主图和 HDR 辅助图时仍按静态 HEIC 处理；必须存在同名 MOV/MP4 sidecar 或可解析的动态视频轨，不能仅凭元数据误判为 Live Photo。
- 新增 `src/core/live-photo/live-photo-parser.ts`、`live-photo-service.ts`、`heic-cover.ts` 和 JPEG 封面元数据单元测试，覆盖小米边界识别、Google 长度/时间戳更新、小米时间线更新和封面 APP 元数据保留。

## 模型下载
- 推荐模型支持国内 ModelScope 和国际 Hugging Face 两个来源。
- 下载进度在侧边栏展示，方便定位卡顿或失败。
- 下载弹窗会默认优先显示设置里选中的下载源，并标记为默认选项。

## 应用自动更新
- Windows/Linux/macOS 正式安装包通过 `electron-updater` 在主进程后台检查 GitHub Releases，并按当前平台获取新版本；开发模式、Windows Store 和 `aivcli` 不触发更新检查。
- 发现新版本后显示 ChatGPT 风格的更新弹窗，展示当前版本和新版本，提供“跳过这个版本”“稍后提醒我”“安装更新”以及自动更新开关；下载前不会打断当前播放或编辑。
- macOS 原生应用菜单新增“检查更新…”入口，Windows/Linux 继续支持设置页和应用内更新入口。
- “跳过这个版本”会写入用户数据目录的更新偏好文件，后续自动检查不会重复提示同一版本；“稍后提醒我”会记录版本和时间，24 小时内不会重复提醒，手动检查仍可立即查看。
- 更新过程通过 IPC 同步 `checking`、`available`、`downloading`、`downloaded`、`installing` 和错误状态，顶栏展示下载进度或重启更新按钮。
- 更新下载完成后只提供“重启并更新”操作，调用安装器完成替换，不在播放或编辑过程中强制退出应用。
- 通用设置新增“自动更新”开关，默认保持开启；关闭后停止启动检查和定时检查，但仍可从菜单、设置页或关于入口手动检查并下载更新。
- 设置页新增“关于”分组，集中显示版本、许可证和项目地址；用户可以手动检查更新，查看最新状态或下载进度，并在下载完成后从设置页重启更新。
- 关于设置页和关于弹窗新增官方网站入口，保留 GitHub 项目地址的同时，用户可以通过带外链图标的按钮打开官网；按钮支持四种界面语言和窄窗口布局。
- electron-builder 配置 GitHub 发布源，Release 工作流会上传 `latest*.yml` 和 `.blockmap` 元数据；GitHub Release 成功后，Windows/Linux/macOS 安装包可按当前平台自动更新。

## 应用图标
- 已配置 AIVPlayer 自定义应用图标，macOS 开发态 Dock 和正式安装包都不再使用 Electron 默认图标。
- 图标采用深色圆徽 + 金色波形的极简风格，和当前播放器主题保持一致。
- macOS 菜单栏应用名称会显示为 AIVPlayer，开发态也不会再保留 Electron 默认名称。
- macOS 原生应用菜单已接入简体中文、English、日本語、한국어四套文案，应用菜单、文件、编辑、显示和窗口菜单会跟随设置中的界面语言即时刷新。
- macOS 菜单补齐了“打开媒体文件”和“设置…”入口，并沿用播放器现有的文件选择与设置弹窗，不需要用户先回到窗口内寻找按钮。
- Linux / Windows 顶部工具栏新增“关于 AIVPlayer”入口，使用应用内无边框弹窗显示版本、许可证和项目地址；macOS 继续保留系统原生“关于”菜单，四种界面语言同步覆盖。
- 安装包声明了已支持的视频容器、专业格式和原始视频流关联，macOS、Windows、Linux 的“打开方式”可以识别 AIVPlayer；新增扩展名会同步维护运行时发现、MIME 和安装包关联。
- 从 Finder、Windows 资源管理器、Linux 文件管理器或命令行打开视频时，AIVPlayer 会把文件传给已有窗口；不会因为系统再次启动一个实例而丢失视频。
- Linux 同时提供 AppImage、deb 和 snap 安装目标；deb 安装会把 desktop MIME 关联正式写入系统，AppImage 保留便携分发能力，snap 支持发布到 Ubuntu Snap Store 实现自动更新分发。
- 0.3.0 发布工作流会在 macOS、Windows、Linux Runner 上分别构建并暂存对应平台的 whisper.cpp 与 FFmpeg 运行时；Linux `.deb` 声明 FFmpeg 系统依赖并随 GitHub Release 上传，避免新 Runner 因仓库忽略本机二进制而生成不可用安装包。

## 默认界面缩放
- 应用窗口默认使用略微收紧的渲染缩放基线，让字体、图标、按钮和卡片整体比系统默认更小一档。
- 这层缩放是窗口级默认值，不需要用户额外手动调整，也不会依赖浏览器或系统的页面缩放状态。

## ASR 二进制兼容
- 自动识别 `whisper-whisper-cli`、`whisper-cli`、`whisper-cpp`、`main` 这几类 whisper.cpp 可执行文件。
- 如果用户选到的是 deprecated wrapper，运行时会优先切换到同目录下真正可用的二进制。
- 资源打包时尽量保留识别到的原始二进制文件名，避免把新版本 whisper.cpp 再改回旧名字。

## 媒体详情 i18n
- 完整媒体详情弹窗支持多语言翻译，使用 `probeFieldLabels` 映射 ffprobe 字段名。
- `humanizeKey` 函数支持精确匹配、小写匹配、带点号匹配三种查找策略，找不到时回退到原有的格式化逻辑。
- 四种语言（zh-CN、en-US、ja-JP、ko-KR）均已添加 `probeFieldLabels` 翻译。

## 工程结构治理
- 源码按执行边界拆分：Electron 主进程和桌面适配代码放在 `src/desktop`，桌面端与 `aivcli` 共用的无 Electron 业务能力放在 `src/core`；electron-vite 的构建产物继续输出到 `out/main`，兼容现有启动器和 smoke 脚本。
- 播放器主界面拆为状态模型、派生数据、播放/字幕动作、副作用和页面区块，`App.tsx` 只负责 Provider 与 Shell 组装。
- 设置页、媒体详情、批量字幕和 ASR 面板按职责拆成独立组件与 Hook，避免 API、业务状态和 UI 渲染继续混在同一个文件里。
- 主进程入口按窗口生命周期、媒体对话框、设置 IPC、ASR IPC、批量字幕 IPC 和剪辑导出 IPC 拆分，主入口只保留应用启动编排。
- `player.css` 按完整 CSS block 拆为多个局部文件；国际化、媒体类型也按 locale / 领域拆分，并新增 `npm run check:file-limits` 作为行数门禁。

## 侧栏面板导航
- 右侧面板导航采用两列两行布局，保证播放列表、ASR、批量字幕和媒体信息在 280px 侧栏中完整显示，不再因为四列压缩而出现省略号遮挡。
- 面板标签同时提供 `aria-label` 和原文 `title`，窄窗口与键盘/辅助技术场景仍能识别完整名称。
- 右侧面板支持从左侧边界拖拽调整宽度，宽度会限制在 240–480px 并持久化到本地设置；拖动时只更新布局，释放后才写盘，同时支持键盘方向键、Home/End 和 `role="separator"` 辅助操作。

## 字幕 QA
- 剪辑时间线提供字幕 QA 面板，自动检查空字幕、同轨时间重叠、时长过短 / 过长、阅读速度（CPS）、行宽、标点异常和疑似识别问题。
- QA 重叠检查按原文 / 译文轨道分别计算；双语字幕即使时间完全相同也不会被误报为跨轨重叠，同时仍会报告同一轨道内的重叠。
- QA 结果只读且不修改字幕或工程数据；每条问题都显示严重级别与时间点，点击后可直接把编辑播放头定位到问题位置。
- QA 面板对时间类问题提供显式“自动修复时间问题”动作：只处理同轨重叠、过短和过长 cue，无法在不碰撞下一条字幕时安全延长的 cue 会保留原状；修复进入工程撤销 / 重做和保存链路，不自动改写字幕文本。
- 真实 Electron Smoke 已验证 4 条字幕的批量时间修复、撤销 / 重做闭环和原文 SRT 导出：3 个可修复问题归零后可撤销恢复并重做，导出保留全部文本和修复后的时间范围，Renderer 无错误。

## 字幕波形时间轴
- 剪辑时间线会为有音频轨的素材生成可复用的音频波形，并按媒体文件指纹、波形尺寸写入用户目录缓存，不把派生 PNG 放进工程或仓库。
- 波形按成片片段窗口展示，保留剪辑后的源时间裁切；点击波形或使用左右方向键可定位编辑播放头，波形生成失败不会阻塞视频剪辑。

## 字幕视觉同步
- 选中字幕 cue 后，编辑器显示当前视频画面对应的字幕文本、起止时间和播放头位置；支持以 0.1 秒为步进前后微调。
- 支持把当前播放头设为字幕起点或终点，所有调整复用现有字幕操作、项目持久化和撤销 / 重做链路。
- 多选字幕后支持两点同步：以选区原始首尾时间为基准，用户记录新的目标起点 / 终点后按比例映射所有选中 cue；会同步缩放词级时间，不改文本或视频剪辑，并可整体撤销 / 重做。

## 跨设备字幕旁车选择
- 工程保存时会为用户选中的 source / translation 旁车生成相对 `.aivproj` 目录的路径提示，打开工程或另存为时可以随工程目录迁移。
- 旧机器上的 preferred path 失效时，字幕 loader 会保留候选证据并回退到自动候选，同时清理失效选择，避免重启后反复挂载旧绝对路径。
- 字幕冲突面板提供“恢复自动候选”动作；它会重新加载自动优先级候选、更新 revision 基线，并进入现有撤销 / 重做链路。
- 编辑模式会持续监听当前活动素材的字幕候选目录，并只对已知 source / translation 候选旁车变化触发快照重载；变化会复用现有 revision 冲突预览，不自动覆盖工程字幕，退出编辑或切换素材后会停止旧目录监听。

## OCR / TTS 任务证据合同
- 新增 OCR / TTS 派生任务的共享状态模型，记录媒体指纹、输入哈希、时间范围、进度、尝试次数、有限重试、取消和失败信息。
- OCR 结果只有在来源指纹和时间范围校验通过后，才可转换为视觉检索证据；TTS 作为独立派生音频产物保存，二者都不会直接改写正式字幕或原媒体。
- 新增可取消、可报告进度的证据任务执行器，按时间范围逐项调用 OCR / TTS 适配器，并统一处理重试、取消和完成状态。
- 新增显式注入路径的本地适配器：FFmpeg 抽帧 + Tesseract OCR，以及可替换命令的 TTS（macOS 可接 `say`）；缺少运行时能力时返回明确错误，不依赖开发机绝对路径。
- 主进程通过独立 IPC 提供 OCR / TTS 能力探测、任务启动、sender 级取消和进度事件；任务启动时重新计算媒体指纹，不信任渲染进程传入的旧指纹。
- OCR 完成后会再次检查媒体指纹，只把仍属于当前媒体的 OCR 结果单条 upsert 到 `video_evidence`，不会清空已有字幕 / 视觉证据；媒体在任务期间变化时会明确标记为 `skipped-stale`。
- 影视库搜索面板新增最小 OCR 任务入口：显示当前媒体和本地能力状态，可输入时间范围、使用播放头前后 5 秒、取消任务，并把进度、落库成功、stale 媒体和落库失败状态直接展示在卡片内。
- 视觉文字搜索会直接召回 `video_evidence` 中的 OCR 行；结果使用证据 ID 保持多段 OCR 不互相覆盖，显示 OCR 标签和精确时间范围，点击结果会把播放器定位到识别区间起点。
- 混合文字搜索在视觉文本模型暂不可用时会回退到本地证据 / 文件名词法检索，已落库的 OCR 文字仍可被定位，不阻断普通播放。
- 新增真实 Electron Smoke，覆盖 OCR IPC、稳定结果落库、重启后保留已有 visual 行，以及 stale 媒体结果不落库。
- TTS 任务卡支持检查本地 provider、填写文本和时间范围、使用播放头前后 5 秒、取消任务，并通过真实 `<audio>` 控件试听生成的 AIFF 音频；macOS `say` 使用 `say -v ?` 探测，避免把可用 TTS 误判为不可用。
- TTS 音频通过 `aiv-media://` 协议加载，AIFF MIME 映射由媒体协议统一提供；OCR 和 TTS 进度监听按任务 kind 隔离，TTS 不会污染 OCR 卡片状态。
- 试听完成后会展示可编辑的字幕草稿文本；只有用户明确点击确认保存，主进程才在用户目录 `evidence-drafts/*.vtt` 生成独立 VTT 草稿，不覆盖、不挂载正式字幕。
- 保存草稿前主进程重新计算媒体指纹，媒体在试听或编辑期间发生变化时拒绝保存；草稿 Smoke 验证“未确认不落盘、确认生成 VTT、正式字幕旁车文件不变”。
- 字幕设置新增 TTS provider 配置，可保存可执行文件路径和 voice；环境变量优先级、macOS `say` 默认值和能力诊断结果在同一条链路中展示，其他平台不会假设系统自带 TTS。
- 字幕草稿以 manifest + VTT 成对原子保存，支持按当前媒体查看、删除；草稿 ID 严格校验，列表和删除不会接受任意文件路径。
- “导入正式字幕”是独立的人工确认动作：主进程会再次校验媒体路径与指纹，生成同目录的 VTT / SRT；发现已有正式字幕时先返回覆盖确认，不会静默覆盖，确认后才挂载到播放器。
- 字幕草稿支持多个 cue：主进程统一校验 cue 数量、时间范围、文本长度、排序和重叠边界；旧版单 cue manifest 仍可读取，避免升级后丢失已有草稿。
- 草稿列表支持选择多个同一媒体版本的草稿合并为新草稿，原草稿保留；不同媒体指纹不能合并，合并结果仍不会自动成为正式字幕。
- VTT / SRT 正式导入会完整保留多 cue 时间轴；字幕写入器补充 VTT / SRT / ASS 多 cue 格式回归，覆盖 cue 序号、换行和时间格式。
- 正式字幕导入生成的同目录 VTT / SRT 会在重新打开同一媒体时自动回填；恢复按当前媒体路径解析，切换到没有旁车字幕的其他媒体不会沿用上一媒体的字幕。
- 旁车 VTT / SRT 在恢复前会由主进程校验 cue、时间轴和文本；空文件、坏时间码或没有有效 cue 时展示可观察的失败提示，不会静默挂载，也不会继续回退到旧缓存。
- 视觉索引重建只更新 `subtitle` / `visual` 证据，会保留同一媒体指纹下的 OCR、场景和实体派生证据；媒体版本变化时旧派生证据不会污染新版本。
- OCR、字幕草稿和视觉索引共用 `path:size:mtime` 媒体指纹；旧版草稿仍允许通过兼容校验导入，避免指纹格式统一后丢失用户已有草稿。
- 真实 Electron Smoke 已覆盖 provider 设置写回、能力诊断、双草稿显式保存、同媒体合并、VTT / SRT 首次导入、同名字幕覆盖确认、两个时间点播放器挂载、草稿删除和 stale 媒体隔离。
- 真实 Electron Smoke 还覆盖重启后的正式字幕旁车回填、相邻媒体无字幕泄漏、坏旁车失败提示且不挂载，以及视觉索引并发期间 OCR 搜索仍可定位。

## 字幕缓存与编辑器联动
- 翻译结果记录源字幕旁车的 revision；当正式字幕覆盖后路径不变但 revision 变化时，Renderer 不再把旧译文当作当前上下文，旧结果会被清理。
- 翻译缓存继续使用源字幕正文哈希区分不同内容，并在 subtitle cache manifest 中记录源字幕 revision，便于排查同路径覆盖、语言、模型和 glossary 组合下的缓存命中。
- 正式字幕导入会返回写入后的实际 sidecar revision，并同步清空当前翻译状态；重新打开媒体时 source / translation 结果按同一源字幕版本恢复。
- 编辑器字幕加载同时维护 source 和 translation 两条轨道；正式字幕 revision 变化会重新触发双轨加载，既能补齐新 cue，又沿用脚本 merge 规则保留人工编辑文本、词级时间和删除标记。
- 总结结果和总结缓存 manifest 同样记录来源字幕 revision；同一路径正式字幕覆盖后，Renderer 会校验来源路径、来源类型、目标语言、模型、模式和 revision，旧总结不会继续显示。
- 总结优先使用当前有效的译文版本；译文版本失效后会回退到当前原文版本，导出 Markdown / TXT / JSON 前也会再次校验当前总结来源，避免把旧字幕总结导出到新字幕。
- 真实 Electron Smoke 通过本地假翻译服务生成旧译文，再覆盖导入不同正文的正式字幕，确认旧翻译状态消失、新 source 显示、重启后新字幕恢复，证据表最终保持单条替换后的字幕证据。
- 真实 Electron Smoke 额外生成旧总结，覆盖正式字幕后检查总结面板为空、旧标题不再出现，重启后也不恢复旧总结；同一 Smoke 继续覆盖 OCR、视觉证据、跨媒体隔离、坏旁车和 stale 媒体保护。

## 字幕版本冲突预览与显式重载

- 编辑项目持久化 `captionSourceRevision`，将原文 / 译文 sidecar revision 绑定到当前接受版本；旧项目无该字段时兼容首次建立基线。
- 编辑器检测到正式字幕版本变化且内容确有差异时暂停自动 merge，展示变更、新增和删除预览；“保留当前编辑”只接受新 revision，“强制重载字幕”才替换字幕轨和脚本轨，同时保留视频、图形、画中画和时间线编辑。
- 词级 Whisper JSON sidecar 只有在拼接文本与当前正式字幕一致时才复用；字幕改写后不再把旧词级时间数据挂到新文本上。
- 新增 `npm run smoke:editing-caption-reload`，使用真实 Electron 验证手工脚本不被静默覆盖、保留分支、强制重载分支，并输出冲突 / 重载截图与无 Renderer 错误证据。
- 冲突预览保留完整 cue diff，不再只展示前 12 条；详情支持按当前 / 新字幕文本搜索、按变更类型和字幕轨筛选，并按 8 条一页浏览，空结果会明确提示。
- 每条差异显示其当前 / 新字幕覆盖的时间范围，并支持按起止秒筛选；变更后时间移动的 cue 会按新旧区间并集命中，避免用户因版本位移漏掉冲突。
- 每条差异提供“定位当前编辑”和“定位新字幕”动作，点击后直接移动编辑播放头；added / removed 缺少对应时间的一侧会自动禁用，避免误定位。
- 点击差异定位时会同步选中对应的脚本 cue 和字幕时间轴卡片；translation diff 会映射到对应的 source 脚本段，added 且当前工程不存在的 cue 不制造虚假选中。
- incoming-only cue 支持“预览新字幕”：冲突确认期间以只读临时卡片和状态条显示新文本 / 时间范围，明确标注尚未写入工程；点击关闭、保留当前编辑或强制重载时都会清理临时预览。
- 新增字幕预览会按 source segment 配对 incoming 原文 / 译文：时间轴显示两张只读临时卡片，对照条明确展示“当前工程（无内容）→ 新字幕”、两条轨道文本和各自时间范围；预览不会写入 `captions` 或 `scriptSegments`。
- changed cue 在点击任一定位动作后会投影 incoming 原文 / 译文 ghost 卡片，并在对照条同时展示 current / incoming 文本与时间；旧实体仍保持可选中，时间移动通过 current / incoming 区间并集可回溯，不会改写工程。
- changed cue 支持单条“接受新字幕”：只替换用户确认的 source / translation cue，并同步对应脚本文本、源时间和词级数据；未确认的其他差异继续保留，操作会进入编辑器撤销 / 重做历史。
- added cue 支持单条“加入工程”：source cue 会创建或合并对应脚本段和时间轴卡片，translation cue 只补充对应脚本译文；其他新增、修改和删除差异保持待处理，操作同样进入撤销 / 重做历史。
- removed cue 支持单条“保留当前字幕”或“从工程移除”：保留会记录当前字幕对该新 revision 的处理结果，原文和译文 removed row 可以分别裁决；source cue 从工程移除时默认同步移除对应译文并将脚本段标记为 `deleted`，但如果译文 row 已单独保留，则保留译文 caption / `translationText`，只移除原文；translation cue 从工程移除只清除对应译文。所有已处理 row 都进入 revision resolution，重算 diff 时不会把已确认的删除误报为新增，决策进入撤销 / 重做历史，并兼容真实 loader 的 source-prefixed ID。
- 孤立译文会由 `scriptSegment.deleted` 与 translation caption 的 loader ID 关系统一识别；时间轴显示四语言提示和虚线状态，仍允许调整时间，但当前 source-led 字幕导出不会包含孤立译文。
- SRT、VTT、烧录 ASS 和时间线导出可用性判断共用 `getEditingCaptionsForSubtitleExport`，避免孤立译文只在某一种导出路径意外出现；编辑器同时支持视频加同名 SRT 的字幕导出，以及只生成 SRT / VTT / ASS 的原文 / 译文字幕文件导出。
- 新增 `npm run smoke:editing-orphan-translation`，真实 Electron reload 后验证 source caption 不补回、孤立 translation 保留、脚本继续 `deleted`，并检查状态提示、data attribute 与 `consoleErrors:[]`。
- 恢复已删除脚本段时，原文字幕按恢复后的源时间重新物化；如果工程中已有孤立译文，则按兼容 loader ID 重新关联并保留用户已编辑的译文文本、时间和实体 ID，不再用脚本快照覆盖它。
- 脚本面板会对仍有孤立译文的已删除行显示“恢复原文并重新关联译文”提示；恢复按钮提供稳定测试入口，恢复后孤立状态提示和虚线样式会自动消失。
- 恢复、撤销、重做共用同一份编辑历史快照：撤销会回到孤立译文状态，重做会恢复原文并重新关联译文，用户调整后的译文时间保持不变。
- 恢复脚本段会按源范围在编辑时间轴上的每个可见区间分别物化原文 / 新建译文字幕；同一素材被重复插入且区间不连续时，不会生成跨越中间画面的字幕块，删除动作也会识别这些片段。
- 编辑器导出新增“仅译文字幕”模式：仍导出编辑后视频和同名 SRT，但 SRT 只包含活动译文轨；同时新增独立“原文字幕文件”和“译文字幕文件”模式，可选择 SRT、VTT 或带当前样式 / karaoke 时间的 ASS，只按当前工程字幕时间线生成文件，不生成视频；孤立译文不会进入这些导出模式，基础剪辑窗口继续保持原有三种导出方式。
- 多段字幕片段持久化 `editedRangeGroupId` / `editedRangeIndex`，重排时按自身片段索引映射到新编辑时间，不再让每个片段重复展开到全部可见区间；旧版派生 ID 也会按脚本段和数字后缀兼容恢复。
- source / translation sidecar 重载按脚本片段族群比较，合法的 `segment` / `segment-1` 不会被误报为 removed；接受外部更新时会保留每个片段的成片位置、实体 ID 和片段关系，只同步文本、源锚点与词级数据。
- sidecar 自动回灌按 script segment 关系去重，兼容真实 loader 的 source-prefixed / translation-prefixed ID，避免恢复后同时出现生成字幕和 loader 字幕；新增 `npm run smoke:editing-fragment-reload` 覆盖恢复、重排、重开、双轨刷新和控制台健康。
- 工程文件导入 / 导出会完整保留 fragment group / index，并拒绝只提供一半的关系元数据；force reload 更新片段文本和词级数据时继续保留用户重排后的成片位置，同时从 canonical incoming cue 重建单一脚本行，不会生成重复脚本段。
- sidecar 删除预览也按 fragment family 聚合为一条 source / translation diff；source remove + translation keep 会移除族群内全部原文片段、保留全部译文片段，并在 reload 后继续保持孤立译文的片段索引与脚本 deleted 状态。
- fragment / script segment 关联在已知 `sourceId` 不一致时拒绝匹配；跨媒体替换后，旧素材的 deleted script 不会把新素材同 ID、同时间范围的 translation 误判为孤立译文。新增 `npm run smoke:editing-cross-source-isolation` 覆盖真实素材拖拽替换、reload 和控制台健康。
- sidecar loader 只扫描仍被时间线片段使用的素材；当前文件的优先字幕路径按真实媒体路径匹配，不再按 `sources[0]` 的数组位置绑定，避免替换后旧素材旁车字幕参与新素材 reload。
- `captionSourceRevision` 的 reload key 同时记录活动 source 集合；当旧素材仍在磁盘但已经没有时间线片段时，旧素材 sidecar 被修改不会触发冲突预览或 force reload。跨媒体 Smoke 额外覆盖旧 source / translation sidecar 修改后的 reload，确认冲突数、旧 sidecar 预览数和 Renderer 错误均为 0。
- 编辑项目新增 `captionSourceRevisions` manifest，按活动素材的 source / translation sidecar 分别记录文件 revision；工程文件导入 / 导出会校验并完整保留该 manifest，旧项目仍兼容单一 `captionSourceRevision`。
- 多素材同时存在时，reload key 会携带每个活动 source 的路径和双轨 revision；某个活动素材的原文或译文旁车被修改、删除或重新生成时，只对对应素材生成冲突预览，不会把另一个素材的差异混在一起。
- 新增 `npm run smoke:editing-multi-source-revision`，真实 Electron 覆盖第二素材 source / translation 同时更新，以及第二素材两条 sidecar 删除；验证差异只归属于第二素材、force reload 更新 manifest，删除后 revision 回到 `null` 且第一素材不受影响。
- 打开 `.aivproj` 时如果素材绝对路径已失效，会要求重新选择视频，并按文件名与时长保守匹配；source ID、片段引用和字幕关系保持不变，无法唯一匹配或素材时长不足时拒绝修复，不按选择顺序猜测。
- 字幕冲突预览的每条差异会显示所属素材文件名，并把 `sourceId` 参与当前 / incoming caption 的匹配；多素材同时间段的差异不会因为字幕 ID 相同而互相应用。
- `.aivproj` 保存时为绝对素材路径写入相对工程文件目录的 `relativePath` 提示；打开工程只在提示目标真实存在时自动恢复路径，旧工程和无法恢复的素材继续进入显式修复流程。
- 编辑器工具栏新增“重建字幕版本清单”入口：只重新读取活动素材的 source / translation sidecar revision 并更新 manifest，不直接覆盖当前时间线字幕；真实 Electron Smoke 覆盖按钮点击、工程更新时间、状态提示和后续多素材冲突回归。
- 字幕旁车候选按配置目标语言、通用 `translated` / `translation` 命名、区域语言别名和大小写扩展名排序；空文件或无有效 cue 的候选会跳过，冲突预览可展开查看实际选中路径与全部候选。
- 当多个候选解析出不同字幕内容时，冲突预览会显示有效候选数量与当前按优先级选中的序号；解析内容相同的大小写 / 命名别名只保留一条有效候选提示，避免 macOS 大小写文件系统产生重复告警。
- 冲突预览可对另一份有效 source / translation 旁车执行“切换并采用”：工程持久化用户选择的 preferred path，重新加载双轨字幕并更新 revision manifest；切换、撤销和重做都保留完整工程历史。
- 新增 `npm run smoke:editing-sidecar-paths`，真实 Electron 覆盖空 `.SRT` 后回退有效 `.VTT`、`zh` 到 `zh-CN` 区域别名、双轨候选展示和控制台健康。
- 工程素材和旁车路径提示同时接受 `/` 与 `\\` 分隔符；打开工程时会逐段按实际目录项恢复大小写不同的路径，遇到同级多个大小写相同候选时保持未解析并进入显式修复流程。
- 工程“另存为”发现目标 `.aivproj` 已存在时，会先展示带完整路径的覆盖确认；用户取消后不写文件，覆盖按钮和取消按钮均支持四种界面语言。
- 工程路径提示新增 Windows 分隔符、大小写路径恢复和另存为覆盖边界测试；`smoke:editing-sidecar-paths` 继续验证真实 Electron 字幕路径链路无 Renderer 控制台错误。
- 工程打开遇到失效素材并进入人工修复时，状态摘要会显示已匹配数量、未解决数量、歧义数量，以及每个 source 从旧文件名到新路径的映射；不唯一匹配会列出候选路径并保持工程不变。
- 新增 `npm run smoke:editing-project-repair`，真实 Electron 验证失效 `.aivproj` 的候选重绑定、source ID 保持、修复摘要和 `consoleErrors:[]`；四种语言均有修复摘要文案。
- 人工重定位素材时，如果 source / translation 的固定旁车路径没有相对工程提示，会清除该旧偏好并按新素材文件名重新生成候选；存在有效相对提示的明确旁车选择会保留，摘要会显示清理数量。
- 字幕 loader 会保留解析内容相同的候选路径分组，同时继续把 `validCandidatePaths` 作为不同内容的代表路径；大小写或命名别名不会被误报成需要切换的不同字幕。
- 冲突面板和工程状态会分别显示“内容相同的候选别名”与“内容不同的候选”，并列出当前选中路径、有效路径总数和内容变体数；只有内容不同的候选提供人工切换入口。
- `npm run smoke:editing-sidecar-paths` 额外验证同内容候选分组、不同内容候选摘要和状态栏解释，继续检查候选切换、撤销 / 重做及 `consoleErrors:[]`。
- 工程状态栏对多份字幕候选只显示素材 / 轨道、有效路径数量、内容变体数和当前文件名级摘要；点击“查看完整候选路径”后才展开完整路径、等价别名和需要确认的不同内容候选，避免长路径挤压时间线工具栏。
- 完整候选详情会按“素材文件 / 原文或译文”分组，每组独立列出当前完整路径、内容相同别名和内容不同候选；多素材工程不会把不同 source 的证据混在同一条列表里。
- 候选详情分组使用原生嵌套 `details / summary` 独立折叠；每个素材 / 字幕轨可单独展开，支持鼠标和键盘操作，关闭一个分组不会影响其他分组。
- 候选详情的外层和分组展开状态由独立的、版本化 renderer UI 偏好存储按 `project.id` 恢复；字幕候选审计状态刷新、短暂切换为普通状态、整页重载或 Electron 重启后都会保留展开位置，切换工程仍使用各自偏好，且不会写入 `EditingProject` 或 `.aivproj`。
- UI 偏好只保存布尔开合状态和工程 / 分组 ID，最多保留 32 个工程、每个工程 32 个分组；localStorage JSON 损坏、版本不支持或存储不可用时回退为默认折叠，不写入媒体路径、工程路径或其他敏感内容。
- 打开候选审计状态时会以本地 `editing-projects.v1` 中仍存在的工程 ID 为白名单，自动清理已删除或已淘汰工程的 UI 偏好；当前工程 ID 会额外保留，避免新建或外部打开工程尚未写入索引时丢失展开状态。
- 候选详情提供“恢复候选详情默认”入口，仅将当前工程的外层和分组状态恢复为关闭并持久化默认值；其他工程偏好保持不变，不修改 `EditingProject`、`.aivproj`、字幕或 revision。
- 候选详情提供“恢复所有候选详情默认”入口；操作前显示本地化确认提示，确认后清空全部工程的独立候选详情 UI 偏好，并将当前外层 / 分组同步关闭；取消确认不会改变任何工程偏好，也不修改工程数据、字幕或 revision。
- 候选审计状态带有明确来源标记；候选从多份有效旁车恢复为无歧义或无有效字幕时，重建字幕版本清单会清除过期候选详情，但会保留项目保存等其他来源的状态提示；真实 Electron Smoke 覆盖文件移走、清单重建、状态清除和文件恢复。
- 发布链路新增项目 MIT `LICENSE` 与直接运行时依赖清单；三平台打包前校验清单，Electron 安装包资源检查强制包含 `LICENSE` 和 `THIRD_PARTY_LICENSES.md`，GitHub Release 继续复用同一批已校验产物。
- 发布链路新增固定 revision 的 SigLIP2 视觉模型暂存；打包前生成 `runtime-metadata.json`，记录 whisper.cpp、FFmpeg、libheif、macOS `sips` fallback 和视觉模型文件的版本、构建特征与 SHA-256，安装包资源检查会阻断缺失元数据的产物。
- GitHub Release 发布前会从合并后的三平台产物生成 `release-manifest.json`，记录每个安装包和更新元数据的大小与 SHA-256。
- GitHub Release 创建后会通过只读 API 回读实际资产，流式下载并核对每个文件的大小、SHA-256 和 `release-manifest.json` 内容；远端缺失、额外或漂移都会让发布任务失败。
- 远端回读会拒绝所有不在本地 manifest 中的附件（包括调试 YAML 等非发布文件），并对成功报告及异常下载消息中的 URL 查询参数和 fragment 做脱敏，避免把远端凭据写入审计报告。
- 发布 job 会先校验 `v<package.json.version>` 与 tag 完全一致，再校验每个 `latest*.yml` 的版本和 `url` / `path` 引用都指向当前批次资产；artifact policy 只接收 `latest*.yml`，不会把 `builder-debug.yml` 等调试文件发布到 GitHub Release。
- 三个平台的构建 job 会在上传前执行平台产物契约：macOS 必须有 DMG / ZIP 与 `latest-mac.yml`，Windows 的 x64 / arm64 安装包会合并到包含两种架构文件的 `latest.yml`，Linux 则分别提供 `latest-linux.yml` 与 `latest-linux-arm64.yml`；缺包、跨平台包泄漏或 metadata 命名错误会在合并 manifest 前阻断发布。
- 构建上传前还会读取安装包格式边界：DMG 校验 UDIF `koly` trailer，ZIP 校验对应容器头，Windows EXE 校验 PE `MZ`，Linux AppImage 校验 ELF、DEB 校验 ar 头；空文件或伪装文件不会仅凭文件名进入发布批次。
- 每个构建 Runner 在上传前生成独立的 `platform-release-report-*.json` evidence artifact，记录平台契约、资产文件名、大小和 SHA-256；Windows 双架构更新元数据会在汇总 job 中合并，Linux arm64 保留标准的 `latest-linux-arm64.yml`，报告参与 workflow 证据留存但不会进入 GitHub Release。
- publish job 下载五组平台构建产物后，会先组装唯一文件集合、合并 Windows 更新元数据，再逐项核对三平台契约、文件集合、大小和 SHA-256，最后执行版本门禁和 `release-manifest.json` 生成；任一架构构建缺失、报告重叠、漂移或合并目录出现未报告资产都会阻断发布。
- 发布清单会记录受控的 CI provenance（提交 SHA、仓库、workflow、运行 ID 和尝试次数）；只允许 GitHub Actions 提供的非敏感标识进入 `release-manifest.json`，不写入 token、URL 查询凭据或本机绝对路径。
- 新增 `npm run release:dry-run` 本地发布演练：用小型格式签名 fixture 分别模拟 macOS / Windows / Linux Runner，执行平台契约、安装包格式、合并 evidence、版本、manifest 和哈希复核；演练不联网、不调用 GitHub API，临时目录默认自动清理。
- Release workflow 的 `workflow_dispatch` 新增 `verify_only` 模式：仍执行真实 macOS / Windows / Linux 构建、artifact 合并、evidence、版本和 manifest 门禁，但跳过 GitHub Release 创建和 GitHub 远端回读，适合真实 CI 发布前演练。
- publish job 会把合并目录的 `merged-platform-evidence.json` 作为独立 workflow artifact 留存，包含实际验证过的文件名、大小和 SHA-256；它只用于审计，不进入 GitHub Release。
- 发布配置为 Windows 与 Linux 安装包启用架构文件名；后续正式版本将分别构建 x64 / arm64 原生 Electron、whisper.cpp、FFmpeg、libheif 和 LanceDB 运行时，Windows 合并双架构 `latest.yml`，Linux 使用 electron-updater 约定的 `latest-linux-arm64.yml`，并在发布前检查安装包与内置二进制的架构。
- Windows ARM64 使用 LLVM 工具链构建 whisper.cpp，并下载带 SHA-256 校验的原生 ARM64 FFmpeg，避免把 x64 媒体运行时混入 ARM64 安装包。
- Windows x64 发布前会对 Chocolatey FFmpeg 安装执行最多三次指数退避，并以实际可携带二进制存在作为成功条件，避免网络 504 或包布局变化把 shim / 空安装误带入安装包。
- 新增 `npm run release:check-push` 本地只读推送前审计：检查工作区干净、相对 `origin/main` 的 diff、commit message 格式、敏感模式和内部计划文件边界；命令不会执行 push 或 workflow 操作。
- 发布汇总与合并 evidence 校验支持显式的根目录扫描边界；不会把平台安装包解包目录中的 `AIVPlayer.exe`、FFmpeg 等运行时文件误判为 Release 资产。
- publish job 会把五份架构 Runner evidence 合并为三份平台报告：Windows 合并 x64 / arm64 的安装包与最终 `latest.yml` 哈希，Linux 合并两种架构与各自 `latest*.yml` 哈希，再由合并目录 evidence 门禁校验完整文件集合。
- Windows 发布产物统一使用 `AIVPlayer-Setup-<version>-<arch>.exe` 命名，与 electron-updater 生成的 `latest.yml` 引用保持一致，避免版本门禁因空格 / 连字符差异阻断发版。

## 本地导入收件箱

- 视觉检索面板新增本地导入收件箱：用户可以配置多个本机目录，程序通过主进程扫描和目录 watcher 发现新视频；不联网下载，不修改原始媒体文件。
- 字幕编辑器支持多选字幕两点同步：记录目标起止画面后按线性比例映射选中字幕和词级时间，操作进入工程保存、撤销和重做链路。
- 视觉影视库支持保存本地文字搜索：可为 hybrid / visual 查询保存名称、证据类型和物体筛选条件，点击已保存条目可重跑，配置只写入当前用户目录并在重启后恢复。
- 收件箱条目使用规范化绝对路径的稳定 ID，记录文件大小 / 修改时间；重复扫描不会重复创建条目，文件版本变化会重新回到待确认状态。
- 条目状态支持待确认、已入队列、处理中、已就绪、已忽略、失败和文件已消失；失败条目可重试，忽略和队列动作都需要用户明确点击。
- 收件箱清单以用户数据目录下的 `media-import-inbox.json` 原子保存，限制目录、条目和单次扫描数量；隐藏文件、临时下载后缀和非视频文件不会进入清单。
- 收件箱条目支持标签、收藏、备注、来源和项目 ID；标签会去重并限制数量与长度，备注和来源也有长度上限，避免清单被异常输入膨胀。
- 可在视觉面板开启旁车元数据保存；元数据写入媒体同目录下隐藏的 `.aivplayer.json` 文件，采用临时文件 + rename 原子替换，重扫时会校验稳定媒体 ID 并回灌外部更新，不覆盖视频文件。
- 收件箱支持按文件名、标签、备注、来源、项目 ID搜索，并可按处理状态和收藏筛选；筛选只作用于视图，不会改变清单或原媒体。
- 新增扫描 / 取消 / 状态迁移 / watcher / 元数据更新 / 条目变更 / 处理进度 IPC 及 renderer 入口；“加入索引队列”现在由主进程处理器串联媒体探测、字幕 sidecar 检查、视觉索引和缩略图生成，只有视觉索引完成才进入 `ready`。
- 处理器将元数据、字幕、视觉索引三个阶段写入清单，字幕缺失是可接受的 `skipped`，视觉模型失败会保留失败原因；应用重启会恢复 `queued` / `processing` 条目，不重复创建同一路径任务。
- 真实 Electron Smoke 已验证隔离目录扫描发现视频、源文件消失后的 metadata 阶段失败、错误原因和 `media-import / failed` 任务中心事件持久化；重启扫描将条目标记为 `missing`，但失败阶段证据仍保留，Renderer 无错误。
- 手动全库索引、索引队列和收件箱处理器统一经过视觉索引调度器串行访问 LanceDB 与视觉模型；界面通过收件箱处理进度 IPC 展示当前条目和阶段，取消操作会中止当前任务并清理等待任务。
- `video_sources` 现在通过只读 sources IPC 暴露正式视觉素材库入口，展示已索引视频的缩略图、原路径和帧数；点击素材复用现有加载 / seek 链路回到原视频，不复制第二套索引。
- 正式视觉素材库查询会先刷新对应媒体的 `.aivplayer.json` sidecar，再按规范化媒体路径投影收件箱标签、收藏、备注、来源和项目 ID；没有收件箱元数据的已索引素材明确显示为未标注。
- 正式视觉素材库支持按文件名、路径、收件箱标签 / 来源 / 项目 / 备注搜索，仅看收藏，并按最近索引、文件名或帧数排序；筛选只作用于只读浏览视图，不改写索引或原视频。
- 真实 Electron Smoke 已验证正式素材库 metadata 搜索、收藏过滤、最近 / 文件名 / 帧数排序，以及修改 sidecar 后重启重新投影；使用隔离 LanceDB 与本地收件箱数据，Renderer 无错误。
- 正式视觉素材库按 100 条一页加载，支持继续加载更多已索引素材；分页偏移在主进程按稳定的最近索引顺序读取，避免大库打开时一次渲染全部卡片和缩略图。
- 真实 Electron Smoke 已用隔离 LanceDB `video_sources` fixture 验证 101 条素材：首屏显示 100 条，加载更多后追加第 101 条且无重复，末页隐藏加载按钮，Renderer 无错误。
- 定向回归覆盖递归扫描、临时文件过滤、去重、缺失文件、文件版本变化、原子清单恢复、sidecar 回灌、watcher 防抖、筛选、流水线失败恢复、索引互斥、场景 / 实体证据、实体目录持久化和正式库接线；完整本轮测试结果以最终验证记录为准。
- 收件箱可在当前筛选结果中多选待确认、失败、忽略或缺失条目，批量入队、忽略或重试；批量动作由主进程一次校验、一次原子保存，失败时不会留下半批状态。
- 真实 Electron Smoke 已验证批量入队、批量忽略、混合失败 / 忽略 / 缺失条目批量重试、错误信息持久化和重启恢复；使用隔离 userData 与缺失媒体，不依赖视觉模型，Renderer 无错误。
- 收件箱可在当前筛选结果中批量清理“已忽略 / 文件已消失”的记录；清理只移除用户数据目录中的收件箱条目，不删除媒体文件、sidecar、视觉索引或失败处理中任务。
- 真实 Electron Smoke 已验证收件箱批量清理：ignored / missing 与 ready 混选会被拒绝且不写盘，合法清理后只保留 ready 记录，原媒体文件仍存在，重启后清单保持一致且 Renderer 无错误。
- 视觉证据来源管理新增只读过期审计：按媒体当前状态显示当前、已变更、文件缺失或无法检查，支持按状态筛选和加载更多来源；审计不会自动删除任何派生证据。
- 真实 Electron Smoke 已验证证据来源 current / changed / missing 状态、缺失状态筛选、派生证据批量清理，以及清理后基础 visual 证据保留；使用隔离 LanceDB 和临时媒体，Renderer 无错误。
- 可选场景证据索引会通过 FFmpeg 场景切点生成有界 `scene` 时间段，写入现有 `video_evidence` 并保留其他证据类型；真实 Electron Smoke 已验证场景阶段完成、场景来源聚合为 2 条、文本检索可命中且时间范围 `[0,1]` 有界，Renderer 无错误。

## 任务中心事件模型

- 新增共享 `TaskCenterEvent` 协议，统一描述 ASR / 批量字幕、视觉索引、本地导入、OCR / TTS、短剧工作流和短剧素材生成的任务 ID、状态、进度、当前文件和更新时间。
- 保留各模块原有 IPC 事件和面板状态；主进程通过纯转换层把原始进度 fan-out 到任务中心，不改变原有业务取消、重试和持久化语义。
- 任务中心在应用全局浮层展示活动和最近结束的任务，活动任务优先排序；支持折叠和清除已结束任务，不把完整本地路径发送到渲染层。
- 任务中心状态覆盖排队、运行、暂停、完成、失败和取消；视觉索引与导入流水线的阶段进度会转换为统一 0–1 进度，ASR 直接复用原有 0–1 百分比；视觉全库导出显示查询 / 写入进度并提供取消入口。
- 视觉索引的可选场景证据阶段沿用同一个任务 ID，并按场景检测视频数转换为统一进度，不额外制造无法取消的后台任务。
- 视觉索引的可选实体证据阶段同样沿用同一个任务 ID，并按实体分析视频数转换为统一进度；实体标签模型加载失败会让本次索引明确失败，不会伪造“已完成”证据。
- 任务中心仅将完成、失败和取消的终态以 `task-center.json` 原子保存；应用重启后通过历史查询恢复最近终态，运行中的任务不会被伪造为仍在执行。
- “清除已结束”现在会通过主进程清理 `task-center.json` 中的终态历史并等待原子写入完成；Renderer 只有持久化成功后才移除本地历史，运行中的任务不受影响。
- 任务中心支持按任务名称、消息、当前文件和任务类型搜索，并按状态筛选；历史结果按 8 条分页展示，筛选与分页只改变观察层，不改变任务状态和持久化事实。
- 新增 task center adapter / model / store 单元测试及 IPC、preload、全局浮层接线测试；任务中心定向测试覆盖场景阶段进度映射，`npm run typecheck` 通过，构建仍需完成本轮最终校验。
- 真实 Electron Smoke 已验证 13 条历史事件的首末页、关键词搜索、失败状态筛选，以及筛选期间实时任务事件到达仍能保留；Renderer 无错误。
- 真实 Electron Smoke 已验证“清除已结束”闭环：旧终态被清除、活动任务保留，清理后由收件箱真实产生的新失败终态正常写入，重启后只恢复新终态且 Renderer 无错误。

## 编辑器画布交互层级

- 图形编辑器在同一图形发生画布移动、缩放或旋转后保持用户当前的编辑表单开合状态，不会因为状态持久化而再次遮挡画布。
- 旋转手柄保持在视频画布内部并位于图形内容之上，避免被视频帧裁切或被下方时间线截获；编辑模式字幕显示菜单提升到时间线之上，所有显示设置仍可直接操作。
- 编辑器长 Smoke 在进入画布操作前显式关闭图形编辑表单，并覆盖移动、缩放、旋转及撤销；最终同时检查字幕显示菜单、导出链路和 Renderer 控制台无错误。

## Flatpak 发布适配（第一阶段）

- 新增 `cn.quniv.aivplayer` Flatpak manifest、Electron BaseApp 配置、`zypak-wrapper` 启动脚本、桌面入口和 AppStream Metainfo，版本与 `package.json` 保持一致。
- 新增 Flatpak 专用 electron-builder 配置，仅打包应用代码、网页资源、许可证和第三方许可证清单，不复用 macOS 本机预编译的 FFmpeg、whisper.cpp、libheif 或视觉模型。
- 新增基于 `package-lock.json` 的离线 npm source 清单、`flatpak:generate-sources` 和 `flatpak:check`，并在 GitHub Actions 中对 manifest、桌面入口、Metainfo、版本 tag 和离线安装约束做静态检查。
- 当前只完成可持续迭代的第一阶段骨架；提交 Flathub 前仍需把 FFmpeg / whisper.cpp / libheif 和原生 npm 依赖改为可审计的源代码构建，并把 SigLIP2 从随包只读模型改为用户数据目录中的可校验安装流程。

## Flatpak Linux 构建验证入口

- GitHub Actions 新增 Linux x86_64 Flatpak 构建、manifest/repo lint 和 bundle artifact 流程；正式 manifest 固定包含 Flatpak 元数据的应用源码 commit，构建前临时替换为当前 checkout，确保 CI 与最终可重建输入一致。
- 视觉模型支持用户数据目录优先：Flatpak 不需要把 SigLIP2 大模型塞进只读应用包；视觉面板可将固定 revision 的九个模型文件下载到 `userData/models/vision`，并保留旧版安装包模型目录作为兼容 fallback。
- Flatpak manifest 已加入固定 commit 的 whisper.cpp `v1.9.1` 源码模块，并将 `whisper-cli` 安装到 `/app/bin`；启动 wrapper 显式设置路径，避免依赖宿主机或开发机上的 whisper.cpp。
- Flatpak manifest 已加入 protobuf `v30.2` 固定 SHA-512 的源码模块，远程 builder 生成 `/app/bin/protoc`，供 LanceDB 的 Rust protobuf build script 使用。
- Flatpak manifest 已加入官方 FFmpeg `8.1.2` 源码归档，固定 SHA-256，关闭 GPL/nonfree 与宿主依赖自动探测，并将 `ffmpeg` / `ffprobe` 安装到 `/app/bin`。
- Flatpak manifest 已加入固定源码的 libheif `v1.23.1`、libde265 `v1.0.16`、libjpeg-turbo `3.1.2` 和 x265 `3.4` 模块，关闭 libheif 插件加载与宿主可选后端探测并安装 `heif-enc` / `heif-convert`；x265 GPL-2.0 许可证作为 Flathub 审核边界单独记录，并用最小 patch 兼容 CMake 4。
- Flatpak manifest 已加入 LanceDB `v0.31.0` 的源码构建描述、Cargo.lock 最小 patch 和 Cargo 离线源码清单；本机只生成并静态检查构建输入，实际 Cargo 编译交给 Flathub/Linux builder。
- Flatpak 启动 wrapper 会按 Linux 架构设置 `NAPI_RS_NATIVE_LIBRARY_PATH`，强制使用 Flatpak 内源码编译的 LanceDB NAPI 绑定，避免运行时回退到 npm 平台预编译包。
- 新增 Flatpak 原生 npm 依赖审计命令 `npm run flatpak:audit-native`：从 `package-lock.json` 枚举 LanceDB、ONNX Runtime、Sharp/libvips 和 sherpa-onnx 的版本、平台条件、安装脚本和来源；默认只报告，`--strict` 可在源码重建方案完成后作为 Flathub 提交门禁。
- Flatpak 应用模块按架构固定引入 Electron `v43.2.0` Linux x64 / ARM64 发行包，并通过独立 `electronDist` 目录交给 electron-builder，保证应用打包阶段不再请求 GitHub。
- Flatpak 发布使用独立的 512×512 PNG 图标，不改变桌面端原始 1024×1024 品牌图标。
- Flatpak AppStream MetaInfo 补齐 OARS 1.1 年龄评级、品牌色和固定资源截图，避免构建完成后才发现商店元数据缺字段。
- GitHub Actions 支持手动选择 Linux ARM64 构建，使用 `ubuntu-24.04-arm` 原生 Runner；CI 生成的本地 manifest 不进入仓库，正式 Flathub manifest 保持完整 commit 固定。
- Flatpak GitHub Actions 会同步安装 `org.freedesktop.Sdk.Extension.rust-stable//25.08`，并在 workflow 自身变更时触发验证，确保 LanceDB Rust 源码模块与 manifest 的 SDK 输入一致。

## 视觉索引失败恢复

- 视觉索引错误会按规范化媒体路径生成稳定 ID，记录失败视频、错误消息、失败阶段、抽帧间隔、场景 / 实体选项、失败时间和重试次数；取消任务不会生成失败记录。
- 失败清单以用户数据目录下的 `library/vision-index-failures.json` 原子保存并限制记录数量与错误长度；重启后主进程恢复清单，成功完成索引后清理对应媒体的记录。
- 视觉索引失败清单支持多选后批量重试；每条失败记录继续沿用自己的抽帧间隔、场景 / 实体证据选项和重试计数，队列不会用最后一条任务的配置覆盖其他任务。
- 真实 Electron Smoke 已验证两条缺失媒体失败记录的批量重试、独立 scene 与 entity/object 配置、retryCount 写回和重启恢复；不依赖视觉模型，Renderer 无错误。
- 视觉面板展示待恢复任务，用户可以对单个视频重试；重试复用现有视觉索引队列、调度器和选项，不复制第二套索引流程；任务中心仍只展示文件名，不扩散完整路径。
- 实体标签目录支持多选后批量隐藏、显示或合并到一个目标标签；批量操作使用稳定 `labelId` 并一次性原子保存，不修改模型生成的证据行。
- 真实 Electron Smoke 已验证实体目录批量隐藏、批量显示、批量合并、目录文件持久化与重启恢复；Smoke 使用隔离 userData 和预置自定义标签，不触发视觉模型索引，Renderer 无错误。
- 实体标签目录支持创建自定义标签：用户提供显示名称、模型查询描述和别名后，标签以稳定 ID 写入用户数据目录，并在下一次可选实体索引中使用；旧内置标签和既有证据不被改写。
- 自定义标签最多保留 100 个，查询文本经过长度和空值校验；实体文本向量按标签定义缓存，目录新增或修改后会在下一次索引时刷新，不需要重启应用。

## 可恢复的视觉全库导出

- 全库 JSON / CSV 导出会在用户数据目录保存任务清单、分块目录和每个 part 的 SHA-256 检查点；每个 part 先原子写入，目标文件在完整校验后再原子替换。
- 应用重启后自动恢复 `queued` / `running` 导出任务；失败或取消的任务保留已验证分块，任务中心提供重试入口，只重写缺失或校验不匹配的 part。
- 真实 Electron Smoke 已覆盖隔离 userData 下的启动恢复与任务中心重试：`queued` / `running` 会恢复并进入可观察终态，失败任务重试不会替换原记录，已验证检查点文件保持不变，且 Renderer 无错误。
- 新建导出任务会保存 `video_frames`、`video_sources`、`video_captions`、`video_search_documents` 和 `video_evidence` 的 LanceDB table version；恢复 / 重试会对这些表使用固定版本读取，索引更新后不会把旧任务悄悄变成另一份结果。
- 固定版本只保证各相关表按任务 revision 读取，不改变 LanceDB 内部一次 `toArray()` 的读取粒度；查询仍受现有 100 万条安全上限约束，旧版本被清理时任务会明确失败而不是伪造成功。
- 任务清单不把绝对输出路径发送到 Renderer，任务中心只展示文件名；重试复用原格式、查询请求、输出路径和分块目录，不创建第二套导出任务协议。
- 任务中心支持对多个失败 / 取消的视觉全库导出批量用当前索引重建；一批任务共用一次 revision 与目录快照，但每个新任务仍有独立 taskId 和分块目录，原任务与审计记录保留。
- 批量或单个重建会避开已有活动任务和重复输出路径；批量结果会在任务中心显示创建数、跳过数和冲突数，不展示绝对路径，也不会删除原任务。

## Clip Inbox 选段集合迁移

- 选段集合可将当前集合导出为版本化 JSON 后，从文件选择器重新导入；导入会校验版本、标题、源信息、证据类型和时间范围，不接受无效或空选段。
- 导入不会复用备份中的集合 ID，而是生成新的本地集合，避免覆盖已有集合；原视频路径、源指纹、精确起止时间和证据 ID 会保留。
- 导入后的源文件仍通过现有可用性检查展示；如果媒体已迁移或不存在，用户可以继续使用原有“修复源文件”入口，不会因导入自动改写媒体或索引。
- 真实 Electron Smoke 已验证 JSON 导出、删除原集合、通过文件选择器导入生成新 ID、时间 / 证据恢复、缺失源提示和 Renderer 无错误。
- 选段集合支持勾选多个集合并导出为版本化批量 JSON 备份；导入入口同时兼容单集合与批量集合，批量恢复会为每个集合生成新的本地 ID。
- 真实 Electron Smoke 已验证勾选两个集合、批量导出、删除原集合、文件选择器批量导入、选段恢复和新 ID，Renderer 无错误。
- 选段集合支持勾选多个集合后批量删除；删除前显示不可撤销确认，主进程在一次事务中级联删除集合及其选段，完成后返回删除和跳过数量。
- 批量删除期间会禁用集合复制、导出、导入和选择操作；真实 Electron Smoke 已验证取消确认不改变数据、确认删除后列表和重启持久化均只保留未选集合。
- 选段集合支持勾选多个集合后按统一前缀 / 后缀批量重命名；输入规则会实时预览最多三条结果，确认后保留集合 ID、标签、排序和选段内容。
- 批量重命名期间会禁用其他集合批处理；真实 Electron Smoke 已验证无规则时按钮禁用、预览、确认重命名、重载持久化和元数据保持不变。
- 选段集合卡片支持直接编辑标题：点击铅笔进入输入态，Enter 保存、Escape 取消，也可以使用保存 / 取消按钮；标题最多 200 个字符，空标题会保留编辑态并提示错误。
- 单集合改名复用已有按 ID 保存链路，保存时保留集合 ID、标签、排序、选段和源文件引用；编辑期间会锁定其他集合批处理与卡片操作，避免全局输入状态被覆盖。
- 真实 Electron Smoke 已验证 Enter 保存、Escape 丢弃、空标题拦截、重载持久化、集合元数据保持和 Renderer 无错误；截图输出到临时目录。
- 当前边界：内联编辑只覆盖集合标题，不提供标签内联编辑、重复标题冲突提示、改名撤销或批量独立命名。
- 选段集合卡片支持标签内联编辑：点击标签旁铅笔进入输入态，标签以逗号分隔，Enter 保存、Escape 取消，空输入可以清空全部标签。
- 标签保存复用统一归一化规则，自动去重、去除空项并沿用单标签 40 字符、总数 20 个的上限；保存期间锁定其他集合批处理和卡片动作，保持集合 ID、排序和选段内容不变。
- 真实 Electron Smoke 已验证重复标签归一化、Escape 丢弃、清空标签、重设标签、重载持久化、选段元数据保持和 Renderer 无错误；截图输出到临时目录。
- 当前边界：标签编辑只覆盖集合级纯文本标签，不提供标签颜色、层级、批量独立标签、标签撤销或集合备注字段。
- 选段集合列表支持按集合名称或标签做本地包含筛选，也支持从已存在标签中选择精确标签；名称 / 标签条件组合时按 AND 语义过滤，并提供清除筛选和无匹配空态。
- 筛选只改变当前面板的可见集合，不写入数据库、不改变已选集合 ID；筛选统计展示当前可见数 / 总数，重载面板后恢复完整列表。
- 真实 Electron Smoke 已验证名称筛选 2 / 3、叠加标签筛选 1 / 3、清除筛选、空结果、重载恢复和集合数据未改变，截图输出到 `/private/tmp/aivplayer-collection-filter.png`。
- 当前边界：筛选只覆盖当前会话的集合名称 / 纯文本标签，不提供跨集合标签管理、模糊评分排序、筛选条件持久化或批量标签编辑。
- 选段集合支持对勾选的多个集合批量替换、追加或移除同一组标签；标签复用统一归一化规则，自动去重、去空并限制最多 20 个标签、每个最多 40 个字符。
- 批量标签更新在一次 SQLite 事务内完成：替换模式的空输入经确认后清空所选集合标签，追加 / 移除模式要求至少一个标签；集合标题、排序、选段、证据和源文件引用保持不变，成功后清空选择。
- 真实 Electron Smoke 已验证两个集合批量替换和追加、空输入清空、第三个集合移除标签、取消确认不改数据、元数据保持和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-batch-tag-modes.png`。
- 当前边界：批量标签只支持把同一组纯文本标签应用到所选集合，不提供按集合独立输入、标签颜色 / 层级或标签历史撤销。
- 选段集合新增标签管理器：按所有已保存集合聚合展示标签及使用数量，点击标签后可确认从所有匹配集合中全局清理该标签，不受当前名称 / 标签筛选或最多 20 个批量 ID 的限制。
- 全局标签清理在主进程使用独立 SQLite 事务，只更新命中的 `tags_json`，保留集合标题、排序、选段、证据和源文件引用；成功后同步更新当前面板统计，取消确认不写入数据。
- 真实 Electron Smoke 已验证标签数量统计、跨两个集合清理、第三个集合不受影响、标签消失、取消确认、元数据保持和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-manager.png`。
- 当前边界：标签管理器只支持纯文本标签的使用统计和全局移除，不提供标签重命名、颜色 / 层级、标签目录、操作历史或跨项目共享。
- 标签管理器支持把一个旧标签全局重命名为新标签；迁移覆盖所有匹配集合，若目标标签已存在则在同一事务中合并并去重，集合其他元数据保持不变。
- 重命名前会确认旧标签、新标签和命中集合数量；成功后更新标签统计和当前筛选，空目标、同名目标、无命中和取消确认都不会产生无效写入。
- 真实 Electron Smoke 已验证跨两个集合迁移、目标标签合并去重、源标签消失、未命中集合不变、取消确认、元数据保持和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-rename.png`。
- 当前边界：标签管理器仍只支持纯文本标签的统计、全局移除和全局重命名，不提供颜色 / 层级、标签目录、操作历史或跨项目共享。
- 选段集合标签支持独立元数据目录：可以为当前标签设置背景色、文字色和一个父标签；元数据存储在 `clip_tag_metadata` 表中，不改写既有集合的 `tags_json`，重启后继续可用。
- 标签管理器会在标签按钮上展示已设置的颜色和父标签关系；清理标签时同步删除对应元数据并解除其他标签对它的父级引用，重命名标签时迁移颜色、文字色和父级引用。
- 颜色只接受 `#RRGGBB`，父标签不能指向自身；当前只支持单层父标签，不提供多层目录、环路检测、标签图标、跨项目共享或操作历史。
- 真实 Electron Smoke 已验证背景色、文字色、父标签保存、集合标签数据不变、重载后元数据持久化和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-metadata.png`。

- 标签管理器支持撤销最近一次全局清理、全局重命名或样式保存；撤销快照写入 clip_tag_operation_history，应用重启后仍可继续按最近未撤销顺序恢复。
- 撤销会同时恢复受影响集合的标签、更新时间和标签元数据；历史最多保留 20 条，已经撤销的记录不会重复执行，并与集合批量标签替换 / 追加 / 移除操作共享标签操作历史。
- 真实 Electron Smoke 已验证清理前后标签和样式恢复、重启后历史仍存在、逆序撤销早先样式操作、历史耗尽和 Renderer 无错误，截图输出到 /private/tmp/aivplayer-collection-tag-undo.png。
- 批量标签替换、追加和移除也会写入同一撤销历史；批量清空所有标签后，面板会显示独立撤销卡片，恢复后重新出现标签管理器。
- 真实 Electron Smoke 已验证选中集合追加标签后撤销、批量清空后的独立撤销入口、未选集合不变和历史栈语义，截图输出到 /private/tmp/aivplayer-batch-tag-undo.png。

- 标签管理器支持重做最近一次已撤销的全局清理、全局重命名、样式保存或批量标签操作；重做快照同时保存操作前后的集合标签与完整标签元数据，清理和重命名不会残留撤销前的样式。
- 标签操作历史增加兼容迁移的 `redoable` 标记；重做按最近撤销时间逆序恢复，应用新标签操作会清空旧重做分支，旧版本只有撤销快照的记录仍可正常撤销但不会伪造重做入口。
- IPC / Preload / Renderer 提供独立重做历史查询和执行入口；标签管理器与无标签集合空态都显示重做卡片，重启后仍可继续重做，历史最多保留 20 条。
- 真实 Electron Smoke 已验证清理撤销后重启重做、元数据删除与恢复、批量标签重做、空标签状态入口、新操作清空旧重做分支和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-redo.png` 与 `/private/tmp/aivplayer-collection-batch-tag-redo.png`。
- 标签管理器新增只读标签操作历史时间线，按最近写入顺序展示最近 20 条清理、重命名、样式 / 元数据和批量标签操作，并区分已应用、已撤销和可重做状态。
- 历史列表复用同一 SQLite 操作历史，跨应用重启后继续显示；它只反映状态，不改变现有撤销 / 重做执行语义，也不会为旧版缺少重做快照的记录伪造入口。
- 真实 Electron Smoke 已验证元数据与清理操作按写入顺序出现、撤销显示可重做、重做恢复已应用状态和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-history.png`。
- 时间线支持按清理、重命名、样式 / 元数据或批量标签筛选，并可将全部匹配历史导出为版本化 JSON；导出只包含操作 ID、类型、时间和状态，不包含集合内容、媒体路径或凭据。
- 真实 Electron Smoke 已验证筛选只保留目标类型、导出 Blob 文件名和 `schemaVersion` / `filter` / 条目数量正确，截图输出到 `/private/tmp/aivplayer-collection-tag-history-export.png`。
- 标签历史存储最多保留最近 100 条；查询支持按操作类型筛选并按偏移分页，界面每页最多展示 20 条，摘要显示筛选后的总数和当前页码。
- 每条历史记录可打开只读详情，展示操作类型、状态、时间、稳定操作 ID，以及集合 / 标签元数据快照数量；详情读取失败不会暴露快照内容或媒体路径。
- 真实 Electron Smoke 已验证 23 条历史记录的两页翻页、第二页条目数、上一页返回、单条详情快照计数和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-history-pagination.png`。
- “导出全部历史”会按当前筛选逐页读取最多 100 条本地保留记录，再生成完整 JSON；分页异常或空页会中止导出，不下载部分结果。
- 当前边界：时间线和详情只读，不支持从历史条目直接跳转或恢复、导入、跨设备同步或云端协作；重做仍只覆盖标签清理、重命名、元数据和批量标签操作。

- 选段集合标签支持多级父标签：标签按钮展示从根标签到当前标签的完整路径，父标签选择器会排除自身和所有会闭合父级环路的候选。
- SQLite Store 与 IPC 双层校验父标签链路；尝试把根标签设置为其后代时会拒绝写入并保留原元数据，已有异常环路读取路径也会安全终止。
- 真实 Electron Smoke 已验证 `项目 / 访谈 / 海边` 路径展示、环路请求拒绝、候选过滤、重载持久化和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-hierarchy.png`。
- 当前边界：父级关系仍按标签名保存在本机 Clip Inbox 元数据表，不跨设备同步；不提供拖拽排序、目录折叠、循环关系修复向导或标签图标。

- 选段集合标签支持备注和收藏状态；备注独立保存于 `clip_tag_metadata`，统一换行、限制 240 个字符，收藏状态可在标签管理器中直接切换。
- 既有数据库会自动补齐 `note` / `is_favorite` 列，标签重命名合并时保留目标备注并合并收藏状态，清理、撤销和重载继续覆盖这些元数据。
- 真实 Electron Smoke 已验证备注填写、收藏勾选、SQLite 保存、重载恢复、集合标签不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-notes.png`。
- 当前边界：备注和收藏只在本机标签目录生效，不提供全文搜索、收藏标签单独筛选、跨设备同步或云端共享。

- 标签管理器支持按标签名或备注内容本地搜索和“仅看收藏”筛选；筛选只改变标签按钮和当前管理项的可见范围，不写入数据库、不改变集合筛选和标签操作语义。
- 无匹配时显示标签空态，清除筛选会恢复全部标签；收藏筛选会让详情区跟随当前可见收藏标签，避免继续编辑被隐藏的标签。
- 真实 Electron Smoke 已验证标签搜索命中 1 项、仅收藏命中 1 项、无匹配空态、清除筛选、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-filter.png`。
- 真实 Electron Smoke 已验证备注搜索命中 2 项、标签名搜索仍可用、清除筛选、备注持久化、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-note-search.png`。
- 当前边界：筛选只在当前面板会话生效，不提供筛选条件持久化、备注高亮、按备注内容排序或云端标签视图。
- 标签管理器支持按标签名、使用次数降序和收藏优先排序；排序作用于当前搜索 / 收藏筛选后的标签按钮，同组按名称稳定排序。
- 标签管理器新增自定义顺序模式；用户可对当前管理标签执行上移 / 下移，顺序与排序模式保存到版本化本地 Renderer 偏好，新标签稳定追加，删除标签不会留下幽灵顺序。
- 排序和自定义顺序只改变 Renderer 视图，不写入 SQLite，不改变集合 `tags_json`、标签清理 / 重命名 / 批量操作和详情数据。
- 真实 Electron Smoke 已验证三种排序、收藏优先首项、使用次数首项、名称顺序、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-sort.png`。
- 真实 Electron Smoke 已验证自定义模式、上移操作、重载后顺序和模式持久化、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-custom-order.png`。
- 当前边界：自定义顺序只提供键盘 / 按钮式上移下移，不提供拖拽排序、跨设备同步、云端共享或写入集合数据。
- 标签管理器支持将颜色、文字色、父标签、备注和收藏状态导出为版本化 JSON，也可以从 JSON 恢复到当前已存在的标签。
- 导入只写入标签元数据，不携带媒体、不改写集合 `tags_json`；文件中不存在于当前集合的标签会安全跳过，父标签环路会拒绝整次导入。
- 导入先生成只读预览，逐项区分新增、无变化、冲突和未使用标签；冲突可选择覆盖本地、保留本地或跳过，确认后才作为一次元数据操作写入现有撤销历史。
- 真实 Electron Smoke 已验证导出版本、覆盖 / 保留本地 / 跳过三种决策、跳过未使用标签、集合标签不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-metadata-import-preview.png`。
- 当前边界：只支持本机 AIVPlayer 版本化 JSON，不提供媒体迁移、跨设备自动同步、字段级合并、冲突历史或导入文件远程共享。

- 标签管理器支持按父标签关系折叠 / 展开层级浏览；父级折叠会隐藏所有后代，独立根标签保持可见，按钮提供 `aria-expanded` 状态和键盘可操作入口。
- 折叠状态保存到版本化 Renderer localStorage 偏好，不写入 SQLite；当搜索或收藏筛选生效时，为避免把匹配结果藏在折叠树下，会展示符合条件的后代标签。
- 真实 Electron Smoke 已验证父级折叠、中间层折叠、筛选穿透折叠层级、清除筛选、重载后折叠偏好恢复、集合标签数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-tag-collapse-persisted.png`。
- 当前边界：不提供拖拽树编辑、批量展开 / 收起、跨设备同步或云端树视图；localStorage 失效时仅保持当前会话状态。
- 标签筛选器会按父标签路径展示选项；选择父标签时同时匹配带有该父标签及任意后代标签的集合，文本搜索仍与标签条件保持 AND 关系。
- 层级标签筛选只读取既有标签元数据，不会自动给集合写入父标签或改变集合 `tags_json`；筛选条件只在当前面板会话生效。
- 真实 Electron Smoke 已验证文本搜索、直接标签筛选、父标签包含后代筛选、无匹配空态、集合标签数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-filter-hierarchy.png`。
- 当前边界：不提供层级筛选多选、隐式标签继承写入、筛选条件持久化或云端标签视图。
- 选段集合标签筛选支持同时选择多个标签；默认按“任一标签”匹配，也可以切换为“全部标签”，每个选择仍沿父标签关系包含其后代集合。
- 多标签筛选与名称 / 标签文本搜索继续按 AND 组合，筛选仅作用于当前面板，不改变集合选择、集合标签或其他持久化数据。
- 真实 Electron Smoke 已验证两个标签 OR 命中 2 个集合、切换 AND 后命中 1 个集合、父标签后代匹配、清除筛选、重载重置和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-filter-multi.png`。
- 当前边界：不提供排除条件的嵌套分组表达式或云端标签视图；排除标签筛选见下方专节。
- 集合批处理的“全选”会根据当前视图工作：无筛选时选择全部集合，有筛选时只选择当前可见集合；清空可见选择不会误删筛选外已经选中的集合。
- 真实 Electron Smoke 已验证先选中 3 个集合、筛选到 2 个集合后只清除这 2 个、隐藏集合仍保持选中，再次全选可见集合后恢复 3 个选择，集合数据和 Renderer 均无异常。
- 当前边界：选择状态只保存在当前面板会话，不提供跨重载选择恢复、跨筛选条件的批处理历史或云端选择集。
- 选中的标签筛选会在多选器下方显示为带完整层级路径的摘要 chip；每个 chip 都可以独立移除，不需要重新操作原生多选器。
- 摘要 chip 只反映当前 Renderer 会话的筛选状态，不写入集合或标签元数据；清除全部筛选后摘要区域自动消失。
- 真实 Electron Smoke 已验证两个标签筛选、单独移除“采访”后保留“精选”、层级路径摘要、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-filter-summary.png`。
- 当前边界：不提供拖拽调整条件顺序、排除条件嵌套分组表达式或云端标签视图。

### Clip Inbox 选段集合筛选条件持久化

- 选段集合的名称查询、标签多选和 OR / AND 模式保存到版本化 Renderer `localStorage`；应用重载后会恢复上次筛选条件，加载完成后自动移除已经不存在的标签条件。
- 持久化只影响当前设备的筛选视图，不写入 SQLite、不改写集合标签或标签元数据；`localStorage` 不可用时退回当前会话内存状态，避免阻塞集合浏览。
- 真实 Electron Smoke 已验证偏好写入、重载恢复名称查询 / 两个标签 / AND 模式、单独清除恢复全部集合、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-filter-preferences.png`。
- 当前边界：不提供跨设备 / 云端同步、按项目隔离的筛选偏好、服务端共享筛选、筛选历史或排除条件嵌套表达式。

### Clip Inbox 保存筛选视图

- 用户可以给当前名称查询、标签多选和 OR / AND 模式命名保存；保存视图出现在本地列表中，点击即可恢复，重复名称会更新原视图并置顶。
- 保存视图最多保留 20 条，使用版本化 Renderer `localStorage`，不写入 SQLite、不改变集合数据；删除只移除对应视图，不影响当前集合或标签元数据。
- 真实 Electron Smoke 已验证保存视图跨重载恢复、清除当前条件后重新应用到 1 / 3 个集合、删除后从 localStorage 消失、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-filter-saved-view.png`。
- 当前边界：不提供跨设备 / 云端同步、按项目共享视图、筛选视图导入导出、权限控制或复杂排除条件嵌套。

### Clip Inbox 筛选视图 JSON 导入导出

- 已保存的筛选视图可以导出为版本化 JSON，也可以从 JSON 导入并与当前列表合并；导入按筛选条件去重，处理 ID 冲突并返回导入 / 跳过统计。
- 文件只包含视图名称、名称查询、包含标签、排除标签、OR / AND 模式和时间信息，不包含媒体路径、视频内容、集合标签数据或账号凭据；格式错误和版本不匹配会拒绝导入。
- 真实 Electron Smoke 已验证导出 Blob 文件名与 JSON 内容、写入临时文件后通过文件选择器导入、导入视图重新出现并可删除，截图输出到 `/private/tmp/aivplayer-collection-filter-import-export.png`。
- 当前边界：不提供云端同步、冲突历史、权限控制或跨应用协议；导入仍受本地 20 条上限约束。

### Clip Inbox 筛选视图导入冲突预览

- 筛选视图导入改为“读取 / 预览 / 确认”两阶段流程；预览区展示冲突、新增和将跳过的数量，不会因为选择文件就立即写入 `localStorage`。
- 同一筛选条件或同一视图 ID 被本地占用时标记为冲突；每条冲突默认保留本地，用户可以逐条选择覆盖本地、保留本地或跳过导入，只有点击确认后才应用结果。
- 真实 Electron Smoke 已验证无冲突文件的预览确认、同一条件改名文件的冲突覆盖、旧视图被替换、结果可删除、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-filter-import-conflict.png`。
- 当前边界：只支持整条筛选视图覆盖，不提供字段级合并、冲突历史、云端协作或跨设备同步。

### Clip Inbox 选段集合排除标签筛选

- 选段集合筛选支持独立的排除标签多选；命中排除标签及其任意后代标签的集合会被隐藏，排除条件优先于包含条件，包含 / 排除重叠时自动从另一侧移除。
- `excludedTags` 已加入版本化筛选偏好、保存筛选视图和 JSON 导入导出合同；旧版 localStorage 或 JSON 缺少该字段时兼容为空数组，不写入 SQLite 或集合 `tags_json`。
- 排除标签会显示为独立的危险色摘要 chip，可单独移除；保存视图摘要和导入冲突预览会展示排除条件，四种内置语言均有对应文案。
- 真实 Electron Smoke 已验证排除父标签会同时隐藏父级及后代集合、只留下 1 / 3 个集合，且覆盖组合筛选、重载恢复、保存视图、JSON 导出 / 导入、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-filter-excluded-tags.png`。
- 当前边界：不提供排除条件的嵌套 NOT / 任意布尔表达式、跨设备同步、云端标签视图或筛选历史。

### Clip Inbox 选段集合列表排序偏好

- 选段集合列表支持按最近更新、名称、选段数量和总时长排序；排序只改变当前可见集合的展示顺序，不改变集合选择范围、集合内容或每个集合内部的片段排序。
- 排序模式保存到版本化 Renderer `localStorage` `aivplayer.vision-clip-collection-order.v1`；坏值和旧环境安全回退到“最近更新”，不新增 SQLite 字段。
- 同排序模式下使用更新时间、创建时间和集合 ID 稳定消歧；名称排序使用当前 Electron 运行时的本地化比较，选段数量 / 总时长排序均按降序排列。
- 真实 Electron Smoke 已验证四种排序、排序偏好写入和重载恢复，并继续覆盖排除标签、组合筛选、可见集合选择、保存视图、JSON 导入 / 导出、集合数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-order.png`。
- 当前边界：不提供拖拽自定义顺序、跨设备同步、云端共享、按标签树目录单独排序或将列表排序写入集合数据。

### Clip Inbox 选段集合收藏 / 归档筛选

- 选段集合现在支持收藏和归档两个独立状态；单个集合卡片可以切换状态，批量选择工具栏也可以一次性设置或取消收藏 / 归档。
- SQLite 的 `clip_collections` 表新增 `is_favorite`、`is_archived` 两个布尔字段；启动时按列存在性执行兼容迁移，旧数据库中的集合默认保持未收藏、未归档，重复保存会保留已有状态。
- 集合视图新增“全部 / 活跃 / 收藏 / 已归档”筛选；收藏和归档状态纳入筛选偏好、保存筛选视图、筛选视图 JSON 导入导出及冲突预览，旧版本缺少 `visibility` 时安全回退到“全部”。
- 收藏 / 归档状态属于集合本身，会随集合 JSON 导出导入；复制集合会从未收藏、未归档开始，避免复制结果意外进入原集合的工作视图。
- 集合卡片上方提供“全部 / 活跃 / 收藏 / 归档”数量统计和快捷筛选按钮；收藏与归档可以同时计数，活跃数量只统计未归档集合，按钮会随单项或批量状态变化即时更新。
- 真实 Electron Smoke 已验证单项收藏、单项归档、活跃集合排除归档项、批量开关、重载后状态保留、收藏视图恢复、保存视图和 JSON 传输，以及 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-favorite-archive.png`。
- 状态统计和快捷入口的真实 Electron Smoke 已验证 `全部 3 / 活跃 2 / 收藏 1 / 归档 1`、快捷按钮筛选、数据不变和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-status-summary.png`。
- 收藏 / 归档单项和批量变更会写入独立的 SQLite 操作历史；撤销会恢复变更前的两个状态和 `updated_at`，集合已被删除时不会凭空恢复。
- 操作历史最多保留最近 20 条，无变化的状态提交不会产生历史；收藏 / 归档历史与标签操作历史分开维护，撤销后仍可继续撤销更早的未撤销操作。
- 真实 Electron Smoke 已补充单项与批量状态撤销、部分收藏状态恢复、归档状态不被误改、历史按钮更新和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-operation-undo.png`。
- 收藏 / 归档操作同时保存变更前后快照；撤销后可以重做，重做支持跨应用重启恢复，产生新操作时会清空已撤销的重做分支。
- 真实 Electron Smoke 已补充单项和批量收藏的撤销 / 重做、重做后归档状态保持、筛选链路和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-operation-redo.png`。
- 当前边界：重做只覆盖收藏 / 归档状态，不覆盖标签操作、集合删除或其他集合字段；状态只在本地数据库保存，不提供跨设备同步、云端共享、归档原因 / 时间线、收藏排序权重或操作历史可视化；“活跃”仅表示未归档，不等同于最近使用。

## 字幕视觉锚点候选预览

- 字幕同步面板支持以当前播放头作为显式视觉锚点，针对当前选中的一条或多条字幕生成只读偏移候选，不会在生成预览时修改工程。
- 候选会展示偏移量、目标时间范围、证据来源和“人工视觉锚点”置信度；候选超出时间线范围时保留预览但禁止应用，避免静默裁切字幕。
- 用户确认后复用现有两点同步、撤销 / 重做和工程保存链路，保持字幕文本与词级相对时间不变；当前不承诺音频峰值互相关或无人值守自动对齐。
- 真实 Electron Smoke 已验证多选字幕生成视觉锚点候选、展示当前播放头与人工视觉锚点证据、确认应用、撤销、重做和重载恢复；不把候选误报为自动音频对齐。

## 视觉相似镜头候选

- 视觉搜索结果支持对单条结果查找相似镜头；查询复用本地 SigLIP2 与 LanceDB 向量检索，不上传视频、缩略图或查询内容。
- 相似镜头查询会校验索引缩略图路径、限制返回数量为 1–100，并排除作为查询来源的原始帧；结果仍可打开、选择、保存 Clip Inbox 或创建剪辑工程。
- 结果区支持在原搜索结果与相似镜头候选之间切换，返回时恢复原结果、分页窗口和已选条目；四种界面语言都提供对应操作文案。
- 此能力只表达“画面向量相似候选”，不提供人物身份、人脸聚类、物体检测框、跨视频身份判断或自动剪辑结论。

## 相似镜头候选分组

- 相似镜头模式会把同一视频中时间相邻的连续抽帧折叠为镜头组，默认以 6 秒时间邻近窗口判断；跨视频或时间间隔过大的结果不会混组。
- 多帧镜头组展示文件名、时间范围和相似帧数量；组内每一帧仍可单独打开、选择或继续查找相似镜头，单帧结果保持原有布局。
- 分组只作用于当前相似候选的展示层，不写入新的索引或用户数据，也不代表人物身份、场景身份或剪辑结论。

## 相似镜头结果分页

- 相似镜头候选支持继续加载更多结果，沿用视觉搜索的 24、48、100 条窗口上限；追加结果会重新分组，但不会清空已选帧。
- 分页过程中仍可返回发起相似查询前的原搜索结果，原结果、分页窗口和选择状态会恢复；相似镜头查询不建立新的持久化快照。

## 可选物体检测 Provider

- 视频设置新增可选的 Transformers.js / ONNX 物体检测 Provider 状态面板，展示当前平台、Provider、模型文件和模型目录。
- 用户可以选择本机模型目录；应用只检查 `config.json`、`preprocessor_config.json`、`onnx/model.onnx` 和 `LICENSE` 收据，不自动联网下载或把第三方权重打包进安装包。
- 视觉检索结果支持对本地缩略图执行单帧物体检测，结果展示候选数量、阈值、置信度和边界框坐标；用户也可以在建立影视库时开启可选物体证据阶段，逐帧读取已有缩略图，将带边界框的 `object` 证据写入 `video_evidence`，并支持按来源清理。
- 搜索结果中的 `object` 证据会根据缩略图原始尺寸在画面上叠加检测框；边界框越界会被裁剪，非法框不会阻断结果点击，其他证据类型保持原有缩略图展示。
- 单帧物体检测结果卡片会复用检测图片并同时叠加全部候选框；缩略图读取失败时仍保留候选数量、置信度和坐标文字，不影响清除结果或继续搜索。
- 单帧检测候选列表支持鼠标悬停、键盘聚焦和点击选中，候选列表与对应边界框会同步高亮；再次点击同一候选可取消选中，其他候选类型不受影响。
- 单帧检测结果支持按物体标签模糊匹配和最低置信度筛选，缩略图边界框、候选列表与“显示 / 总数”计数同步收敛到当前筛选结果。
- 单帧检测结果会按当前筛选后的标签聚合类别和数量，类别 chip 可一键设置或取消标签筛选，类别统计与候选列表、边界框保持一致。
- 类别 chip 支持多选 OR 组合；多类别条件与自由标签文本、最低置信度共同筛选，当前选中的类别会以可访问的按下状态显示。
- 保存搜索会记录当前物体标签、最低置信度和类别多选条件；运行或导入旧保存搜索时会恢复这些条件，旧配置没有物体条件时继续按默认筛选运行；单帧检测卡片仍可独立观察筛选结果。
- 文字搜索和以图搜图会把已保存的物体条件传入主进程，在 `video_evidence` 全库扫描满足标签、类别和最低置信度的 `object` 证据，并与同一帧的视觉 / 字幕结果求交集；直接命中的物体证据也可以作为结果返回，加载更多会复用同一条件。
- 真实 Electron Smoke 已验证保存的 person 标签、person 类别和 80% 最低置信度条件只返回全库中的高置信度 person 证据，排除低置信度 person 和 vehicle 证据。
- `video_evidence` 为旧表提供一次性兼容迁移：新增边界框字段使用可空坐标，不会丢弃原有字幕、画面、OCR、场景、实体或说话人证据；物体证据会带源指纹、模型版本、时间范围和检测置信度，可参与文字搜索、类型筛选、保存搜索和 Clip Inbox。
- 当前没有内置模型，也不把帧级零样本标签包装成人物身份、人脸识别或跨视频身份聚类；模型缺失时会明确提示准备目录和 LICENSE 收据。

## Windows 安装包发布

- 发布工作流会按 tag 构建 Windows x64 / arm64 安装包，并将全部平台产物发布到 GitHub Release；当前不再向 MinIO 或 Cloudflare R2 上传额外副本，未来大陆下载入口计划使用 R2。
- 发布渠道已移除旧镜像：工作流、远端校验、脚本、测试和公开下载页面只保留 GitHub Release；未来大陆用户下载入口计划迁移到 Cloudflare R2。
- macOS 自动更新配置进入正式打包门禁：将固定的 `resources/app-update.yml` 在签名前作为 macOS 专属 `extraResources` 放入 `Contents/Resources`，并由 macOS 打包资源检查验证 GitHub 更新源；Windows / Linux 继续沿用原有更新产物流程，避免 macOS 安装后首次检查更新时报 `ENOENT`。

## ARM64 Snap 发布

- Linux ARM64 发布任务使用 `ubuntu-24.04-arm` 构建 Snap，并通过现有 `SNAP_CSC_LINK` 自动发布到 `aivplayer` 的 `stable` 与 `edge` 通道；Snap 客户端会按设备架构选择 ARM64 revision。
- ARM64 Release 产物现在同时包含 AppImage、deb 和 snap；手动 `verify_only` 运行只构建不上传，正式 tag 发布才写入 Snap Store。
- Snap 构建已从普通 Linux Job 拆为独立的 x64 / ARM64 Job，与 AppImage/deb 并行；独立 Job 负责 LXD、Snapcraft、Snap Store 发布和 Snap 产物 evidence，主 Linux Job 不再等待 Snap Store processing。
- Snap Job 复用普通 Linux Job 已完成的 `linux-unpacked` 应用目录，只在独立 runner 中执行 Snapcraft 封装和商店发布，避免再次执行 Electron 应用构建。
- Snap Store Listing 已完成基础商店资料：图标、`Photo and Video` 分类、MIT 许可证、官网、源码地址、Issue 地址，以及适合商店展示的 Summary / Description；后续版本发布不覆盖这些独立 Listing 字段。

## R2 超限安装包回退

- 发布下载清单遇到超过 Cloudflare R2 REST 单文件限制的安装包时，不再阻断整个发布；保留 GitHub Release 直链、文件大小和 SHA-256，其他安装包继续上传 R2。当前 x86_64 Snap 约 300,052,480 bytes，仅超过 300,000,000 bytes 约 52 KB，属于 R2 镜像限制而不是 Snap Store 限制。

## Flatpak LanceDB 预编译二进制

- Flatpak 构建不再从源码编译 LanceDB，也不依赖 `org.freedesktop.Sdk.Extension.rust-stable`；改为使用 npm 官方预编译平台包 `@lancedb/lancedb-linux-x64-gnu` / `@lancedb/lancedb-linux-arm64-gnu`（v0.31.0），与 Snap、macOS、Windows 渠道保持一致。
- 背景：2026-08-21 起 Flathub 上游 rust-stable 25.08 发布不完整（binding commit 元数据存在但内容对象 404，见 flathub/org.freedesktop.Sdk.Extension.rust-stable#538），导致依赖该扩展的 CI 持续失败；移除该依赖后发版流水线不再受上游单点故障影响。
- manifest 删除了 `lancedb-native` 与 `protobuf` 源码模块（protobuf 仅服务于 LanceDB 的 Rust build script）、Cargo 离线源码清单和 Cargo.lock patch；构建时把 `node_modules` 中的预编译 `.node` 复制到 `/app/lib/aivplayer/`，运行时加载路径不变，并随包提供 Apache-2.0 许可证收据。
- `scripts/check-flatpak.mjs` 的门禁同步反转：断言 manifest 不再包含 rust-stable / cargo build / protobuf，且离线 npm 清单必须包含 LanceDB linux 预编译包。

## Clip Inbox 选段集合批量合并

- 选段集合支持多选后合并为一个新集合；合并按 `sourceId`、视频路径和文件指纹隔离来源，并将同一来源中重叠或间隔不超过 0.5 秒的选段合并，避免把不同版本的视频错误拼接。
- 新集合会合并去重标签、保留选段证据和文本，并从未收藏、未归档状态开始；原集合不会被删除或修改，合并失败也不会产生部分新数据。
- Renderer 提供批量合并操作卡片和确认弹窗，至少选择两个集合后才可执行；完成后清空选择并显示新集合与选段数量，四种界面语言保持同一语义。
- 新增共享批量合并请求 / 结果类型、主进程 IPC 和 Preload API；纯函数与 Store 单测覆盖来源隔离、相邻区间、标签去重、状态重置、输入校验和原集合不突变。
- 真实 Electron Smoke 已验证真实勾选、确认合并、合并区间 `[1,5]`、标签 / 证据保留、原集合保留、重载持久化和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-batch-merge.png`。
- 当前边界：只创建一个固定本地化名称“合并选段集合”的新集合，不提供合并结果自定义名称、集合级撤销 / 删除原集合、云端协作或跨设备同步。

## Clip Inbox 批量合并预览与自定义名称

- 批量合并卡片现在允许输入新集合名称，默认使用当前语言的建议名称，并限制为 200 个字符；确认弹窗会明确显示最终名称。
- 执行前显示合并预览：来源集合列表、合并后的选段数量、去重后的标签；预览与实际合并复用同一套本地纯函数，避免界面摘要与写入结果口径不一致。
- 合并成功后仍只创建新集合并保留原集合，输入框清空；当前筛选、收藏 / 归档和集合内部选段逻辑不变。
- 真实 Electron Smoke 已验证自定义名称输入、预览来源 / 数量 / 标签、确认文案、合并结果和重载持久化，截图输出到 `/private/tmp/aivplayer-collection-batch-merge-preview-before.png` 与 `/private/tmp/aivplayer-collection-batch-merge-after.png`。
- 当前边界：预览是本地当前集合快照，不提供合并结果编辑、合并撤销或跨设备同步。

## Clip Inbox 批量合并逐片段差异预览

- 合并预览现在按来源集合展示每个原始选段的时间范围，并单独展示合并后实际写入的时间范围；时间格式统一为 `mm:ss.s`，超过一小时自动使用 `hh:mm:ss.s`。
- 预览数据由共享纯函数生成来源快照和合并结果，来源集合按 ID 去重并保持不突变；Renderer 只负责展示，IPC / Store 仍以主进程最终读取的数据为准。
- 真实 Electron Smoke 已验证两条来源范围 `00:01.0–00:03.0`、`00:02.6–00:05.0` 和合并后范围 `00:01.0–00:05.0`，同时验证自定义名称、标签 / 证据、原集合保留、重载持久化和 Renderer 无错误；截图输出到 `/private/tmp/aivplayer-collection-batch-merge-ranges-before.png` 与 `/private/tmp/aivplayer-collection-batch-merge-ranges-after.png`。
- 分阶段提交：`b4742f5 feat(选段集合) : 增加合并差异预览合同`（来源快照、合并结果预览类型和核心回归）；`4af687d feat(选段集合) : 展示逐片段合并差异`（Renderer、四语文案、范围格式化和样式）；`4d6299b test(选段集合) : 验证逐片段合并预览`（Electron Smoke 范围断言和来源门禁）。
- 当前边界：只展示时间范围差异，不提供波形 / 视频缩略图、单个范围编辑、冲突解决、合并撤销或跨设备同步。

## Clip Inbox 批量合并可选片段

- 合并预览中的每个来源选段现在都可以单独勾选或取消；取消后会实时重算已选来源数量、合并后时间范围和标签，所有片段取消时仍保留来源列表以便重新选择。
- 提交时 Renderer 会把每个来源集合的选段键传给主进程，Store 会重新从本地数据库筛选后再合并，避免只修改预览而实际仍写入未选片段；未提供选段选择时保持旧版全选兼容。
- 确认弹窗会同时显示集合数量和实际选段数量；成功后原集合保持不变，选择状态和名称输入一起清空。
- 真实 Electron Smoke 已验证取消 `00:10.0–00:11.0` 后只保留 2 个来源选段，最终范围 `[1,5]`，未选证据未写入，原集合保留、重载持久化和 Renderer 无错误；截图输出到 `/private/tmp/aivplayer-collection-batch-merge-selection-before.png` 与 `/private/tmp/aivplayer-collection-batch-merge-selection-after.png`。
- 分阶段提交：`978c8a4 feat(选段集合) : 支持选择合并片段`（共享选择合同、核心纯函数、Store、IPC 和回归）；`ec202e8 feat(选段集合) : 增加合并片段勾选`（Renderer、四语文案、样式和 UI 契约）；`d20a03d test(选段集合) : 验证合并片段勾选`（Electron Smoke 取消选段和来源门禁）。
- 当前边界：只支持按已有选段整体勾选，不提供单个范围裁切、波形 / 视频缩略图、拖拽排序、合并撤销或跨设备同步。

## Clip Inbox 批量合并单段范围编辑

- 合并预览中的每个来源选段支持编辑起点和终点，输入按源视频时长限制并保留一位小数；修改会实时反映到来源范围和合并后输出范围。
- 提供恢复原范围操作；无效或结束时间不大于起点的输入不会写入预览状态，提交时主进程再次校验范围，避免 Renderer 输入绕过数据边界。
- 真实 Electron Smoke 已验证 `00:10.0–00:11.0` 编辑为 `00:08.0–00:09.0`、恢复原范围、再取消该片段，最终新集合仍只写入 `[1,5]`；同时覆盖自定义名称、原集合保留、重载持久化和 Renderer 无错误，截图输出到 `/private/tmp/aivplayer-collection-batch-merge-range-edit-final-before.png` 与 `/private/tmp/aivplayer-collection-batch-merge-range-edit-final-after.png`。
- 分阶段提交：`5b7f1b6 feat(选段集合) : 支持合并范围覆盖`（共享覆盖合同、核心筛选和 Store 回归）；`5813514 feat(选段集合) : 增加合并范围编辑`（Renderer、四语文案、控件样式和 UI 契约）；`4c86963 test(选段集合) : 验证合并范围编辑`（范围修改、恢复、取消和来源门禁）；`841b12f style(选段集合) : 调整范围预览高度`（完整显示编辑控件）。
- 当前边界：只支持按已有选段编辑起止时间，不提供波形 / 视频缩略图、拖拽排序、单独拆分片段、合并撤销或跨设备同步。

## Clip Inbox 批量合并撤销与重做

- 批量合并现在会记录新生成集合的完整本地快照；撤销只删除这次生成的合并集合，原集合及其选段、标签和证据保持不变。
- 重做会按原集合 ID 恢复合并结果和全部选段数据，支持应用重启后继续重做；新的集合操作会清理旧的可重做状态。
- 为避免覆盖用户后续编辑，合并结果被修改或删除后，撤销 / 重做会拒绝执行并保留当前数据；Renderer 会根据 IPC 返回的新增 / 删除 ID 同步列表状态。
- 真实 Electron Smoke 已验证合并、撤销、原集合保留、重做、原 ID 恢复、重载持久化和 Renderer `consoleErrors: 0`；Store 单测覆盖完整快照、重启重做和编辑冲突保护。
- 分阶段提交：`094f8e4 feat(选段集合) : 支持合并撤销`（共享类型、SQLite 快照、撤销 / 重做和 Store 回归）；`356b9c0 feat(选段集合) : 接入合并撤销界面`（Renderer、四语文案和 UI 契约）；`30e4e23 test(选段集合) : 覆盖合并撤销 Smoke`（真实 Electron 交互验证）。
- 当前边界：只记录批量合并操作，不扩展普通手动删除、重命名、编辑或复制操作的撤销；历史仍为本地最近 20 条集合操作，不提供跨设备同步或云端协作。

## Clip Inbox 批量删除撤销与重做

- 批量删除现在会在同一 SQLite 事务内保存被删除集合的完整快照；撤销按原集合 ID 恢复标题、标签、状态、排序、选段和证据，未选集合不受影响。
- 重做会再次删除同一批集合；撤销 / 重做过程中遇到 ID 冲突或恢复后的集合被编辑时会拒绝覆盖，避免误删用户后续数据。
- 删除成功后会立即刷新集合操作历史，用户可以从现有撤销 / 重做卡片恢复或再次删除；单集合原有删除入口保持原语义不变。
- 真实 Electron Smoke 已验证取消确认不改数据、删除 2 个集合、撤销恢复 3 个集合、重做删除 2 个集合、重载持久化和 Renderer `consoleErrors: 0`；Store 单测覆盖完整快照、重启重做和编辑冲突保护。
- 分阶段提交：`3ca51ab feat(选段集合) : 支持批量删除撤销`（共享类型、SQLite 快照、撤销 / 重做和 Store 回归）；`bc835b3 feat(选段集合) : 接入删除撤销界面`（Renderer 刷新和 wiring 回归）；`d9d14e8 test(选段集合) : 覆盖删除撤销 Smoke`（真实 Electron 交互验证）。
- 当前边界：只记录批量删除，不把单集合手动删除、重命名、编辑或复制纳入历史；历史保留最近 20 条，不提供跨设备同步或云端回收站。

## Clip Inbox 批量重命名撤销与重做

- 批量重命名现在记录重命名前后的完整集合快照；撤销恢复原集合标题及全部集合数据，重做恢复批量标题。
- 撤销 / 重做按集合 ID 校验完整快照；用户在重命名后编辑集合时拒绝覆盖，避免误删或覆盖后续数据。
- 批量重命名成功后刷新集合操作历史，用户可从现有撤销 / 重做卡片立即恢复；单集合手动改名保持原有行为。
- 真实 Electron Smoke 已验证自定义前缀 / 后缀、预览、元数据保留、撤销、重做、重载持久化和 Renderer `consoleErrors: 0`；Store 单测覆盖完整快照、重启重做和编辑冲突保护。
- 分阶段提交：`f00c467 feat(选段集合) : 支持批量重命名撤销`（共享类型、SQLite 前后快照、撤销 / 重做和 Store 回归）；`c485f8f feat(选段集合) : 接入重命名撤销界面`（Renderer 刷新和 wiring 回归）；`b778760 test(选段集合) : 覆盖重命名撤销 Smoke`（真实 Electron 交互验证）。
- 当前边界：只记录批量重命名，不把单集合手动改名、编辑、复制或其他字段变更纳入历史；历史保留最近 20 条，不提供跨设备同步或云端协作。

## Clip Inbox 单集合内联改名撤销与重做

- 单集合卡片内联改名现在通过专用改名接口写入集合操作历史；撤销恢复原标题及完整集合数据，重做恢复新标题。
- 改名历史按集合 ID 和完整快照校验；集合被删除或被后续编辑时不会重新创建、覆盖或强制恢复用户数据。
- Enter 保存、Escape 取消和空标题保护保持原有交互；改名成功后立即刷新现有撤销 / 重做卡片，四种界面语言提供集合不存在提示。
- 真实 Electron Smoke 已验证 Enter 保存、撤销、重做、Escape 取消、空标题拦截、元数据保留、重载持久化和 Renderer `consoleErrors: 0`。
- 分阶段提交：`0c68ecb feat(选段集合) : 支持单集合改名撤销`（快照核心和 Store 回归）；`c1887d5 feat(选段集合) : 接入单集合改名 IPC`（专用 IPC / Preload）；`2216a10 feat(选段集合) : 接入单集合改名历史`（Renderer、四语提示和 UI 回归）；`b893d9b test(选段集合) : 验证单集合改名撤销`（真实 Electron Smoke）。
- 当前边界：只记录单集合标题内联改名，不把标签编辑、集合内容编辑、单集合删除或复制纳入这条历史；历史保留最近 20 条，不提供跨设备同步或云端协作。

## Clip Inbox 批量复制撤销与重做

- 批量复制现在在一次 SQLite 事务内保存全部新副本的完整快照；撤销删除本次生成的副本，原集合及其 ID、标签、选段和证据保持不变。
- 重做会恢复原副本 ID 和全部选段数据，支持应用重启后继续重做；复制结果被用户编辑或删除时，撤销 / 重做会拒绝覆盖。
- 批量复制成功后立即刷新集合操作历史，仍保持最多 20 个集合的批处理上限；单集合复制保持原有行为，不混入这条批量历史。
- 真实 Electron Smoke 已验证勾选 2 个集合、生成 2 个副本、撤销、重做、原 ID 恢复、原集合独立编辑、重载持久化和 Renderer `consoleErrors: 0`。
  - 分阶段提交：`4893faf feat(选段集合) : 支持批量复制撤销`（快照核心、撤销 / 重做和 Store 回归）；`d21156b feat(选段集合) : 接入批量复制历史`（Renderer 历史刷新和 wiring 回归）；`53741627 test(选段集合) : 验证批量复制撤销`（真实 Electron Smoke）。
- 当前边界：只记录批量复制，不把单集合复制、普通编辑或媒体文件复制纳入历史；历史保留最近 20 条，不提供跨设备同步或云端协作。

## Clip Inbox 单集合复制撤销与重做

- 单集合复制现在在同一 SQLite 事务内保存新副本的完整快照；撤销删除该副本，原集合及其 ID、标签、选段和证据保持不变。
- 重做会恢复原副本 ID 和全部选段数据；复制结果被用户编辑或删除时，撤销会拒绝覆盖，避免误删后续数据。
- 单集合复制成功后立即刷新现有集合撤销 / 重做卡片；复制、撤销、重做和重载后的状态继续沿用通用集合操作历史。
- 真实 Electron Smoke 已验证单集合复制、撤销、重做、原 ID 恢复、原集合独立编辑、重载持久化和 Renderer `consoleErrors: 0`。
- 分阶段提交：`267c6cbf feat(选段集合) : 支持单集合复制撤销`（副本快照、撤销 / 重做和 Store 回归）；`1e73008b feat(选段集合) : 接入单集合复制历史`（Renderer 历史刷新和 wiring 回归）；`d01ecb22 test(选段集合) : 验证单集合复制撤销`（真实 Electron Smoke）。
- 当前边界：只记录单集合复制，不把单集合删除、标签编辑、集合内容编辑或媒体文件复制纳入历史；历史保留最近 20 条，不提供跨设备同步或云端协作。

## Clip Inbox 单集合标签编辑撤销与重做

- 单集合卡片内联标签编辑现在写入统一标签操作历史；撤销恢复原标签，重做恢复编辑后的标签，集合标题、排序、选段、证据和源文件引用保持不变。
- 历史类型新增 `single`，与全局清理、重命名、样式 / 元数据和批量标签操作共用时间线、筛选、导出、撤销和重做入口。
- 单集合标签被后续编辑、删除或数据分支修改时，撤销 / 重做拒绝覆盖；空输入继续合法清空标签。
- 真实 Electron Smoke 已验证规范化标签保存、Escape 取消、单集合撤销、重做、清空 / 重设、重载持久化和 Renderer `consoleErrors: 0`；截图输出到 `/private/tmp/aivplayer-inline-tags-undo.png` 与 `/private/tmp/aivplayer-inline-tags-final.png`。
- 分阶段提交：`d8e0e971 feat(选段集合) : 支持单集合标签撤销`（标签历史核心、单测和冲突保护）；`f359820f feat(选段集合) : 接入单集合标签历史`（IPC、Preload、四语类型和 Renderer 接线）；`e99cf1c5 test(选段集合) : 验证单集合标签撤销`（真实 Electron Smoke）。
- 当前边界：只记录单集合标签数组编辑，不把集合标题、选段内容、颜色 / 父标签 / 备注元数据或媒体文件操作混入这条历史；历史保留最近 20 条，不提供跨设备同步或云端协作。

## Clip Inbox 单集合删除撤销与重做

- 单集合卡片删除现在写入现有集合操作历史；撤销恢复完整集合快照，重做再次删除目标集合，标签、收藏 / 归档状态、排序、选段、证据和源文件引用保持不变。
- 单集合删除与批量删除共用 `delete` 操作类型、集合撤销 / 重做卡片和最多 20 条本地历史；删除成功后刷新撤销入口并清理已删除集合的选择状态。
- 集合被撤销恢复后又被编辑、删除或发生 ID 冲突时，重做 / 撤销拒绝覆盖；不存在或空 ID 不新增历史，既有批量删除语义保持不变。
- 真实 Electron Smoke 已验证单集合删除、撤销、重做、重载后重做持久化、未删除集合保留和 Renderer `consoleErrors: 0`；截图输出到 `/private/tmp/aivplayer-single-delete-undo.png` 与 `/private/tmp/aivplayer-single-delete-final.png`。
- 分阶段提交：`9268dcb1 feat(选段集合) : 支持单集合删除撤销`（完整快照、事务、Store 回归和冲突保护）；`b4c7d9e8 feat(选段集合) : 接入单集合删除历史`（Renderer 刷新、选择清理、四语说明和接线回归）；`afe5e25a test(选段集合) : 验证单集合删除撤销`（真实 Electron Smoke 和 npm 入口）。
- 当前边界：只记录单集合卡片删除，不提供独立回收站、删除前确认弹窗、跨设备同步或云端回收站；历史保留最近 20 条，恢复后的集合被编辑时不会强行重做删除。

## Clip Inbox 单集合内容编辑撤销与重做

- 单集合“合并相邻选段”“反选时间范围”和“修复缺失源文件”现在通过专用内容更新入口保存完整前后快照，并进入现有集合撤销 / 重做时间线。
- 撤销恢复标题、标签、收藏 / 归档状态、排序、选段、证据和源文件引用等完整集合数据；重做恢复内容编辑后的快照，应用重启后仍可继续重做。
- 内容编辑结果被后续编辑、删除或其他字段变更时，撤销 / 重做会拒绝覆盖；无效集合、空选段和未发生变化的输入不会新增内容历史。
- 真实 Electron Smoke 已验证合并相邻选段、反选时间范围、撤销、重做、重载持久化、集合元数据保留和 Renderer `consoleErrors: 0`；截图输出到 `/private/tmp/aivplayer-content-undo.png` 与 `/private/tmp/aivplayer-content-final.png`。
- 分阶段提交：`f84c26a9 feat(选段集合) : 支持单集合内容撤销`（内容快照、事务更新、Store 回归和冲突保护）；`c648e610 feat(选段集合) : 接入内容编辑历史`（IPC、Preload、Renderer、四语说明和接线回归）；`0cc6207e test(选段集合) : 验证单集合内容撤销`（真实 Electron Smoke 和 npm 入口）。
- 当前边界：只记录上述单集合选段内容更新，不记录列表排序、标签元数据、媒体文件本身或独立回收站状态；历史保留最近 20 条，不提供跨设备同步或云端协作。

## Clip Inbox 集合操作历史可视化

- 集合面板新增最近 20 条本地操作时间线，展示收藏 / 归档、合并、删除、重命名、复制和内容编辑的操作类型、目标集合、集合数、选段数、时间和当前状态。
- 时间线只返回面向 UI 的摘要，不暴露完整快照 JSON 或媒体路径；删除、复制和合并即使目标集合已经不存在，也能从不可变快照显示标题与选段数量；收藏 / 归档记录会回查当前集合，已删除目标保留 ID 作为降级标识。
- 状态区分“已应用”“已撤销”和“可重做”，与现有撤销 / 重做卡片、SQLite 本地历史和应用重载保持一致；损坏或不匹配的历史摘要会被安全跳过。
- 四语言（zh-CN / en-US / ja-JP / ko-KR）均补充时间线文案，并保持现有集合操作入口不变。
- 真实 Electron Smoke 已验证时间线可见、收藏操作摘要、撤销后变为可重做、重做后恢复已应用、重载后的状态持久化和 Renderer `consoleErrors: 0`；截图输出到 `/private/tmp/aivplayer-collection-history-undo.png` 与 `/private/tmp/aivplayer-collection-history-final.png`。
- 分阶段提交：`92093f35 feat(选段集合) : 增加操作历史摘要`（shared 类型、SQLite 摘要查询和 Store 回归）；`e6f5dfaa feat(选段集合) : 接通操作历史查询协议`（IPC、Preload 和接线回归）；`5dacf649 feat(选段集合) : 展示操作历史时间线`（Renderer、样式、四语文案和 UI 回归）；`989706ba test(选段集合) : 验证操作历史时间线`（真实 Electron Smoke 和 npm 入口）。
- 验证合同：Store 定向测试 51 项、历史协议 / UI 接线测试 3 项通过，`npm run typecheck`、`npm run build` 和宿主权限下全量回归 328 个测试文件 / 1225 项通过。
- 当前边界：时间线只读，不提供逐条历史详情、逐条撤销、导出、跨设备同步或云端协作；仍沿用本地最近 20 条集合操作保留策略。

## Clip Inbox 单条集合操作历史详情

- 时间线每条记录新增“查看详情”入口，可展开操作前 / 操作后两组安全集合摘要，展示标题、集合 ID、标签、收藏 / 归档状态、排序方式和选段数量。
- 详情按操作类型适配快照：收藏 / 归档显示前后标记，重命名 / 内容编辑显示完整前后集合字段，删除显示删除前集合，复制 / 合并显示生成后的集合；不存在的另一侧明确显示为空状态。
- 详情查询只返回安全字段，不返回完整快照 JSON、媒体路径、证据文本或媒体文件内容；无效 ID、损坏快照和过期请求安全失败，不覆盖当前详情。
- 详情面板复用集合历史时间线的现有布局和四语言文案；刷新、撤销 / 重做后会清理旧详情，避免用户看到过期状态。
- 真实 Electron Smoke 已验证详情展开、前后状态、标题 / 标签 / ID / 选段数量、安全字段不泄露媒体信息、关闭详情和 Renderer `consoleErrors: 0`；截图输出到 `/private/tmp/aivplayer-collection-history-detail.png`。
- 分阶段提交：`74156187 feat(选段集合) : 支持历史详情查询`（安全详情类型、SQLite 查询和 Store 回归）；`3052ebb8 feat(选段集合) : 接通历史详情协议`（IPC、Preload 和接线回归）；`b8efb2c8 feat(选段集合) : 增加历史详情面板`（Renderer、样式、四语文案和 UI 回归）；`01dc9cf6 test(选段集合) : 验证历史详情交互`（真实 Electron Smoke 和 npm 入口）。
- 验证合同：Store 定向测试 53 项、历史协议 / UI 接线测试 3 项通过，`npm run typecheck`、`npm run build` 和宿主权限下全量回归 328 个测试文件 / 1227 项通过。
- 当前边界：详情只读，不提供逐条撤销、逐条导出、字段级 diff 高亮或跨设备同步；集合历史仍只保留本地最近 20 条。

## Clip Inbox 集合操作历史字段差异高亮

- 详情按集合 ID 对齐操作前 / 操作后的安全摘要，逐字段比较标题、标签、收藏 / 归档状态、排序方式和选段数量；集合 ID 本身保持中性，不参与变化提示。
- 同一字段使用 `unchanged`、`changed`、`added`、`removed` 四种状态；变化字段显示对应四语标签和轻量色彩边界，新增 / 删除集合的单侧字段也能明确标注。
- 差异计算独立为纯函数，Renderer 只负责展示，不扩大历史详情协议；仍不比较媒体路径、证据文本或选段内容，只把选段数量作为安全摘要字段。
- 真实 Electron Smoke 已验证收藏操作后的状态字段出现差异高亮、详情安全字段不泄露、关闭详情和 Renderer `consoleErrors: 0`；截图输出到 `/private/tmp/aivplayer-collection-history-diff.png`。
- 验证合同：差异纯函数 3 项、历史详情 UI 接线 2 项通过，`npm run typecheck`、`npm run build` 和宿主权限下全量回归 329 个测试文件 / 1230 项通过。
- 分阶段提交：`a7394527 feat(选段集合) : 增加历史字段差异计算`（共享类型、纯函数和单测）；`76d1cfc2 feat(选段集合) : 高亮历史字段差异`（Renderer、样式和四语文案）；`ad65fbca test(选段集合) : 验证历史字段差异`（UI 接线与真实 Electron Smoke）。
- 当前边界：仅对当前历史详情已返回的安全摘要做字段级提示，不提供逐字段撤销、逐字段编辑、差异导出或跨设备同步。

## Clip Inbox 集合操作历史逐条撤销与重做

- 集合操作历史的每条记录现在可独立执行“撤销此操作”或“重做此操作”，顶部原有的“最近一次”撤销 / 重做入口保持兼容。
- 指定操作通过历史 ID 复用现有 SQLite 快照和 IPC / Preload 协议；只有已应用记录可撤销、可重做记录可重做，执行成功后刷新集合、历史状态并关闭过期详情。
- 所有操作类型继续执行快照冲突保护；收藏 / 归档额外校验操作后 flags 与更新时间，较早条目遇到后续修改时只返回失败提示，不覆盖当前集合。
- 四语言新增逐条操作标签、成功提示和冲突提示；历史行按 `active` / `redoable` 状态显示对应动作，已撤销记录不提供重复操作按钮。
- 真实 Electron Smoke 已验证打开详情、逐条撤销、状态变为可重做、逐条重做、状态恢复可撤销、详情刷新和 Renderer `consoleErrors: 0`；截图输出到 `/private/tmp/aivplayer-collection-history-targeted-undo-redo.png`。
- 验证合同：Store / 协议定向测试 56 项、UI 接线测试 3 项通过，`npm run typecheck`、`npm run build` 和宿主权限下全量回归 329 个测试文件 / 1232 项通过。
- 分阶段提交：`b4c1c3c4 feat(选段集合) : 支持指定历史撤销重做`（Store、flags 冲突保护和回归）；`6e94f28b feat(选段集合) : 接通指定历史操作协议`（IPC、Preload 和接线回归）；`f3012ef9 feat(选段集合) : 增加历史条目操作`（Renderer、样式和四语文案）；`b2cd62ab test(选段集合) : 验证历史条目操作`（UI 接线和真实 Electron Smoke）。
- 当前边界：逐条操作仍是只读历史回放，不提供批量选择撤销、跨设备同步或云端协作；发生快照冲突时不会强行覆盖用户后续数据。

## AI 服务多配置档案与独立设置分类

- AI 服务从字幕设置独立为一级设置分类（新「AI 服务」tab），与界面、视频、字幕等并列，统一管理翻译和总结使用的 AI 服务。
- 支持多配置档案：内置托管服务 + 多个自定义 OpenAI-compatible 服务，可新增、编辑、删除档案并随时切换激活档案；激活档案决定翻译请求走向。
- 旧版单服务配置（schemaVersion 29 的 custom 模式 baseUrl / model / apiKey）在升级时自动迁移为一条自定义档案并置为使用中，术语表等其余配置不受影响；迁移兼容旧版加密存储的 API Key。
- API Key 仍加密存储在本机，不以明文写入普通设置文件；档案编辑中测试连接失败不会落盘生效。
- AI 引导弹窗同步支持档案切换，与设置面板共用同一套激活档案状态。
- 移除废弃的服务模式切换 UI 与对应的多语言文案键，四语言（zh-CN / en-US / ja-JP / ko-KR）AI 服务相关文案完整无缺键。
