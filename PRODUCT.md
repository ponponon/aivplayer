# Product

## Register

product

## Users

本地视频播放、字幕生成、翻译和视觉媒体库用户，通常在桌面端处理自己的视频素材；他们希望在等待模型、转码或 AI 任务时继续掌握任务状态，而不是猜测应用是否卡住。

## Product Purpose

AIVPlayer 是一个 local-first 的跨平台 AI 视频工作台，把播放、字幕、视觉检索和编辑集中在一个桌面应用中。成功意味着用户能清楚知道本地任务正在做什么、还要等待多久，并能放心地把媒体和工程留在自己的设备上。

## Brand Personality

专业、克制、可靠。界面应让长时间运行的本地任务显得可理解、可预期，不用夸张装饰制造注意力。

## Anti-references

不要做成高饱和的下载器仪表盘、充满无意义动效的 loading 页面，或只显示一条没有数字反馈的“假进度”横条。不要用颜色作为唯一的状态表达，也不要为了显示数据把紧凑的播放器工作区撑得过高。

## Design Principles

- 先回答用户最关心的问题：任务在进行什么、进度到哪里、还要等多久。
- 状态透明但不打扰工作：增加信息密度时保持播放器工作区的紧凑节奏。
- 不确定时诚实降级：没有总大小或速度不足以估算时，明确显示“正在计算”，不伪造 ETA。
- 同一类状态在不同任务入口使用一致的视觉和文案语言。
- 进度不依赖颜色：数字、文本和语义化状态共同传达反馈，并兼容减少动效设置。

## Accessibility & Inclusion

按 WCAG 2.1 AA 的默认方向处理：状态不能只靠颜色表达，进度区域使用可读的 status 语义；窄窗口下允许内容换行，不裁切关键数据；动效支持 `prefers-reduced-motion: reduce`。
